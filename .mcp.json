{
  "mcpServers": {
    "fetch": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@modelcontextprotocol/server-fetch"],
      "description": "Fetch web content — used for ASF scraping and external data sources"
    },
    "filesystem": {
      "command": "cmd",
      "args": [
        "/c", "npx", "-y",
        "@modelcontextprotocol/server-filesystem",
        "D:/Python/yangwenli/original_data",
        "D:/Python/yangwenli/backend/backups"
      ],
      "description": "Structured read access to original_data/ COMPRANET CSVs and backups"
    }
  }
}
