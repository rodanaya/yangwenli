/**
 * DetectivePatterns — redirected to Administrations
 * Content merged into Administrations > Patterns tab
 */
import { Navigate } from 'react-router-dom'

export default function DetectivePatterns() {
  return <Navigate to="/administrations" replace />
}
