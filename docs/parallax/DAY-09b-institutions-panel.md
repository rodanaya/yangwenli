# PARALLAX Day 9b — four-perspective panel on `/institutions` + `/institutions/:id`

Panel run 2026-09-23 22:55–23:30 CET on the Day 9 ship (`d408357e`, BUILD_ID `2026-09-23-parallax-d9-institutions`): four read-only Opus judges against the local stack, shared brief `_parallax_shots/day09/panel/BRIEF.md`, verdicts + evidence in `_parallax_shots/day09/panel/{reader,architect,a11y,dataviz}/`. **Scores: reader 62 · architect 71 · a11y 70 · dataviz 64.** Every finding below was re-verified by Fable in code, on the local API or in the DB (read-only) before it entered a change; the verified `file:line` is the one cited.

**Classification stays GOLD — enhance in place.** Day 9's mechanics hold (the judges' Keep lists repeat the plate, the cards, the honor roll, the boleta, the seals, the register, the keyboard layer, the phone cards). What the panel found is a **truth layer** Day 9's mechanics-only audit could not see: a broken filter, four false printed numbers, one inverted Spanish sentence, a caption that describes a population the plate does not draw, and a scoring input that penalises every large buyer.

## Findings folded into this plan (re-verified)

| # | finding | judges | verified |
|---|---|---|---|
| F1 | **Tier filter is broken on prod.** `?grade=B&scope=federal` → HTTP 500 (`grade=B&scope=all` → 200; `grade=B&sector=…` → 200). The grade-distribution query zips `params` against `where_clauses[1:]`, which misaligns whenever the scope clause carries no parameter. The error box has no `role=alert`; stale rows stay under the active pill. | X1 | `scorecards.py:503`; reproduced locally AND on rubli.xyz (one GET each) |
| F2 | **Red flag #1 prints "National percentile 50".** CENSIDA's DB value is `0.0`; `round(r[8] or 0.5, 3)` turns a true zero into 0.5 (3 rows at 0.0). | R1 | `scorecards.py:521,:693`; DB `institution_scorecards` 1425 → 0.0 |
| F3 | **P90 prints 0 / 1.** `p90_risk_score.toFixed(0)` on a 0–1 value (IMSS 0.421 → "0", CENSIDA 1.0 → "1"); every other risk figure on the page is /100. | R2 V1 A6 | `PillarBoleta.tsx:89` |
| F4 | **The thin-sample sentence is false and the headline counts a population the plate does not draw.** `n = 12` rows say "the headline excludes it"; the masthead (476 / 120), FINDING (11 %) and plate caption/median (476 · 46.1) come from `getInstitutionStats({scope})` with no floor, while the plate draws the 445 floored strokes (`useLeagueField` `MIN_CONTRACTS = 30`). Floored: 445 · 116 · 8.8 % · median 45.8. | R3 A3 V5 | `InstitutionLeague.tsx:522-526,:650,:704-711`; `SpectralRegister.tsx:190`; `useLeagueField.ts:29`; API with/without `min_contracts=30` |
| F5 | **The masthead window "FEDERAL · 2018–2025" is static; the scorecards have no year filter.** Honor-roll #1 UNAM has 40 contracts, all in 2003 (13.4M MXN), "Percentile 100". | R4 | `en/es institutionleague.json:3`; `compute_scorecards.py:149-168`; DB 612 → (2003, 2003, 40) |
| F6 | **Spanish inverts the plate's axis.** ES lede «La posición … es el indicador de riesgo» and the plate's ES `aria-label` «trazadas por indicador de riesgo» — the axis is the integrity score (right = better); EN says so. | R5 X2 | `es/institutionleague.json:12`; `SpectralRegister.tsx:230` |
| F7 | **The dossier mixes two contract bases and states no gap.** Hero + stat strip print `institutions.total_contracts` 673,326 / 1.8T; every panel below (vendor pool, categories, EU rows, waterfall) uses `institution_stats` 662,001 (CENSIDA 636 vs 634 / 25.3B vs 24.0B). CLAUDE rule 4 names `institution_stats` canonical. | A2 R8 V-Q2 | `institution_service.py:173-179,:209`; DB both tables |
| F8 | **The reconciling sentence is canned and false for IMSS.** «…signal is one of process — concentration and direct award — not pricing» fires for every low-model-risk institution; IMSS's HHI is 110 and direct award is a *protective* −0.003 in its own waterfall. And "Process" names two measures: seal «Process · no open bid 68 % · WEAK» vs boleta «P · PROCESS 16/25» (the Process-Integrity pillar). | V2 V3 R6 A-Q1 | `InstitutionDossierBody.tsx:100-102`; `InstitutionHero.tsx:298,:317`; `institution-pillars.ts:51`; `api_251__risk-waterfall.json` |
| F9 | **«What they buy» disagrees with `/categories/:id` and states no coverage.** IMSS Medications 173,351 · 709.3B on the dossier (matches the DB) vs 145,131 · 680.1B on `/categories/20` (`precomputed_stats['category_top_institutions']` dated 2026-06-09); the six rows cover 76 % of contracts / 81 % of spend with no "6 of 71" line. | A4 V8 R8 A8 | `categories.py:1168-1191`; `_precompute_category_enrichments.py:160-163`; `InstitutionDossierBody.tsx:463` |
| F10 | **Register share bars have no common base; "Current HHI" names no year.** `DotBar max={Math.max(20, share)}` per row (CENSIDA 41 % → 18 dots, 10 % → 9); header "43 % / 80 %" on the vendor-pool base vs rows on the institution total; «110 CURRENT HHI» is the last active year (2025 IMSS / 2022 CENSIDA), «top 10 hold 16 %» is all years, «3,975 SUPPLIERS» is 2025 beside an all-years register. AT hears "HHI 865-yr avg" (no space). | V7 A5 R8 X5 | `InstitutionDossierBody.tsx:233,:264-270,:321,:345,:366` |
| F11 | **Four IMSS "largest contracts" exceed the 10B review line with no mark** (14.8B "Servicios de basura municipal…", 2009, risk 100). The data rule says FLAG; `FLAG_THRESHOLD` has zero call sites in `.tsx`. | A7 R10 | `InstitutionDossierBody.tsx:564`; `constants.ts:315` |
| F12 | **Backend prose leaks across languages; officers' risk is 0–1.** EN page prints the Spanish officials note ("Responsable de la Unidad Compradora · 2018+ … no es una acusación"), ES gets English "Structure"; risk column 0.26 while the page prints 24, 30, 97. | R9 | `institutions.py:2305`; `InstitutionDossier.tsx:439,:451-452` |
| F13 | **Risk over time on a 0–100 % axis for a /100 score; a 1-contract 2004 point; 2025 unmarked.** IMSS line fills 11.5 of 54 px. | V9 | `primitives.tsx:183-194` |
| F14 | **ReportCard DA bullet labelled "EU line 10 %" but benchmarks 0.25** → "↓1pp" for a 24 % rate. | V10 | `ReportCard.tsx:230-231` |
| F15 | **Plate: the empty-Excellent note sits over Satisfactory/Regular (60 % at 1440; at 390 it crosses two unlabelled bands); 319/445 strokes at the 3 px floor including true zeros; strokes at opacity .55 measure 1.65–1.82:1 against their band; `domainMax = 86` makes the Excellent band 6 units wide.** | V4 V6 R7 | `SpectralRegister.tsx:94,:103,:271,:287-298,:319` |
| F16 | **The tier ink ramp collapsed at the cool end.** Excelente `#3d5a80` vs Satisfactorio `#3f5f86`: ΔE 2.1 (one colour in type); all five inks at L* 38–41 (hue-only ramp); every ink passes AA (5.5–6.7). Regular→Deficiente ΔE 12.6 holds. | all four Q4 | `tiers.ts:54-88`; `q4_inks.txt` ×3 |
| F17 | **A11y: pills expose no pressed state and the active sector pill uses the raw sector hex as text (10 of 12 fail, Energy 1.69); 50 identical "Show pillar breakdown" buttons placed before the name; nothing announced on sort/page/filter/floor (0 live regions), focus left off-screen after a tier pill (Act I unmounts above it); drop cap hides the first letter ("nstituto Mexicano…"); officers scroller has no keyboard path at 390; ES share-bar labels in English; radiogroup without roving tabindex; unnamed DualSeal `<aside>`.** | X3 X4 X6 X7 X8 X9 X10 | `InstitutionLeague.tsx:639,:978,:1064,:1172-1176,:1343,:1440`; `InstitutionHero.tsx:207-223`; `InstitutionDossier.tsx:408`; `InstitutionDossierBody.tsx:366,:368,:541` |
| F18 | **Scoring input defect (decision, not a change): External Flags counts contracts, not vendors.** `_load_institution_aria` does `SUM(CASE WHEN ips_tier = 1 …)` over the `contracts` join — IMSS stores `aria_t1_vendors` 42,128 while the DB has ~100 distinct T1 vendors at IMSS (299 nationwide); `t1_rate = 42,128 / 3,975` saturates the 4-point penalty for every large buyer. The stored `pillar_process` 6.4/15 would be ≈12.5 with distinct vendors → IMSS ≈55 not 49.0 (C → C+). The code's own comment says the count-based version "mechanically penalized large buyers" — this is the same defect one join deeper. | A1 | `compute_scorecards.py:253-262,:423-427`; DB `COUNT(DISTINCT vendor_id)` at IMSS with `ips_tier = 1` = 107 |

Not folded (→ backlog): plate leader lines behind other callouts; `reporte` 390 `BenchmarkRow`; `risk-profile` 16.6 s; `useLeagueField` 5 requests; 9 dossier queries; expanding a row re-renders the plate twice (memoise `SpectralRegister`); "Percentile 100" / "Worse than 100 % of its sector" include the institution itself (R-Q3, folded into Change 3 as copy); the trend `—` glyph means stable on 49 of 50 rows (folded into Change 3).

## Keep (from the four judges, not up for re-litigation)
The masthead sentence shape; the FINDING callout; the plate (strokes, bands, median, leader-lined callouts, the tooltip); the five `ActaCard`s with the 60 px numerals and the "Weakest pillar · Worse than · X at risk · N red signals" agate; "Honor roll — with reservations"; the pill filters with their dots; the table's columns, the tier tile as verdict, the `PillarBoleta` row-expand; the phone card with the spelled-out weakest pillar; the zero sentence worded once; the `DualSeal` composition with the EU multiple sub-line; the stat strip's `6.8× EU / ≤20 % EU` sub-lines; the boleta's 56 px score + five bars; the § kickers and the provenance sentence; «What they buy» as chips with count · DA · risk indicator; the ES "AD"; the keyboard layer (140 ringed stops, `aria-sort`, `aria-expanded`/`aria-controls`, named SORT/FLOOR groups, `RiskOverTimePanel` as a named image, the `<ol>` card list, the global reduced-motion rule).

## Decision for the user — F18 (the External Flags pillar)
Fixing the join changes the External Flags pillar for most large buyers and therefore their total score, tier and rank (IMSS ≈ +6, C → C+). That is a regrade of the public transparency ranking, so it is **not** shipped by this day. Change 0 below fixes the code and produces a dry-run table; the rescore (`python -m scripts.compute_scorecards` on the local DB → dump → prod load) waits for the user's yes.

## Change (8 + Change 0)

### 0. The pillar input, code-only + dry run — `backend/scripts/compute_scorecards.py`
- `_load_institution_aria` → `COUNT(DISTINCT CASE WHEN aq.ips_tier = 1 THEN c.vendor_id END) AS t1` (and t2), matching the neighbouring EFOS/GT loaders that already count distinct vendors; the stored key stays `aria_t1_vendors`. Add `--dry-run` to the script: computes every institution's new pillars/total/grade in memory, writes **no** row, prints and saves `_parallax_shots/day09b/rescore-dryrun.md`: count of institutions whose grade changes (by from→to), the 20 largest movers (id, name, old → new total, old → new grade), IMSS and CENSIDA before/after, the new grade distribution vs the current one. One test: the loader returns distinct-vendor counts on the fixture (`t1 ≤ vendor_count`).
- Accept: the dry-run file exists with those tables; `institution_scorecards` unchanged (row count + a checksum of `total_score` before/after); the test passes; **no rescore, no prod load**.

### 1. Backend truths — `backend/api/routers/scorecards.py`, `backend/api/services/institution_service.py`, `backend/api/routers/institutions.py`, `backend/api/routers/categories.py` (+ `scripts/_precompute_category_enrichments.py` run)
- F1: build the no-grade distribution query from the same `(clause, params)` pairs as the main query (keep a list of `(sql, [params])` per clause, drop the grade pair, flatten) — never `zip` clauses to a flat params list. Tests: `grade=B&scope=federal` 200 with `grade_distribution` present; `grade=B&sector=1&scope=federal` 200; `scope=all` unchanged.
- F2: `national_percentile` → `round(r[8], 3) if r[8] is not None else None` (both sites); the frontend already prints `—` for null. Test: an institution stored at 0.0 returns 0.0.
- F7: the detail response's `total_contracts` / `total_amount_mxn` / `vendor_count` come from `institution_stats` (COALESCE stats, institutions) — the same base every other panel uses; the hero/strip then print 662,001 / 1.8T; `vendor_count` is currently `null` in the detail payload (`InstitutionHero.tsx:555-560` frame 1 never fires) → fill it from `institution_stats.vendor_count`. Test: detail `total_contracts` == `institution_stats.total_contracts` for 251.
- F9: `top-categories` response gains `total_categories` (count of the institution's category rows), `total_contracts_categorised` and `total_value_categorised` (sums over all its rows) so the panel can print "6 of 71 · 76 % of contracts · 81 % of spend"; the `category_top_institutions` precompute (June 9) is recomputed by running `_precompute_category_enrichments.py` on the local DB and exported as `_parallax_shots/day09b/precompute/category_top_institutions.sql` (the `precomputed_stats` rows for that key only) — Fable loads it on prod before the deploy. Test: `/categories/20/top-institutions` IMSS row == `institution_category_stats` (251, 20) contract count.
- F12: the officials `note` becomes two fields `note_es` / `note_en` (or the frontend keeps the text in i18n and the backend sends none — executor's call; report); `avg_risk_score` stays 0–1 in the API, the page prints `Math.round(× 100)` (Change 4).
- Accept: the five tests green + `-k "institution or scorecard"` green; prod-shape curls locally: `grade=B&scope=federal` 200; `/scorecards/institutions/1425` `national_percentile` 0.0; `/institutions/251` `total_contracts` 662,001 and `vendor_count` non-null; `/institutions/251/top-categories` carries the three totals; the SQL dump loads into a scratch copy.

### 2. One population — `InstitutionLeague.tsx`, `SpectralRegister.tsx`, `i18n/{en,es}/institutionleague.json`
- The masthead h1, the FINDING and the plate caption/median/aria use `getInstitutionStats({ scope, min_contracts: RELIABLE_MIN })` (445 · 116 · 8.8 % · 45.8 today) — the same population the plate draws and the honor roll / red flags use; the full table keeps listing everyone with its `n = 12` marker, whose sentence becomes true: "Below the 30-contract reliability floor — listed here, excluded from the headline, the plate, the honor roll and the red flags" / ES twin. The results count under the search stays the table's own total (476).
- F5: the masthead `meta` "FEDERAL · 2018–2025 · v0.8.5" → "FEDERAL · 2002–2025 · v0.8.5" (the scoring window; verify against `compute_scorecards.py` — if a window exists, print it); rows whose `last_contract_year < 2018` get a second mono marker "last contract 2003" (`title`/`aria` "No contracts since 2003 — outside the 2018+ officials window") next to `n = …`; the scorecards list payload gains `last_contract_year` from `institution_stats` (backend, one column in the SELECT at `scorecards.py:480-499` — include in Change 1's tests).
- F6: ES lede «La posición en la placa de abajo es el **puntaje de integridad** (más a la derecha, mejor); la altura del trazo es el dinero en riesgo.»; ES plate aria «trazadas por puntaje de integridad, de 20 a 100 (más a la derecha, mejor)»; grep every ES string on the two pages for "indicador de riesgo" applied to the score (the risk indicator wording stays for model risk).
- Accept (probe EN + ES 1440/390): dump contains "445" in the h1 and the plate caption and "median 45.8" / "mediana 45.8"; FINDING says "9 %"; the thin-sample title text is the new sentence; UNAM's row carries "last contract 2003"; no ES leaf says "indicador de riesgo" inside the masthead or the plate aria; `meta` shows 2002–2025.

### 3. The boleta and the card copy — `PillarBoleta.tsx`, `InstitutionLeague.tsx`, `i18n/{en,es}/institutionleague.json`
- F3: P90 → `Math.round(p90 * 100)` ("P90 risk indicator 42"); EN/ES.
- Percentile: null → `—`; thin rows (`n < 30`) print the confidence band instead of a percentile in the table cell ("wide" → see below); "Percentile 100" → cap the display at 99 (`Math.min(99, …)`) and "Worse than 100 % of its sector" → "Lowest in its sector" / «La más baja de su sector» when the value rounds to 100.
- Confidence bands: the backend ladder is an interval width (wide/medium/narrow/high); print it as confidence: wide → "low" / «baja», medium → "medium" / «media», narrow → "high" / «alta», high → "very high" / «muy alta» (replace the Day 9 judge-round `confidenceBands` values).
- Trend: the stable glyph `—` (49 of 50 rows) collides with the missing-data dash → stable renders `→` (lucide `MoveRight`) with its `aria-label`; `—` stays for null only.
- Phone card: add the sector label (the table has it) and the word "weakest" before the pillar ("Weakest · Competitive Openness 20/30").
- Accept: dumps contain no "P90 risk indicator 0|1" (EN) / "P90 0|1" (ES); CENSIDA's boleta shows no percentile-50; "Confidence: low" on El Llano's expanded row; no "Percentile 100"; the trend column has no `—` on rows with `trend_direction` present.

### 4. One reading on the dossier — `InstitutionHero.tsx`, `InstitutionDossierBody.tsx`, `InstitutionDossier.tsx`
- F8: the second seal's label → "Procedure · no open bid" / «Procedimiento · sin licitación» (the word "Process" belongs to the boleta's pillar); the boleta pill on the dossier prints the letter grade with the tier: "C · ADEQUATE" / «C · REGULAR» (the letter is unambiguous where the tier word is not; the EN tier word stays — a rename is the user's call, note it in the report).
- The reading caption (`:100-102`) is generated from the data only: "The model weighs «institution diversity» downward and «vendor concentration» upward" + one sentence from the three verdicts: "Its contracts sit near the platform mean (24/100); it awards 68 % without an open bid, 6.8× the EU line; the transparency score is 49 of 100, grade C." — every number from `institution`, `scorecard`, the seal's `top` ratio; no hard-coded "concentration and direct award — not pricing" clause anywhere.
- The lede's frame 1 (`hr >= 20 && vendors`) now fires when `vendor_count` arrives (Change 1); check both dossiers' ledes read (IMSS frame 2, CENSIDA frame 1).
- F12: officers note from i18n by `lang` (drop the backend string); risk column `Math.round(avg × 100)` with the `RISK_TEXT_COLORS` ink.
- Accept: dossier dumps EN + ES contain "Procedure ·" / «Procedimiento ·», "C · ", the generated sentence with 24 / 68 / 6.8 / 49 for IMSS and the CENSIDA values; no "not pricing"/«no de precio» anywhere; officers note in the page language only; officers' risk printed /100.

### 5. The register, the HHI strip, the categories, the largest contracts — `InstitutionDossierBody.tsx`, `primitives.tsx`, `ReportCard.tsx`
- F10: share `DotBar`s share one base — `max = max(20, top1)` for every row (same for all), shares computed on the vendor pool's `institution_total` (the header sentence's base) so header and rows agree; `HhiStat` prints "HHI 2025" / "5-yr avg 2021–25" / "Suppliers 2025" with the year(s) from `history`, and the register sentence gains "· all years 2002–2025"; a real separator between value and label (`{value} · {label}` or `aria-label`) so AT reads "86, 5-yr avg".
- F9: «What they buy» caption "6 of 71 categories · 76 % of contracts · 81 % of spend" from the new totals; category chip stays.
- F11: any contract row with `amount_mxn > FLAG_THRESHOLD` gets a mono "⚑ review" mark (`title` "Above the 10B MXN review line — include but verify") in `RISK_TEXT_COLORS.high`; the same helper on the register's amounts if any row exceeds it.
- F13: `RiskOverTimePanel` y axis "/100" not "%" (the series is the avg risk score × 100; label ticks 0 · 50 · 100 with a "/100" axis caption), the last year `2025*` marked partial via `PARTIAL_YEAR_NOTE`, and years with `< 5` contracts drawn hollow/omitted if the timeline payload has counts (check `risk-timeline` — if it lacks counts, mark only 2025 and report).
- F14: `ReportCard` DA benchmark 0.10 to match its "EU line 10 %" label (or the label to 25 % if 25 is the intended national anchor — read `EU_DA_ANCHOR_PCT` and pick the one the sector rows use; report which).
- Accept: CENSIDA register row dots monotonic with share (41 % ≥ 4× the 10 % row's dots); IMSS header "top 10 hold 16 %" and Σ of the top-10 rows agree within 0.5 pp; HHI strip dump has the years; "6 of 71" line present on both dossiers; ≥ 4 "⚑ review" marks on IMSS's largest contracts; axis caption "/100"; ReportCard DA row consistent with its label.

### 6. The plate — `SpectralRegister.tsx`
- F15: `domainMax` 86 → **100** (the Excellent band becomes 80–100 and the note fits inside it at every width; the axis prints 20 · 40 · 60 · 80 · 100); the empty-Excellent note is seated **inside** the Excellent band (right-anchored with its measured width ≤ band width at 1440; at 390 it becomes a caption line under the plate instead of svg text, and the band names return at 390 as a one-line legend row under the axis: Deficient · Adequate · Satisfactory · Excellent in their inks); zero-money strokes render as 1 px ticks in `var(--color-text-muted)` (distinct from the 3 px floor for > 0), stroke opacity .55 → .8 (marks, not text; report the measured ratio vs the band wash); the callout mechanic and the median stay.
- Accept: the note's bbox ⊂ the Excellent band bbox at 1440/1024 EN + ES; at 390 no svg text over the bands; the legend row present at 390; zero strokes distinguishable (report count and height); `svgTextContrast` still 0; the callout checks from Day 9 still pass (0 overlaps / outside / clipped).

### 7. A11y — `InstitutionLeague.tsx`, `InstitutionHero.tsx`, `InstitutionDossier.tsx`, `InstitutionDossierBody.tsx`
- F17: pills get `aria-pressed`; the active sector pill's text uses `getSectorTextColor(code)` (marks keep the sector hex on the dot/border) and the accent-data selected state uses an ink ≥ 4.5 on its tint; the expand button is named "Show pillar breakdown — {{name}}" / «Mostrar desglose de pilares — {{name}}» and rendered **after** the name link in the cell; one polite `role=status` region announces "{{num}} institutions · page {{page}} of {{total}} · sorted by {{label}}" on every list change (and the dossier's FLOOR change "{{n}} suppliers shown"); the error box gets `role=alert`; after a pill/sort/page click, focus moves to the results heading (`league-table-heading`, `tabIndex=-1`) so it is never off-screen; the drop cap keeps its visual but the paragraph exposes the whole word (CSS `::first-letter` on the `<p>` — the Day 5b backlog pattern — or an `sr-only` first letter + `aria-hidden` glyph); the officers scroller `tabIndex={0} role="region" aria-label`; ES `ariaLabel`s on the share bars and the largest-contracts table; the register's risk numeral gets its noun ("risk indicator 100" in the accessible text); the scope radiogroup gets a roving `tabIndex` with ←/→; the `DualSeal` `<aside>` gets `aria-label` "Verdicts" / «Veredictos».
- Accept (probe EN + ES 1440/390): `pressedCandidates` = 20 pills all carrying `aria-pressed`; active sector pill text ≥ 4.5 on 12/12; `dupNames` 0; `liveRegions` ≥ 1 and the Tab walk after a pill click lands on the results heading (`activeElement` inside the viewport); AT text of the lede starts with the full first word; the officers scroller is a Tab stop at 390; `role=alert` present when the list errors (force by hitting the pre-fix 500 in a test or a stubbed route); radiogroup one Tab stop with arrows.

### 8. The tier ink ramp — `lib/tiers.ts` (+ every site that reads `TIER_STYLES.ink`)
- F16: Satisfactorio `ink` → a lighter steel that keeps ≥ 4.5 on `--color-background-elevated` (the judges' candidate `#4a6d99` measures 5.06 / 4.72; a touch lighter is acceptable down to 4.5) and Excelente stays `#3d5a80` or darkens toward L* 30 (`#2c4566`) so Excelente→Satisfactorio ΔE ≥ 10 and the five inks climb in L* (Crítico darkest → Excelente… no: the ramp must read cool-strong → warm-weak; report the five L* values and every neighbour ΔE); pills, tiles, band labels, boleta pill and the card score all follow through the token (no per-site hex).
- Accept: six ratios ≥ 4.5 on both papers (report); ΔE Excelente→Satisfactorio ≥ 10; crops of the five pills at 1440 and the plate band labels EN + ES read as five distinct inks.

## Out of scope (→ PARALLAX backlog)
- F18 rescore (user decision; Change 0 prepares it).
- EN tier word "Adequate" for `Regular` (backend label "Preocupante") — rename is the user's call; Change 4 adds the letter grade beside it.
- Plate leader lines behind other callouts; memoise `SpectralRegister` (2 re-renders per row expand); `reporte` 390 `BenchmarkRow`; `risk-profile` 16.6 s; `useLeagueField` 5 requests; 9 dossier queries; the honor roll's recency floor (policy).

## Risk
- Change 1 touches four backend files: every edit ships with a test; run `-k "institution or scorecard"` after Change 1 and the full suite once at the end with no probes in flight (52 min).
- Change 2 moves the headline numbers (476 → 445, 11 % → 9 %): the day file records both; the FINDING i18n keys keep their shape.
- Change 6 changes the plate's domain: the callout checks from Day 9 must be re-run; never write state in render.
- Change 0 must not write to `institution_scorecards` (assert row count + checksum in the test).
- The `category_top_institutions` precompute reload on prod is Fable's step, in-container, before the deploy (Day 8/9 pattern).

## Build notes for the executor
Same stack and rules as `DAY-09-institutions.md § Build notes`: worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, create branch `parallax/day09b-institutions-panel` from `origin/main` (`725df53b`), servers running (backend `127.0.0.1:8001` no-scan — **restart it after Change 1**; Vite `localhost:3009`), ≤ 2 browsers, `MSYS_NO_PATHCONV=1` + `D:/` script paths, never rubli.xyz. Order: **1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 0**. One commit per change: `fix(institutions § PARALLAX D9b § Change N): …` (Change 0: `fix(scorecards § PARALLAX D9b § Change 0): …`), body cites `docs/parallax/DAY-09b-institutions-panel.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Progress file `_parallax_shots/day09b/progress.md` (one line after EVERY step); the report goes in your final message (a hook blocks report.md). Probes: copy `day09/audit9.mjs` → `day09b/audit9b.mjs` and add the checks named in each Accept (population numbers in the h1/caption, `pressedCandidates`, `dupNames`, live regions + focus after a pill click, the note ⊂ band bbox, zero-stroke heights, P90/percentile/trend strings, the HHI strip text, "⚑ review" count, the generated reading sentence); run EN and `LANG_ES=1` at `WIDTHS=1440,1024,390`; census `day04/clipcensus4.mjs` on the four routes → 0 at ≥ 1024. Crops (`day09/crop9.mjs` → `day09b/crop9b.mjs`): masthead + FINDING, plate (1440 + 390), pills active state, table rows 1–10 with a thin row and UNAM, expanded row (P90, confidence), phone cards; dossier hero + seals (IMSS, CENSIDA), boleta, reading caption, HHI strip + register rows 1–8, «What they buy» with its caption, largest contracts with ⚑, officers; at 1440 and 390, EN + ES. **LOOK at every crop.** Review: `rubli-bilingual-audit` on every touched TSX; gates from `frontend/`: tsc (`tsconfig.app.json`) · build · lint:tokens · eslint on touched files; backend: `-k "institution or scorecard"` after Change 1, full suite last. Do NOT rescore, bump BUILD_ID, push, deploy or merge — Fable judges first.

## Result

Built by Opus executor `parallax-day09b` (Changes 1 → 8 + Change 0 dry run + a crop pass, one commit per change), judged by Fable on the `_parallax_shots/day09b/after/` crops (plate 1440 + 390, pills montage, expanded row, IMSS hero/reading, ES masthead) and on the executor's after-probe (`after-{en,es}.txt`, `rerun-{en,es}.txt`; the ES 390 first run tripped the backend's own 100-req/min limiter — 429s, not a defect — and was re-run clean). Judge round 1 `8aa546d5`: J1 scope-aware headline noun («state and municipal buyers» / «buyers on CompraNet» — the subnational and all boards had said "federal buyers"), J2 zero-share rows drew one dot (18 rows → 0), J3 the plate's callouts skipped the #3 exposure (CFE 402.9B) after three failed lifts → seat ladder with side offsets, **8/8 callouts in exposure order at 1440 EN + ES, 3/3 at 390**, 0 overlaps / outside / over-text / clipped. Ship commit also drops the hard-coded «El» before the institution name in the ES lede («El Secretaría…»). Gates: tsc 0 · build OK · lint:tokens PASS · eslint 0 new · bilingual audit 9 files clean · backend **907 passed** (1:05:34, full suite, no probes in flight).

| Finding | before | after |
|---|---|---|
| F1 tier filter | `?grade=B&scope=federal` **500** (prod) | 200 + `grade_distribution`; error box `role=alert` |
| F2 percentile | CENSIDA "National percentile 50" (`or 0.5`) | `0.0` returned; null → `—` |
| F3 P90 | "P90 risk indicator 0" / "1" | 42 (IMSS) · 100 (CENSIDA) on the /100 scale |
| F4 population | h1 476 · FINDING 11 % · caption/median 476 · 46.1 vs plate 445 | **445 · 9 % · 116 · median 45.8** everywhere the plate draws; thin-row sentence "listed here, excluded from the headline, the plate, the honor roll and the red flags" |
| F5 window | masthead "2018–2025"; UNAM #1 with 40 contracts from 2003 unmarked | "2002–2025"; **44** "last contract YYYY" marks on page 1 (UNAM "last contract 2003") |
| F6 ES axis | «indicador de riesgo» for the score (lede + plate aria) | «puntaje de integridad (más a la derecha, mejor)» |
| F7 contract base | hero/strip 673,326 vs panels 662,001; `vendor_count` null | **662,001** everywhere; 27,548 vendors; the lede's frame 1 can fire |
| F8 one reading | canned "concentration and direct award — not pricing"; "Process" ×2 | generated: «…low model risk indicator (24/100); it awards 68 % without an open bid, 6.8× the EU line; the transparency score is 49 of 100, grade C»; seal «Procedure · no open bid»; boleta pill «C · Adequate» |
| F9 categories | `/categories/20` IMSS 145,131 (June precompute) vs dossier 173,351; no coverage line | precompute rerun (IMSS 173,351); «6 of 71 categories · 76 % of contracts · 81 % of spend» |
| F10 register/HHI | per-row bar base; "Current HHI"; "HHI 865-yr avg" to AT | one base (header and rows agree), «HHI 2025 · 5-yr avg 2021–25», register sentence «all years 2002–2025», separators |
| F11 review line | 4 IMSS contracts > 10B unmarked | 4 × «⚑ review — Above the 10B MXN review line» |
| F12 officers | Spanish note on the EN page; risk 0.26 | note per language; risk 26 (/100) |
| F13/F14 axes | risk-over-time in %, 2025 unmarked; ReportCard "EU line 10 %" vs 0.25 | «/100» axis + `2025*`; benchmark matches its label |
| F15 plate | note over Satisfactory; `domainMax` 86; zero strokes = 3 px floor; opacity .55 | domain **20–100**, note inside the Excellent band (caption + legend row at 390), zero-money ticks under the line, opacity .8 |
| F16 inks | Excelente/Satisfactorio ΔE 2.1 | five distinct inks (montage), all ≥ 4.5 on both papers |
| F17 a11y | 0/20 `aria-pressed`; sector pill text 1.69–3.9; 50× "Show pillar breakdown"; 0 live regions; focus off-screen after a pill; "nstituto…" | pressed 19/19 (+ "all" states), 12/12 sector pills ≥ 4.5, unique names after the link, 1 polite status region («476 institutions · page 1 of 10 · sorted by Score»), focus → results heading in viewport, roving radios, full first word to AT, scroll regions as Tab stops |
| F18 pillar (decision) | `aria_t1_vendors` = contracts with T1 vendors (IMSS 42,128) | code counts distinct vendors (IMSS 107); **dry run only**: 146 grades would change (C→C+ 61, C+→B 58, D→C 19, B→B+ 5, D→D- 1, C→D 1, C+→C 1); IMSS 49.0 C → 54.8 C+, CENSIDA 22.4 → 22.7 D-; `institution_scorecards` checksum unchanged — **no rescore until the user decides** |
| probes | — | sub-10px 0, contrast fails = the 3 large-text leaves only, footers 1, headings clean, census 0 at ≥ 1024 (390 `reporte` 3 = shared `BenchmarkRow`) |

Shipped 2026-09-24 00:24Z: origin/main + VPS HEAD `1369a911` (branch `parallax/day09b-institutions-panel` on `725df53b`), BUILD_ID `2026-09-24-parallax-d9b-institutions-panel`, entry `index-CCJELu2u.js` → `index-CzVVimf4.js` (BUILD_ID string verified in the served entry), health OK (3,058,286 contracts); the refreshed `category_top_institutions` precompute upserted into `/app/RUBLI_DEPLOY.db` in-container (`wal_checkpoint(TRUNCATE)` → 0,0,0) before the deploy; prod after: `scorecards/institutions?grade=B&scope=federal` 200, `/scorecards/institutions/1425` percentile 0.0, `/institutions/251` 662,001 contracts, `/categories/20/top-institutions` IMSS 173,351; deployed via `deploy-safe.sh`. **F18 rescore not applied — user decision.**
