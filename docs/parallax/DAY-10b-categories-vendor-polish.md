# PARALLAX Day 10b — Categorías + vendor-dossier polish (user review before Day 11)

Routes: `/categories` (El Alzado + catalogue), `/categories/:id` (the stock-card dossier; reference id **20** Medications & Pharma), `/vendors/:id` (reference id **29277** Grupo Fármacos Especializados — the category's top vendor, T1, GT). Files: `components/categories/CategoryAlzado.tsx` (820), `pages/CategoriesIndex.tsx` (896), `pages/CategoryDossier.tsx` (638), `components/category/CategoryDossierSections.tsx` (706), `components/widgets/AriaMemoPanel.tsx` (483, rendered by `components/vendor/VendorEvidenceTab.tsx:99`), `components/vendor/VendorActivityTab.tsx` (the track-record table), `components/vendor/VendorNetworkTab.tsx:157`. Branch `parallax/day10b-polish` off `origin/main` (`b4768840`) in worktree `.claude/worktrees/parallax-day01`.

**User's ask (2026-09-24, verbatim intent):** no three dots anywhere; the text cuts and the fixed columns leave wide gaps; El Alzado's "height is risk" should carry more visual impact without changing the chart; the vendor dossier reads well (risk decomposition, deviation, price volatility, where the money goes, track record) except the investigation-memo block, which needs a polish in the dossier's own voice; "be a bit more creative but don't change a lot — it's pretty much done". Everything below is enhance-in-place. Evidence: `_parallax_shots/day11/pre/` (probe `cat11.mjs` → `probe.log`, `*.json`, `*-full.png`, `crops/*`; memo capture `memo11.mjs` → `crops/vendor-en-1440-memo.png`, `crops/aria-panel-en-1440-full.png`).

## Audit evidence (2026-09-24, local stack, EN + ES, 1440 + 390)

| Where | What the probe/crops show | Code |
|---|---|---|
| El Alzado (`crops/cats-en-1440-03-SECTION.png`) | only 3 of 16 head columns carry a name, all three end in `…` (`Building…`, `Medications & Pha…`, `Food & Prov…`); the 13 other head columns are anonymous (hover-only); top-right callouts overprint each other (`80%`/`16 categories` over `Telecom & Networks`, `+53†` over the needles), `HIGH 40` / `MEDIUM 25 · inventory mean` rule labels collide with the needle stack; `½ 6 categories` prints as `½6 categories`; the plate's 98px top margin is empty except those callouts; the 16 head columns all sit between 19 and 32 → the "elevation" reads as a flat wall and the tall marks are unnamed tail needles | `CategoryAlzado.tsx:583-609` (rotated in-column name, `maxChars = (colH − 12) / 6.6`, `label.slice(…) + '…'`, only `hi < 3`), `:676-690` (svg callouts with `estLabelPx` char estimate), `:62-64` (`MONO_CHAR_PX = 7.2` estimate), `HRule` labels right-anchored at `innerW − 2` |
| `/categories` catalogue | `ellipsis` **13** EN / 13 ES at 1440 (`Grupo Farmacos…`, `Mantenimiento…`, `Repsol Explorac…`, `Seguros Banorte SA de…` …) — every catalogue row's top-vendor chip and the coda chips | `CategoriesIndex.tsx` `EntityIdentityChip … size="sm"` without `fullName` (`:264` category chip is fine — category names are short; the vendor chips at `:842-868` and the per-row top-vendor chip) |
| `/categories/20` dossier | `ellipsis` **11** EN / 11 ES (supplier register `Grupo Farmacos Especial…`, `Distribuidora Internaci…`; largest contracts `Multiequipos y Medicame…`, `Operadora de Centro de…`; buyers/pairs); `clipped` 3 = ARIA fingerprint `P6 Institutional cap…`, `P5 Threshold splitti…`, `P4 Coordinated netwo…` in a fixed 168px column; the largest-contracts list is 686px wide in a 1,088 frame (the "wide gap"); December-tell caption clipped at the plate's left edge (`line = even share (8.3%) · height = % of annual value` starts mid-word, `crops/cat20-en-1440-09-signals.png`); hero right block wraps `69% direct-` / `award` mid-token; subcategory names + `e.g.` examples + contract titles are `truncate` | `CategoryDossierSections.tsx:337-341` (168px label + `truncate`), `:387`, `:402`, `:644` (`truncate`), `:474,:634,:692` (chips `size="sm"` no `fullName`), the `<ol>` of largest contracts (Day 5 `li` measure — bound it to the section width), `CategoryDossier.tsx` hero right column |
| `/vendors/29277` | `clipped` **18** at 1440 / 20 at 390: every track-record title in a `max-w-[300px] truncate` cell (`Umae dvfn expediente de contratación abr …`, `Medicamentos, material de curación, radiol…`) next to ~450px of empty row (`crops/vendor-en-1440-18-contracts-title.png`); the memo panel (`crops/vendor-en-1440-memo.png`): hard-coded Spanish chrome on the EN page (`Análisis de Investigación · Generado por IA`, `Copiar`, `Generado automáticamente — requiere verificación periodística`, date in `es-MX`), widget styling (Inter 13px header, Playfair 15.5/13.5px body, mono `<p>` sub-heads `RESUMEN EJECUTIVO · EVIDENCIA CLAVE · PATRÓN DE RIESGO · FUENTES · ACCIONES SUGERIDAS`), body in two 500px columns inside the 1,088 card with the stale-model banner as a yellow box; **the memo prints the vendor's RFC** (`RFC GFE061004F65`) — `.claude/rules/security.md` § 1 forbids exposing RFCs (the API already returns `vendor_rfc: null`); `Open investigation panel` → `/aria/29277` lands on the 404 page (no such route — `App.tsx:326` has only `aria`; prod returns the SPA shell then the 404 view) | `VendorActivityTab.tsx:467-476`, `AriaMemoPanel.tsx:365-483`, `VendorNetworkTab.tsx:157` |
| headings | `/categories` has **h1 only** (the § kickers are `<div>`s — the Day 10 defect); `/categories/20` h1 → h2 ×11 ✓; `/vendors/:id` h1 → h2 ×3 ✓ | `CategoriesIndex.tsx` section titles |

Toolkit: `ui-ux-pro-max --domain ux` "label truncation ellipsis full text disclosure" → *Compact Label Overflow: a label stays whole; bound only unpredictable values; expose the full text to keyboard/pointer/touch* (the chips); `--domain chart` queries for bar-mekko labels returned no verified match → the repo's own mechanic applies (`hooks/useMeasuredWidth`, `lib/plateLabels.ts` `fitLabel` / `measureLabel` / `placeLabels`, the Day 7b drawn `PlateIndex` for columns too narrow to label).

## Keep
- El Alzado's geometry, colours and order: width = cumulative spend, height = risk on a zero baseline, sector fills, the ½ / 80 % ochre walls, the tail band, the needles at their true position, the MEDIUM / HIGH dashed rules and the inventory-mean rule, the hover dossier, keyboard cycling. The plate height (340 / 240) stays.
- The dossier's stock-card composition and every section; the vendor dossier's hero, diagnosis grid, "why flagged", deviation ledger, price volatility, where the money goes, risk over time, track record, network, ARIA card, coda. No section moves.
- Every fact in the memo (all five sub-sections, the sources, the suggested actions, the provenance badges T1 / LLM / PLANTILLA / STUB, the stale-model warning, the Copy action).

## Change (5) — hard rule for all five: **zero `…` on the three routes at 1440, 1024 and 390, EN and ES** (probe `ellipsis` and `clipped` both 0; the only tolerated `…` is the global search placeholder).

### 1. El Alzado — every column named, callouts seated, the height channel visible — `CategoryAlzado.tsx`
- **Names.** Move the in-column names out of the svg into an HTML label layer over the plate (`useMeasuredWidth` for the plate width; the svg keeps every mark). Every head column gets its **full name**: rotated (`writing-mode: vertical-rl` / `rotate(180deg)` reading bottom-to-top like today) inside the column when the column is ≥ 22px wide and the name fits at 11px mono in ≤ 2 lines (`fitLabel(name, colH − 12, 2)` — measured, never a char estimate); when it does not fit, the column gets a drawn index badge at its foot (`①`-style circle + number, the Day 7b `PlateIndex` pattern, 14px disc, 10px numeral is NOT allowed — 11px) and the name goes to an **index legend row** under the x-axis: `① Medical Supplies · 3.1 % · 27`, mono 12px, wrapping as needed. Needles keep their top-right callouts but every callout is seated with `placeLabels` (measured boxes, leader lines to the mark, no overprint, none outside the plate); the `80 % · 16 categories` and `½ · 6 categories` wall labels get their own reserved row above the callouts (print `½ · 6 categories` with a space); the `HIGH 40` / `MEDIUM 25 · inventory mean` rule labels move to the **left** end of their rules when the right end is occupied by needles (collision-checked). Mobile (< 768): badges + legend for all head columns, top-2 needle callouts only.
- **Height channel.** Add a 3px **risk-tier cap** on the top edge of every head column and needle, in `RISK_COLORS[getRiskLevelFromScore(avg_risk)]` (low → `var(--color-text-muted)` at .5, never green); the sector fill is unchanged. One legend chip row under the plate: `cap = risk tier · low · medium · high · critical`. The kicker keeps `width is spend, height is risk`. **Fable judges this cap on the crop before it ships** — implement it behind a single boolean `SHOW_TIER_CAP = true` so it is one line to remove.
- Accept (probe EN + ES, 1440/1024/390): `ellipsis` 13 → **0** on `/categories`; the dump contains every head-column name in full (list the 16 names and whether each is in-column or in the legend at 1440 and 390); a new check `alzadoLabels` = { inColumn, badges, legendRows, calloutOverlaps: 0, calloutOutside: 0, ruleLabelOverlaps: 0 }; crops of the plate at 1440 + 390 EN + ES compared with `pre/crops/cats-en-1440-03-SECTION.png` — same columns, walls, needles, rules; the cap visible on every column.

### 2. Chips and columns on both category pages — `CategoriesIndex.tsx`, `CategoryDossierSections.tsx`, `CategoryDossier.tsx`
- Every vendor / institution `EntityIdentityChip size="sm"` on both pages gets `fullName` (catalogue top-vendor chips, the coda chips `:842-868`, dossier `:474, :476, :634, :639, :692`); rows wrap rather than cut (`min-w-0` + `whitespace-normal` on the chip's container; the catalogue row's magnitude spine keeps its width).
- ARIA fingerprint (`:337-341`): the label column `width: 168` + `truncate` → `minmax(200px, max-content)` with the name wrapping to two lines at 12px; the bar takes the rest. Subcategory name (`:387`) and `e.g.` example (`:402`) and contract titles (`:644`) lose `truncate` (wrap, ≤ 2 lines — sentence-case the ALL-CAPS COMPRANET titles via the existing title caser if one exists, otherwise leave case). Largest-contracts `<ol>`: inline `style={{ maxWidth: 'none' }}` on its `<li>`s (the Day 5 escape) and the bar track spans the section width — the list fills the 1,010 measure (report `li ≥ ul − 40`). December-tell caption: render it inside the plate's padding (left-anchored at the plot's left edge, not at x < 0). Hero right column: `whitespace-nowrap` on `69% direct-award` / `avg risk 32` tokens (wrap between tokens only).
- `/categories` section kickers → `<h2>` (same classes) so the page has h1 → h2 ×N (Day 10 pattern); report `headings` before → after.
- Accept: `ellipsis` `/categories/20` 11 → **0**, `clipped` 3 → **0** (EN + ES, 1440/1024/390); `liMeasure` on the largest-contracts list `li ≥ ul − 40`; `nowrapOverflow` 0; crops of the register, the largest contracts, the tells and the hero at 1440 + 390 LOOK.

### 3. The memo panel in the dossier's voice — `AriaMemoPanel.tsx` (+ `i18n/locales/{en,es}/aria.json`, `VendorEvidenceTab.tsx`)
- Chrome → dossier: the card becomes a § block with the shared `SubSectionTitle` / kicker style used by the neighbouring sections (`§ EL MEMO ARIA` / `§ THE ARIA MEMO`, `formatEntityName` for the vendor in the memo title), every string bilingual through the `aria` namespace (`Generado por IA` → `AI-generated` / `Generado por IA`, `Copiar` → `Copy`, the footer sentence, the false-positive and stale-model notes), the date in the page locale via `Intl.DateTimeFormat`. Provenance badges (T1 / LLM / PLANTILLA / STUB) stay, restyled as the dossier's mono seals; the stale-model warning becomes a folio marginal note (mono, hairline, `var(--color-accent-hover)`), not a yellow box.
- Body → one reading column at the 640/68ch measure, EB Garamond 15px / 1.55; the five memo sub-sections (`RESUMEN EJECUTIVO`, `EVIDENCIA CLAVE`, `PATRÓN DE RIESGO`, `FUENTES`, `ACCIONES SUGERIDAS`) become mono § kickers (12px, tracking .14em, `text-secondary`) with the memo's own language (the memo text is Spanish — keep it; the chrome is bilingual). Evidence bullets keep their `›` marks; the meta box (`TIPO`, `CONFIANZA`) becomes a two-column `dl` in mono. **Redact the RFC**: never render the `RFC …` line or any 12–13-char RFC token from `memo_text` (mask to `GFE···` or drop the row — `.claude/rules/security.md`); report how many memos in the fixture carry one.
- At ≥ 1024 the sources + actions may sit in a right rail (the Day 3b "one text axis" rule: the body stays on the 640 column, the rail at 640 + 40). Below `lg` one column.
- Accept: the memo crop at 1440 + 390 EN + ES LOOKS like the sections above and below it (same kicker style, same serif, same measure); no English page shows Spanish chrome (dump check for `Generado por IA`, `Copiar`, `Análisis de Investigación` on EN → 0); `proseP` for the memo body ≥ 15px at ≤ 700px wide; no RFC pattern (`/\b[A-ZÑ&]{3,4}\d{6}[A-Z0-9]{3}\b/`) in the dump; bilingual audit clean.

### 4. Track record table — `VendorActivityTab.tsx:460-480`
- The Object cell: `max-w-[300px] truncate` → `minmax(320px, 1fr)` with the title wrapping to ≤ 2 lines (`break-words`), the contract-number sub-line stays mono 12px on its own line; column widths re-balanced so the row has no empty band (Object takes the slack; Institution wraps at `max-w-[260px]`; amount / USD / date / risk stay fixed). `title=` tooltips dropped (the text is visible). Below `md` the existing responsive path stays (report what it is).
- Accept: `clipped` on `/vendors/29277` 18 / 20 → **0**; the probe's `boxes` (slack > 90) drop the row-level entries; the table crop at 1440 shows titles in full on ≤ 2 lines.

### 5. The dead investigation link — `VendorNetworkTab.tsx:157`
- `/aria/${vendor.id}` has no route. Read `pages/AriaQueue.tsx`: if it has a search/filter state that can take a vendor, add a nuqs key (`?q=` or `?vendor=`) and link there; if not, link to `/aria?pattern=${primaryPattern}` (the card already prints `Pattern: P1 (95%)`) and change the label to `Open the ARIA queue` / `Abrir la cola ARIA`. Report the choice.
- Accept: the link resolves to a 200 view (not the 404 page) locally; `pre/crops/aria-panel-en-1440-full.png` shows the before.

## Out of scope
Vendor dossier Day 13 items beyond the memo and the table (hero, chips inks); `ObservatoryScatter`/`PlateFrame` shared items; the categories taxonomy (72 → S.10–S.12).

## Risk
- Change 1 moves glyphs out of the svg on a loved plate: never write state in render; label layer only when `W > 0`; reserve the plate height; compare crops. The tier cap is the only new mark — keep it behind `SHOW_TIER_CAP`.
- Change 3 touches a widget that other pages may import: `git grep AriaMemoPanel` first (the audit found only `VendorEvidenceTab.tsx:22`); if another importer exists, keep the props API and restyle inside.
- RFC redaction is a security rule, not a style choice — do it even if it costs a line of the memo.

## Build notes for the executor
Same stack and rules as `DAY-10-panel.md § Build notes`: worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, branch `parallax/day10b-polish` (already created from `origin/main` `b4768840`; this file is its first commit), local backend `127.0.0.1:8001` (no-scan, no backend change today, do not restart), Vite `localhost:3009` on the worktree, `MSYS_NO_PATHCONV=1`, ≤ 2 browsers, never rubli.xyz. Order: **2 → 4 → 5 → 3 → 1**. One commit per change: `feat(categories|vendor § PARALLAX D10b § Change N): …`, body cites `docs/parallax/DAY-10b-categories-vendor-polish.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Progress file `_parallax_shots/day11/progress-10b.md` (one line after EVERY step); report `_parallax_shots/day11/report-10b.md` (per change: done, every acceptance number before → after, deviations, noticed-not-fixed). Probes: extend `_parallax_shots/day11/cat11.mjs` (tag `after`, add `alzadoLabels`, `liMeasure`, `nowrapOverflow`, the RFC regex, the Spanish-chrome check) and run EN + ES at 1440/1024/390; crops via the same script's block crops + `memo11.mjs`; put before/after pairs of the Alzado (1440 + 390), the supplier register, the largest contracts, the memo, and the track-record table in the report and **LOOK at every crop**. Review: `rubli-bilingual-audit` on every touched TSX; gates from `frontend/`: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` · `npm run build` · `npm run lint:tokens` · `npx eslint` on touched files. Do NOT bump BUILD_ID, push, deploy or merge — Fable judges first.


## Result

Built by Opus executor `parallax-day10b`: five changes `9894834b` `23500923` `ca711b8d` `7cd760a2` `69d93248`, plus judge round `65f61331`. Fable judged the change on the after-crops (`_parallax_shots/day11/after/crops/*-N-*.png`).

| Measure (EN + ES, 1440/1024/390) | before | after |
|---|---|---|
| `…` on `/categories` | 13 | **0** |
| `…` on `/categories/20` | 11 | **0** |
| clipped on `/categories/20` | 3 | **0** |
| clipped on `/vendors/29277` (1440 / 390) | 18 / 20 | **0** |
| El Alzado head columns named | 3, all cut | **19 of 19** in full: in-column where they fit, otherwise an index badge plus a legend. Callout, wall-label, rule-label and badge overlaps are all 0 |
| risk-tier cap | none | 3px cap in the risk palette on every column and needle (`SHOW_TIER_CAP`, approved on the crop) |
| headings on `/categories` | h1 only | h1 + 6 h2 |
| Spanish chrome on the EN vendor page | 4 | 0 |
| RFC on the vendor page | 1 | **0** (`lib/redact.ts` applies to screen and Copy, with a vitest; 141 of 1,902 local memos carried one) |
| sub-11px on `/categories/20` / `/categories` | 19 / 2 | 0 / 0 |
| largest-contracts list width | 686 of 1,088 | fills the section at every width |
| track record | titles cut at 300px | titles wrap to ≤ 2 lines, dates on one line, institution ≤ 3 lines, fits 862 of 864 at 1024 (USD hidden between lg and xl) |
| ARIA link | `/aria/29277` → 404 | `/aria?q=<vendor>` → the queue row |

Judge round 1: at 390 the badges collided, so they moved to a packed two-row band under the axis. At 1024 the track-record dates wrapped inside the token, so dates and amounts became nowrap and the institution column got a cap.

Deviations accepted: Building Construction gets a level two-line name; callouts are seated right-aligned first; the memo's right rail appears only when the panel is at least 64rem wide; the mobile top margin is 76px; the shared `DossierSectionHeader` now wraps instead of truncating; `cleanContractDescription` takes an optional `maxChars`.

Noticed, not fixed: the shared StatStrip sub-line sits 3px into its padding at 1024; on `/aria` the expanded row's readout runs into the memo band and the factor labels are cut; the ARIA link uses an ExternalLink icon for an internal link; two older ESLint errors remain (`AriaQueue.tsx` set-state-in-effect, `contract-audit.ts` control-character regex).

Shipped 2026-09-24 10:23Z: origin/main + VPS HEAD `6c702ee9` (rebased onto `b4768840`), BUILD_ID `2026-09-24-parallax-d10b-polish`, entry `index-DkkfMVz8.js` → `index-CBdWJ8-C.js` (BUILD_ID string verified in the served entry), health OK, `/categories`, `/categories/20`, `/vendors/29277` 200; deployed via `deploy-safe.sh`.
