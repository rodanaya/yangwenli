# PARALLAX Day 10 — Panel (Dashboard) (`/dashboard`)

Route: `/dashboard` (Folio·I «RUBLI executive briefing» — masthead (eyebrow + Print, dateline, EB Garamond h1 with two accent spans, mono byline, lede) → § 1 El Atlas (lens toggle + the shared `ObservatoryScatter` + «Open full Atlas») → «Five administrations · one structural failure» (`MacroArc`, Folio·III) → «Lead-time advantage» (`LeadTimeChart`, Folio·IV) → «Headline numbers» (four chapter tiles I–IV, Folio·V) → «What the analysis found» (four FINDING cards: ghost economy triptych, audit blindspot triptych, threshold-gaming histogram, institutional-capture dumbbell + hover strip) → «Pesos at risk» (`PesosAtRiskChart` Cleveland pairs, Folio·VI) → «Where the money goes» (`TopCategoriesChart` ranked list, Folio·VII) → § 2 La Lente (Cascade Ledger, four log rungs + GT anchor band + methodology line, Folio·VIII) → amber rule → «Documented corruption cases» (`CaseTimeline` seismograph + numbered legend, Folio·IX) → «Recent critical alerts» (five wire rows) → «Start Here» CTA → § Adónde ir coda (ARIA link + 3 `EntityIdentityChip`s) → credibility strip). Files: `pages/Executive.tsx` (2,065), `components/dashboard/MacroArc.tsx` (407), `components/executive/{CaseTimeline 219, LeadTimeChart 191, TopCategoriesChart 323, PesosAtRiskChart 274}`, `hooks/useExecutiveData.ts` (59, one bundled request). Route → component verified in `App.tsx:72,217` (`/executive`, `/executive-summary` redirect here). Not this page's: `components/atlas/ObservatoryScatter.tsx` + `lib/atlas/useScatterClusters` (shared with `/atlas`, Day 16), `components/atlas/PlateFrame.tsx` (shared, Day 1/7 backlog), `components/gap/GapRecoveryPanel.tsx` (the Day 8 note guessed it lived here — it is imported only by `Administrations.tsx:34`). Branch `parallax/day10-panel` off `origin/main` (`f6f13757`) in worktree `.claude/worktrees/parallax-day01`.

**Classification: ⭐ GOLD — enhance in place, no layout change.** The dashboard capstone (`82227f2b`, Jun 15) stands: the masthead and the two-accent headline, the Atlas as the first plate, the MacroArc line with its four callouts, the lead-time dumbbells, the four chapter tiles with their Roman rail and micro-vizzes, the four FINDING cards with their triptychs, the Cleveland pairs, the ranked category list, the Cascade Ledger with the GT anchor, the seismograph, the wire, the CTA, the coda. Section order, plate geometry, colours, the folio chrome — untouched. No `/designus`. What is wrong is the Days 4–9 list (sub-10px glyphs inside fixed-height svgs, mark colours as type, `role=link` divs, no `<h2>`, 1,100 frame, two colophons, 12px intro prose) plus **five honesty defects the code read found**: the byline still says `Updated May 2026`; the seismograph draws the old calendar (AMLO 2018–24, Sheinbaum 2024–25, green band) while the MacroArc plate two folios above draws the canonical one (2019 / 2025) — two calendars on one page, the Day 8 defect; the MacroArc line draws 2001 (the 19-contract stub) and 0 % for 2002–2009 (Structure A does not code the procedure type: `0.01 / 0.0 / 0.05 %`) as if Mexico had no direct awards for eight years; the lead-time plate prints `MEDIAN RUBLI LEAD-TIME: 2.7 YEARS` over eight rows whose median is 3.5 (leads 7 6 4 4 3 3 1 1); the pesos section carries the caveat of a method it no longer uses (`assumes uniform value distribution across risk bands` was the pre-M3v2 count-share approximation — the plate below uses pattern-specific models and the tile uses `high_risk_value_mxn`). Also the P6 count is typed (`15,923` ×2) while the bundle carries `pattern_counts.P6 = 15,939`, and the threshold-gaming bars are drawn from a constant array but captioned as `contract count by amount`.

## Audit evidence (2026-09-24, local backend `127.0.0.1:8001` no-scan, Vite 3009 on the worktree at `f6f13757`; prod not touched)

Probe `_parallax_shots/day10/audit10.mjs` (from `day09b/audit9b.mjs` minus the institution parts; D10 checks: `h1Style`, `staleDateline`, `roleLinks`, `fixedHeightSvgs` (rendered scale, side gutter, vertical letterbox), `eraLabels`, `staticTabs`, `sectionKickers`, `hoverOnly`, `capitalizeSector`, `usdEnCount`), runs `before-{en,es}.txt` + `before/{en,es}-report.json`; full pages + JSON `before/shots10.mjs` → `before/dash-{en,es}-{1440,390}-full.png`; per-block crops `before/crop10.mjs` → `before/crops/en-{1440,390}-NN-<block>.png` (14 blocks each).

| Measure | 1440 EN (ES identical unless noted) | 390 |
|---|---|---|
| sub-10px text leaves | **102**: `LeadTimeChart` 50 × **9.6px** rendered (`fontSize 7.5` × 1.28: years, `CASE`, `LEAD TIME`, grid years), `yrs` tspans 8 ×**10.3**, median banner 8; `PesosAtRiskChart` tick units `B MXN` ×7 **7.7px**, ticks 9.6, `P5` pills 10.9, `3,985 vendors` 9.6, `→ Investigate` 10.9; `CaseTimeline` era labels 7.5 → 9.6, ticks 9.6, badge numerals 7 → 9; tile legends `text-[8px]` ×6 (`● crit 5%`, `0.5 · random`…); FINDING 01/02 `text-[7px]` ×4 (`SAT official`, `gap`, `ASF audits`), `text-[8px]` ×2 (`RUBLI detected`, `95% never audited`); FINDING 03 svg `fontSize 8/7` ×3 + `text-[8px]` caption; FINDING 04 row labels `text-[8px]` ×5, `+56.3pp` svg 8 ×5, legend `text-[8px]`; Cascade `Anchor` 8.5; `ObservatoryScatter` `HIGH SCALE × HIGH RATE` 7.6 (shared) | **197**: the six fixed-`viewBox` svgs at 0.28–0.35 scale → every svg glyph at **2.1–4.5px** (MacroArc ticks 3.7, era labels 4.5, lead-time years 2.6, pesos units 2.1, timeline 2.6…) |
| fixed-height svgs (`fixedHeightSvgs`) | `MacroArc` 820×260 in a 1,052 cell → drawn at 820, **116px gutter each side**; `LeadTimeChart` 820×272 (`style height 272`) gutter 116; `PesosAtRiskChart` 820×358 gutter 116; `CaseTimeline` 820×190 gutter 116; FINDING 03 320×110 in 476 → gutter 78 | drawn at 286 wide, **height fixed** → dead letterbox: lead-time **89px**, pesos **117px**, timeline **62px** above and below the drawing (`before/crops/en-390-06-section-pesos.png`) |
| contrast < 4.5:1 — every failing leaf (HTML) | **114** (`minC` 1.43): badge numerals `#ffffff` 8px on sector fills 1.43–3.82 ×10 (`CaseTimeline.tsx:201`); `#eab308`/`#22c55e`/`#16a34a`/`#8b5cf6`/`#ea580c` sector hex as 12–13px type 1.54–3.75 ×25 (`CaseTimeline` legend years + `●`, `TopCategoriesChart.tsx:221` sector names, `LeadTimeChart` legend); `#f59e0b` as type: tile III numeral **40px 2.15**, its sub-line `5.6T MXN at stake · 56.3%` 13px 2.15, `The flag` kicker 1.85, `III` 22px 1.9, `··· 47× drop` 1.69, `● high 6%`, `95% never audited` 1.72, `MX$1.25T` 36px 2.15, `19×` 2.04 (`Executive.tsx:579,769,774,792,1023,1049,1722`); `#ef4444` as type 3.57–3.76 ×14 (`6,118` 42px, `145×`, `RUBLI detected`, `CRITICAL` pill ×5, chip names ×5, `··· 24× drop`, `● crit 5%`, the 64px `7 in 10`); `#a06820` as 8.5–22px type 2.76–3.81 (`Anchor` .75, `Training corpus` .75, `43` .85, `The spend`, `All categories`, `View all`); `#736a65` at opacity 2.7–3.52 (`≈US$…` ×8 .8, `ranked by premium…` .7, `logarithmic scale…` .75, `← informs…` .85, `·` ×7 + `Indexed` ×7 = PlateFrame, shared); `#6b6560` .85 ×3 (rung operations) | 100 |
| svg text < 4.5 (`svgTextFail`) | **86**: MacroArc era labels `#1a5276` 3.66 / `#c41e3a` 3.35 / `#7b2d8b` 3.71 at opacity .7, `EU 10%` `#22d3ee` **1.6**, `Mexico · 68.3%` 4.28, callout years; lead-time years in sector hex 1.7–4.28 ×16, `B MXN` 2.29 ×7, pills; `ObservatoryScatter` axis ticks `#7a7064` 4.12 ×21 (shared) | 86 |
| `riskAsType` / `sectorAsType` / opacity-dimmed | **28 / 25 / 30** (21 of the 30 are PlateFrame's `·`/`Indexed`, shared) | same |
| frame / footers / headings | `max-w-[1100px]` (`:211`) — `frame` null (the probe looks for 1010/5xl/6xl/7xl/`data-frame`), content 1,052; **2 footers** (page `<footer class="page-footer">` `:2040` + the shell colophon, hidden by `main:has(.page-footer) + footer` but in the DOM); **h1 → h3 ×5** (`hSkip` 1): the eleven section titles are `<div>`s (`sectionKickers` 11), `§` kickers 3 | 366 |
| `role=link` non-native (`roleLinks`) | **21**: 4 chapter tiles `div` (`:643-651`), 4 FINDING `article`s (`:866,972,1075,1255`), 8 `li` category rows (`TopCategoriesChart.tsx:179-190`), 5 wire rows `div` (`:1883-1889`) each with the vendor chip **inside** (`stopPropagation`, `:1898`) | 21 |
| raw `<a href>` (full reload) | Cascade rungs ×4 + GT anchor (`:1658`, `:1734`), pesos `→ Investigate` ×7 (`PesosAtRiskChart.tsx:240`, 67×12px targets), `methodology` (`:1801`); `<button onClick={navigate}>` ×4 (`All categories` `:1485`, `View all` `:1857`, `Open ARIA queue` `:1955`, `Search a vendor` `:1962`) | |
| no focus style / targets < 24px | **26 / 4** (`Print this page` 16px, `Open full Atlas` 18, `All categories` 18, `View all` 20; the 7 `g[role=button]` + `▤7 Patterns` are ObservatoryScatter's); `outlineNone` `Open ARIA queue` (`focus:outline-none focus:ring` — `focus:`, not `focus-visible:`); `hoverOnly` 4 (`investigate` labels `opacity-0 group-hover:opacity-100`, no `group-focus-visible`) | 26 / 2 |
| Atlas lens toggle | `role=tablist` + 4 `role=tab` with **no `tabpanel`, no roving tabindex, no arrow keys** (`:400-433`); lens not in the URL (`/atlas` reads `?lens=`) | |
| truncation | 0 | **13**: Cascade rung labels `truncate` ×4 (`CO…`, `HI…`, `AR…`, `TI…` — `before/crops/en-390-08-section-la-lente.png`), operations ×3, category captions ×3, wire titles ×2, chip ×1; `chipsClipped` 4; the GT anchor band prints one word per line (`43 / named / cases / · / 1,554 / GT / vendors`) inside the 136px count column | |
| calendar | `CaseTimeline.tsx:35-40` `ERA_BANDS` Calderón 2008–2012 · Peña Nieto 2012–2018 · **AMLO 2018–2024 · Sheinbaum 2024–2025**, Sheinbaum `#10b981` (green); `MacroArc.tsx:30-36` FOX 2002–06 · CALDERÓN 2007–12 · PEÑA NIETO 2013–18 · AMLO 2019–24 · SHEINBAUM 2025 (canonical years, own hex) — `lib/administrations.ts` is neither's source | |
| MacroArc series | `/analysis/year-over-year` → `2000 0.0 · 2001 68.42 (19-contract stub) · 2002 0.01 · 2003 0.0 · 2004 71.43 · 2005–2009 0.0–0.05 · 2010 62.7 …`; the `> 0` filter (`MacroArc.tsx:58`) keeps 0.01 and 0.05, so the plate draws a line that dives to 0 % for 2002–2009 and starts at **2001**; caption `Source: COMPRANET 2001–2025` (`before/crops/en-1440-02-section-macro-arc.png`) | |
| stale / typed numbers | byline `Updated May 2026` (`:323-324`, `staleDateline` EN+ES); lead-time `MEDIAN_YEARS = 2.7` (`LeadTimeChart.tsx:61`) vs 3.5 from its own rows; `15,923` (`:1287`, `PesosAtRiskChart.tsx:41`) vs bundle `aria_stats.pattern_counts` `{P6: 15939, P2: 6118, P5: 3772, P3: 2972, P7: 285, P4: 220, P1: 44}` (typed vendors `3985 / 2974 / 257`); pesos caveat `(estimated · assumes uniform value distribution across risk bands)` (`:1454-1458`); FINDING 03 bars `PRE/POST` constants (`:1101-1103`) captioned `Bar height = contract count by amount` (`:1240`); lede `Every administration since 2001` (`:365`) | |
| wire rows | sector names raw from the API on EN: `Salud`, `Tecnologia`, `Medio Ambiente`, `Hacienda` (`capitalize`, `:1913`); title `truncate` (`:1904`) | |
| prose | lede 17px EB Garamond in 555px ✓ (D2c 68ch); **eleven section intros 12px Inter** (`text-xs`, `proseP` 12 ×13), FINDING bodies 12px, timeline/wire/CTA intros 14px; captions 13.5px serif at **38 %** of the figure (`captions` 415/1100 ×7 — the Day 4 `captionFull` rule) | |
| components inside render | `ChapterRail` and `ColumnFrame` are defined inside an IIFE in JSX (`:595-662`) → new component identity on every render → the four tiles **remount and replay their entrance motion** whenever `capDetail` (capture-row hover, `:1319`) or `atlasMode` changes; the hover state lives on the 2,065-line page, so a hover re-renders the whole page including `ObservatoryScatter` | |
| endpoints (local) | bundle 1 request ✓ (`useExecutiveData`), `year-over-year`, `categories/summary`, `atlas/cluster-stats` (per lens); `load` 6.7 s, console errors **0** | 7.8 s |
| h1 | `text-align: start`, `text-wrap: balance`, 64px — the "justified hero" of the schedule row was fixed by D2c (`e181c3eb`); nothing to do | 36px ✓ |
| italics / `US$` in ES / MDP | 0 / **0** ✓ (`usdEs` 9 on EN = tile I + category rows, by design) / `B MXN` ×7 in the pesos axis units on both languages (`PesosAtRiskChart.tsx:111` prints `MDP` in ES ✓; EN `B MXN`) | ✓ |

Toolkit passes run IN the audit:
- **web-design-guidelines** (rule set fetched 2026-09-24):
  - `Executive.tsx:643-651,866-878,972-984,1075-1087,1255-1267,1883-1890` — `<div>`/`<article>` with click handlers + `role=link` (should be `<Link>`); `:1898` — `stopPropagation` to host a link inside a `role=link` (nested-interactive workaround); `:1658,:1734,:1801` — raw `<a href>` (full reload, no history state); `:1485,:1857,:1955,:1962` — `<button>` for navigation; `:254` — Print button 16px, no focus ring; `:400-433` — `role=tablist` without panels/arrow keys; `:883,:989,:1092,:1272` — hover-only reveal; `:1957` — `focus:` ring on click; `:323` — hardcoded "Updated May 2026"; `:1923` — `toISOString().slice` date (Intl); `:1913` — `capitalize` on a code; `:1690,:1707,:1898,:1904` — `truncate`; `:595-662` — components declared in render; `:791,:840,:907,:920,:951,:1013,:1026,:1054,:1238,:1323,:1394` — 7–8px text; `:1454` — caveat text describing a method the section does not use.
  - `MacroArc.tsx:58` — filter keeps 0.01 %; `:30-36` — own calendar; `:293,:321` — label width by char estimate; `:207-221` — cyan `#22d3ee` text 1.6:1; `:143-155` — era labels at opacity .7; `:118-128` — `onMouseMove` hover with no keyboard/touch alternative (tooltip only; the plate has `role=img` + label ✓).
  - `LeadTimeChart.tsx:61` — typed median; `:68-69` — `aria-label` English-only; `:73-81,:84-108,:149-161,:169` — 7.5–8px svg text; `:181` — legend dot `#64748b` for flag years that are drawn in sector colours (legend ≠ marks); `:23` — `href` in data never rendered.
  - `CaseTimeline.tsx:35-40` — old calendar + green; `:74` — `aria-label` English-only; `:88-99,:115-123,:167-176` — 7–7.5px svg text; `:201` — 8px white numerals on colour (1.43:1); `:186` — legend two columns at every width.
  - `PesosAtRiskChart.tsx:38-46` — typed vendor counts; `:116-125` — 7.5/6px ticks + units; `:165-169,:177-181,:248-257` — 8.5/7.5px; `:240-258` — `<a>` inside svg (67×12px target, no ring); `:266` — 8px footnote; `:88` — `paddingLeft: LABEL_W` on an HTML header assumes the 820px drawing width.
  - `TopCategoriesChart.tsx:179-190` — `li role=link tabIndex`; `:208,:227` — `truncate-balance` / `truncate`; `:215,:285` — `title` tooltips as the only disclosure; `:221` — sector hex as 13px type; `:269` — `opacity: 0.8` on muted text.
  - Pass: `tabular-nums` on numerals, `Intl` via `formatNumber`/`formatCompactMXN`, `prefers-reduced-motion` global `MotionConfig`, `aria-label` on the Print icon button, `role=status aria-live` on the loading pill and the capture strip, `aria-labelledby` on 9 sections, `text-balance` on the h1, `text-pretty` on prose, `@media print` chrome, `≈US$` EN-only.
- **ui-ux-pro-max** `--domain ux` "clickable card nested link navigation" → *Keyboard Navigation: tab order aligned with visual order, every action without a pointer* (the wire row's chip + row = two targets for one row; make the title the row's link); "duplicate heading section landmarks" → *Heading Hierarchy: sequential h1–h6, never skip* (h1 → h3 here); "hover only reveal keyboard focus" → *Focus States: visible ring on every control* + *Focus Not Obscured*; "stale timestamp data freshness label" → *Compact Label Overflow: a chip stays whole on one line, disclose truncation* (the Cascade rung labels at 390). `--domain chart` "svg fixed height viewBox scaling label legibility" → no verified match for the letterbox itself (fallback: the Day 4–8 mechanic "HTML owns glyphs, SVG owns geometry", `useMeasuredWidth`); "dot plot log scale axis annotation" → no verified match (fallback: *label directly, never colour alone* — the Cleveland pairs already print the value on the dot ✓).
- **vercel-react-best-practices**: one bundled request + three lens/category queries, all keyed and cached ✓; `useMemo` on `stats`/`codaChips` ✓; **components declared inside render** (`ChapterRail`, `ColumnFrame`) ✗ — remount per render; page-level hover state (`capDetail`) ✗ — extract; `whileInView` everywhere with `once: true` ✓; no layout reads in render ✓; `LensVisualization.tsx` (230 lines) unimported since 2026-05-20 (`Executive.tsx:35` comment) → STEP 0.
- **a11y** (Fable's pass): the rows above; `liveRegions` 2 ✓; every plate svg `role=img` with a sentence ✓ (two English-only); the `ObservatoryScatter` orbs are `g[role=button] tabindex=0` with `outline: none` and no ring ×7 (shared — Day 16); micro-viz svgs `aria-hidden` ✓; no `<caption>`-less tables (no tables) ✓; `document.title` unchanged per route (site-wide backlog).
- **folio voice**: intact — Folio·I…IX, § 1 / § 2 / § Adónde ir kickers, «Plate —» captions, chapter Romans, the wire's `CRITICAL` seals. Copy changes are the honesty fixes only (byline, caveat, schematic label, `since 2001`, the computed median).

## Keep

- The whole composition and order: masthead → Atlas → MacroArc → lead-time → chapter tiles → four findings → pesos → categories → Cascade → seismograph → wire → CTA → coda → strip. No section moves, merges, or leaves; the amber rule between the Cascade and the seismograph stays.
- The h1 with its two accent spans (ochre spend, red `7 in 10`), the 17px EB Garamond lede, the mono eyebrow/byline, the `Print / PDF` button and the print stylesheet, the paper-grain overlay.
- Every plate's geometry, marks and colours: MacroArc crimson line + area + admin wash bands + four FT callouts + the Playfair COVID pull-out + cyan EU dashed line + right-edge `Mexico · %`; the lead-time bars in sector colour with the red public dot; the chapter rail (Roman + rule + kicker) and the four micro-vizzes (23 bars, the 100-dot waffle with its neutral 10 % mark, the four-band strip, the AUC gauge); the two triptychs; the violet histogram with the red dashed threshold; the capture dumbbells + amber hover strip; the Cleveland pairs on the shared log axis with the value on the dot and the pattern pill; the ranked category list (`DotBar`, Playfair amounts, sector chip, risk pip); the Cascade's log bars in `RISK_COLORS` and the dashed GT anchor band; the seismograph spikes, caps and the numbered legend; the wire's `CRITICAL` seal + sector dot + amount/date column; the ochre CTA panel; the coda chips.
- `EntityIdentityChip` for every vendor/sector (rule 1 ✓), `formatCompactUSD` EN-only companions, `formatCompactMXN` in the category list and the wire, MDP in ES (`usdEs` 0), `DotBar` (no inline circles beyond the sanctioned waffle/dumbbell marks), no green for low risk, no italic, no dot-grid.
- `ObservatoryScatter` and `PlateFrame` are not edited today (shared: Days 16 and 1).

## STEP 0 — dead code (its own commit, before Change 1)

`git grep` first (direct imports, lazy `import()`, string literals, tests, `_dev/ChartCatalog.tsx`, `i18n/index.ts`) — the audit's greps found:
- `components/executive/LensVisualization.tsx` (230; retired 2026-05-20, only the comment at `Executive.tsx:35-38` names it) — imported by nothing.
- The `executive` i18n namespace: `Executive.tsx:48` is the only consumer (`useTranslation('executive')`) and it reads `i18n.language` only — no `t()` call anywhere; `i18n/locales/{en,es}/executive.json` + the two imports/registrations in `i18n/index.ts:20-21,79` go; `Executive.tsx` switches to `useTranslation()`. Report the key count removed.
- The stale docblock `Executive.tsx:1-17` (describes a 2×2 KPI grid, Signal cards, recommendations by audience — none exist) → rewrite to the composition in this file's first paragraph; drop the four "removed on <date>" comment blocks (`:33`, `:35-38`, `:44-45`, `:528-531`, `:1813-1818`) — git history carries them.

Gate: tsc + build + lint:tokens. Commit `refactor(dashboard § PARALLAX D10 § STEP 0): remove LensVisualization + the unused executive i18n namespace`. Report the exact line count.

## Change (8)

### 1. Honesty — the byline, one calendar, the DA floor, the computed median, live pattern counts, the caveats — `Executive.tsx`, `MacroArc.tsx`, `CaseTimeline.tsx`, `LeadTimeChart.tsx`, `PesosAtRiskChart.tsx`
- Byline (`:322-324`): `Updated May 2026` / `Actualizado may 2026` → `Data cut 2025·09·28` / `Corte de datos 2025·09·28` (the Day 3 convention; the model tag stays `Model v0.8.5`). Lede (`:365`, `:374`): `Every administration since 2001` → `Every administration the register can score` / `Cada administración que el registro puede calificar` (the MacroArc kicker already says it).
- One calendar: `CaseTimeline.ERA_BANDS` and `MacroArc.ERA_BANDS` derive their years and labels from `ADMINISTRATIONS` / `ADMIN_DISPLAY_ACCENTED` in `lib/administrations.ts` (`calderon 2007–12 · epn 2013–18 · amlo 2019–24 · sheinbaum 2025–`; the seismograph's window starts at 2008 so Calderón's band is clipped to `max(start, 2008)` and Sheinbaum's to the 2025 axis end). **Colours**: MacroArc keeps its own wash hexes (`#1a5276 / #c41e3a / #7b2d8b` — plate marks, Keep list); CaseTimeline's bands switch to `ADMIN_COLORS` (its Sheinbaum band is `#10b981` green today — no green on this site outside the sector palette) at the same opacities (.04 fill / .18 cap). Report the band x-positions before → after (2018 → 2019, 2024 → 2025).
- DA floor (`MacroArc.tsx:56-60`): `const DA_SERIES_FLOOR = 2010` (Structure B is the first structure that codes the procedure type — CLAUDE.md § Data Sources) and `.filter((d) => d.year >= DA_SERIES_FLOOR)`; `Y_MIN_YR` follows (2010); the caption ends `Source: COMPRANET 2010–2025 · 2002–2009 not drawn — Structure A does not code the procedure type.` / `… 2002–2009 no se dibujan — la Estructura A no codifica el tipo de procedimiento.`; the FOX band disappears with its years (four bands remain: CALDERÓN clipped to 2010–12, PEÑA NIETO, AMLO, SHEINBAUM). The kicker `Five administrations · one structural failure` → `Four administrations …` / `Cuatro administraciones …` and the plate caption `across five administrations` → `four` (the register scores four; the lede's "every administration the register can score" now agrees).
- Median (`LeadTimeChart.tsx:60-64`): compute from the rows (`sorted leads → median` = 3.5 today) and print `Median lead-time across these 8 cases: 3.5 years` / `Ventaja mediana en estos 8 casos: 3.5 años`; never a typed constant.
- Live counts: FINDING 04 `15,923` (`:1287`) → `ariaStats?.pattern_counts?.P6 ?? 15_939`; `PesosAtRiskChart` takes a `patternCounts?: Record<string, number>` prop from the page (`ariaStats.pattern_counts`) and prints `formatNumber(patternCounts?.[p.code] ?? p.vendors)`; the typed fallbacks in `PATTERN_RISK` become the CLAUDE.md numbers (P5 3,772 · P3 2,972 · P7 285 · P6 15,939). The `pesosBn`/`baselineMdp` estimates stay typed (they are the illustrative model) and the plate caption keeps `Estimates are illustrative`.
- Caveats: `:1454-1458` `(estimated · assumes uniform value distribution across risk bands)` → `(estimates · pattern-specific overpayment models · illustrative, not measured)` / `(estimaciones · modelos de sobrepago por patrón · ilustrativas, no medidas)`; FINDING 03 caption (`:1238-1242`) → `Schematic — bar heights are illustrative, not measured counts. The pattern: a spike just below the tender threshold.` / `Esquema — las alturas son ilustrativas, no conteos medidos. El patrón: un pico justo bajo el umbral de licitación.`
- Dead destination: `handleAtlasClusterClick` `patterns` → `/clusters#code` (`:174`) redirects to `/atlas` and loses the hash (`App.tsx:365`). Read `pages/Atlas.tsx` for the params it honours (`?lens=`, `?pin=` or the Sep-17 three-scope keys) and route to the one that opens that cohort; report which. Wire-row dates (`:1923`) → `Intl.DateTimeFormat(lang === 'es' ? 'es-MX' : 'en-US', { dateStyle: 'medium' })`.
- Accept (probe EN + ES, 1440): `staleDateline` [] (both languages); `eraLabels` from the seismograph = `CALDERÓN · PEÑA NIETO · AMLO · SHEINBAUM` with band edges at 2013/2019/2025 (add the check: the x of each band's left edge ÷ the axis → year); MacroArc's first x tick ≥ 2010, no point below 50 % on the line (add: min `cy` of the `circle`s); the dump contains `3.5 years` / `3.5 años` and no `2.7`; the dump contains `15,939` twice (finding + pesos row) and neither `15,923` nor `3,985`; the dump contains `illustrative` twice (`pesos` caveat + finding 03) and not `uniform value distribution`; the atlas orb click lands on a URL that `Atlas.tsx` reads (manual: click the P2 orb → report the URL and that the cohort opens).

### 2. Every figure at 1:1 with HTML glyphs and an 11px floor — `MacroArc.tsx`, `LeadTimeChart.tsx`, `PesosAtRiskChart.tsx`, `CaseTimeline.tsx`, `Executive.tsx` (FINDING 03 histogram, FINDING 04 dumbbells, tile legends, triptych labels)
- The Day 4/5/6/7/8 mechanic: each figure measures its wrapper with `useMeasuredWidth` (`hooks/useMeasuredWidth.ts`), sets the svg `width = W` / `viewBox = 0 0 W H` (no `style={{ height }}` letterbox; `H` derived from rows × row height), renders geometry only when `W > 0`, and moves **every text glyph out of the svg** into an absolutely positioned HTML label layer (or plain HTML rows beside the svg) at **≥ 11px** — years, axis ticks, units, `CASE`/`LEAD TIME`, the median line, the `yrs` tspans, pattern pills + names + vendor counts, `B MXN`/`MDP` units, the `→ Investigate` links (Change 3 makes them `<Link>`s), era labels, tick years, the badge numerals, MacroArc's four callouts (leader lines stay in the svg; the box is an HTML label measured with `measureLabel` from `lib/plateLabels.ts` — no more `label.length * 5.4`), the `EU 10%` and `Mexico · 68.3 %` edge labels, the y/x ticks. Marks (lines, dots, bars, bands, connectors) stay svg. The lead-time year labels above the dots become one 11px mono row per case in the HTML layer (flag year left of the flag dot, public year right of the public dot, collision-checked with `placeLabels`).
- Phones: MacroArc at 334px thins its x ticks to the band starts + 2025 (Day 8 seam pattern), the era labels sit above the plot as a mono row; lead-time rows keep 26px height with the case name on its own line above the track when `W < 560`; the pesos plate stacks label above track below `sm` and the `→ Investigate` link moves under the value; the seismograph keeps its 10 spikes (7px bars at 334 are fine) and its legend goes single-column below `sm`; FINDING 04 dumbbells measure the card (240px fixed today) and print the **full institution name** (`formatEntityName('institution', leader.institution_name, 'sm')`, `INSTITUTO` no longer cut) on its own 12px line above the track below `sm`, beside it at `sm+` with a 140px column.
- Tile legends `text-[8px]` ×6 → `text-[11px]`; triptych `text-[7px]/[8px]` ×6 → 11px (`SAT official`, `gap`, `ASF audits`, `RUBLI detected`, `95% never audited`); FINDING 03's three svg captions → HTML 11px under the svg (the histogram itself measures its card and draws at 1:1 — 13 bars); FINDING 04 legend + row labels 8 → 11/12; Cascade `Anchor` 8.5 → 11.
- Accept (probe EN + ES, 1440/390): `sub10` 102 → **≤ 1** at 1440 (the ObservatoryScatter eyebrow — list it) and 197 → **≤ the ObservatoryScatter's own count** at 390 (list every remaining leaf; none may belong to this page's files); `fixedHeightSvgs`: every svg `gutter` 0 and `vGap` 0 at both widths, `scale` 1.00; `svgTextSizes` from this page's figures empty (glyphs are HTML) — the atlas plate's entries remain; `svgClip`/`svgOut` unchanged (atlas-owned `10`/`100k`); the after crops of the six figures at 1440 and 390 EN + ES compared with `before/crops/*` — same marks, colours, order, callout positions (MacroArc's four boxes within ±12px of before at 1440).

### 3. Links, not roles — `Executive.tsx`, `TopCategoriesChart.tsx`, `PesosAtRiskChart.tsx`
- Chapter tiles (`:635-662`): `ColumnFrame` becomes a module-level component (Change 8) rendering `<Link to={step.story} aria-label className={tileBase…}>` — `role/tabIndex/onKeyDown/onClick` gone; the rail stays a `div`. FINDING cards ×4: `<motion.article>` → `<motion.article>` wrapping content with the card itself as `<Link>` (`motion(Link)` or `<Link className="block …">` inside the article) — no interactive descendants except FINDING 04's hover rows (`onMouseEnter` only, not interactive ✓); the `investigate` reveal labels gain `group-focus-visible:opacity-100` and are always visible below `md`. Category rows (`TopCategoriesChart.tsx:179-190`): the `li` loses `role/tabIndex/onClick/onKeyDown/aria-label`; the name is `<EntityIdentityChip type="category" id={category_id} name={name} variant="name" fullName size="md">` (Day 7b/9 rule-1 link; the fallback rows with string ids `medicamentos…` have no dossier — render them as plain text with the `illustrative` footer already present); `title` tooltips (`:215,:285`) dropped — the risk pip gets a visible `riskLabel` in the mono sub-line instead. Wire rows (`:1883-1927`): the `div` loses `role/tabIndex/onClick/onKeyDown`; the contract title `<p>` becomes `<Link to={`/contracts/${c.id}`}>` (13px, `text-text-primary`, ring), the vendor chip stays (no `stopPropagation`), the row keeps its hover tint via `group`. Cascade rungs `<a href>` ×4 + the GT anchor `motion.a` → `<Link>`; `methodology` `<a>` → `<Link>`; pesos `→ Investigate` ×7 → `<Link to={`/aria?pattern=${code}`}>` in the HTML layer (Change 2), `min-h-6`, ring; `<button onClick={navigate}>` ×4 → `<Link>` with the same classes; `Open ARIA queue` `focus:` → `focus-visible:`.
- Rings on everything that had none: Print (`min-h-6`), the four lens buttons, `Open full Atlas`, `All categories`, `View all`, `Search a vendor`, `Open the ARIA investigation queue`, `methodology` — `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-1`. Targets: `Print`, `Open full Atlas`, `All categories`, `View all` → `min-h-6` (24px).
- Accept (probe EN + ES, 1440/390): `roleLinks` 21 → **0**; `focusableNonNative` 28 → 7 (the atlas orbs, shared — list them); `nested` 0 stays 0; `noFocus` 26 → **8** (the 7 orbs + `▤7 Patterns`, atlas-owned — list); `outlineNone` → the 7 orbs only; `touchSmallReal` 4 → 0; `svgSmall` 7 → 0 at 1440 (the pesos `<a>`s are HTML now; the 390 orbs stay, shared); `hoverOnly` 4 → 0; `rawAnchorsFullReload` — every `main a[href^="/"]` is a router link (manual: Cmd-click on a tile, a category name, a wire title and a rung opens a new tab; plain click does not reload — `performance.getEntriesByType('navigation').length === 1` after the clicks); the Tab walk from the h1 reaches Print → 4 lens buttons → (atlas) → `Open full Atlas` → 4 tiles (`A+ring`) → 4 findings (`A+ring`) → 7 `→ Investigate` (`A+ring`) → `All categories` → 8 category chips → 5 rungs + anchor → methodology → `View all` → 5 × (title link, vendor chip) → 2 CTA links → coda link + 3 chips.

### 4. Headings, the lens control, landmarks — `Executive.tsx`, `CaseTimeline.tsx`, `LeadTimeChart.tsx`
- The eleven section title `<div>`s (`sectionKickers`: `:395`, `:483`, `:506`, `:855`, `:1447`, `:1482`, `:1513`, `:1825`, `:1850`, `:1940`, `:1980`) → `<h2>` with the same classes and ids (`aria-labelledby` keeps working; add `id`s to the three that lack one: seismograph, wire, CTA); the `Start Here` kicker becomes the h2 and `Investigate a vendor.` stays h3 ✓; the FINDING h3s now sit under an h2 ✓. `headings` 6 → 17, `hSkip` 1 → 0; every h2 `scroll-mt-14` (the sticky header).
- Lens control (`:400-433`): `role=tablist/tab` → `role="group" aria-label` with `<button aria-pressed>` (the lens is a filter, not a tab — the Day 7/9 pill convention), rings, and the lens in the URL as `?lens=` via nuqs (`parseAsStringLiteral(['patterns','sectors','categories','sexenios'])`, default `patterns`, `clearOnDefault`) so `/dashboard?lens=sectors` deep-links and the `Open full Atlas` link keeps passing it through. `staticTabs` → [].
- `CaseTimeline` (`:74`) and `LeadTimeChart` (`:69`) `aria-label`s bilingual (`lang` is already a prop); the seismograph's label carries the count + window (`Ten documented cases, 2008–2025, by year, severity and sector`). The chapter tiles' micro-viz svgs keep `aria-hidden` ✓. The atlas section's loading/empty `<p>` gets `role="status"`.
- Accept (probe EN + ES): `headings` 17 with `hSeq` `1,2,2,2,2,2,3,3,3,3,2,2,2,2,2,2,3,2` (h1 → h2s → the four FINDING h3s → h2s → CTA h3 → coda h2) and `hSkip` 0; `sectionKickers` [] (they are `H2:` now — extend the check to `h2`); `staticTabs` []; `ariaPressed` 4 with exactly one `=true`; `?lens=sectors` in the URL after clicking SECTORS and the lens restored on reload; `Open full Atlas` href `/atlas?lens=sectors`.

### 5. Inks — `Executive.tsx`, `TopCategoriesChart.tsx`, `CaseTimeline.tsx`, `LeadTimeChart.tsx`, `PesosAtRiskChart.tsx`, `MacroArc.tsx`
- Risk colours as type → `RISK_TEXT_COLORS`: tile III numeral (`:769` `#f59e0b` 40px, 2.15 — fails even as large text) + its `at stake` sub-line (`:774`) + `The flag` step accent as **type** (`:579` — the rail's rule and drop-line keep `#f59e0b` as marks; `ChapterRail` gets `ink` beside `accent`) + `III`; `MX$1.25T` (`:1049`) + `95% never audited` + `19×` → `RISK_TEXT_COLORS.high`; `6,118` / `RUBLI detected` / `145×` / the `CRITICAL` seals ×5 / `··· N× drop` (`:1722` `rungColors[i+1]` → a parallel `rungInks` from `RISK_TEXT_COLORS`) / `● crit` `● high` `● med` legend text → `RISK_TEXT_COLORS.*`; the `●` glyphs keep `RISK_COLORS` (marks). The h1's `7 in 10` (`var(--color-risk-critical)` at 64px, 3.57) — large display type ≥ 3:1 passes; **keep** (Keep list) and report the ratio.
- Sector colours as type → `getSectorTextColor(code)` / `SECTOR_TEXT_COLORS`: `TopCategoriesChart.tsx:221` sector names, `CaseTimeline` legend years + numbered pills' text, `LeadTimeChart` (now HTML) year labels + `N yrs` counts; the marks (bars, dots, spikes, chips) keep `SECTOR_COLORS`. Badge numerals (`CaseTimeline.tsx:167-176,:201`, white 8px on sector fill 1.43–3.82): → 11px `text-text-primary` on a `color` tint at 18 % with a 1px ring in `color` (numbers readable on every sector), same 18px disc.
- Ochre as small type → `var(--color-accent-hover)` (`#835616`, the Days 6b/7/8 decision) at **full alpha**: `Anchor` + `Training corpus` + `43` (`:1749-1768`, opacity .75/.85 gone), `The spend` kicker/`I`, `All categories`, `View all`, `Open the ARIA investigation queue`, `Start Here`, `methodology`, the FINDING 04 `P6 — capture` span, `▶ ASIPONA` labels, `+56.3pp`; ochre stays on marks (rules, bars, dots, the tile I numeral at 48px — large, report the ratio).
- Muted at opacity → full alpha `var(--color-text-muted)`/`text-secondary`: `≈US$…` ×8 (`:685`, `TopCategoriesChart.tsx:269`), `ranked by premium…` (`PesosAtRiskChart.tsx:93`), `logarithmic scale…` (`:1637`), `← informs every filter…` (`:1782`), the rung operation captions (`:1712`), `(estimates · …)` (`:1454` `text-muted/70`), `· feed frozen…` (`:1853` `/70`), `bar = share…` (`TopCategoriesChart.tsx:304` `opacity-80`), the category `·` separator (`:226` `opacity-50` → `aria-hidden` full alpha). MacroArc's HTML labels (Change 2): era labels keep their band hue but at the AA twin (`#1a5276` → `#123a5a`-class, `#c41e3a` → `#9c1830`, `#7b2d8b` → `#5f2170`; executor measures, each ≥ 4.5 on the plate paper) and full alpha; `EU 10%` → `var(--color-text-secondary)` (the cyan dashed line stays); `Mexico · 68.3%` → `RISK_TEXT_COLORS.critical`-class red ≥ 4.5 (the line stays `#dc2626`).
- Accept (probe EN + ES, 1440): `failingContrast` 114 → **≤ 16** = PlateFrame's `·` ×7 + `Indexed` ×7 (shared) + the h1 `7 in 10` + the 48px tile I numeral (both large, ≥ 3:1 — list every remaining leaf with colour + ratio); `riskAsType` 28 → 0; `sectorAsType` 25 → 0; `dimmed` 30 → ≤ 14 (PlateFrame only); `svgTextFail` 86 → ≤ 21 (all `ObservatoryScatter`); `lint:tokens` PASS (no new raw hex in `src/pages` / `src/components`; the AA twins for MacroArc's three wash hues live beside `ERA_BANDS` as named constants).

### 6. Reading frame, one colophon, prose, captions — `Executive.tsx`
- `max-w-[1100px]` (`:211`) → `max-w-[1010px]` + `data-frame` (keep `mx-auto px-4 sm:px-6 py-6 sm:py-8`); the print rule `.executive-page { max-width: 100% }` stays. The plates lose 42px of width, which Change 2's measured figures absorb.
- Colophon: the credibility strip (`:2040-2060`, a page `<footer class="page-footer">` that hides the shell colophon via `main:has(.page-footer) + footer`) → the page's single colophon `<p>` inside `main` (Days 5/6/8/9): one 13px mono line `AUC 0.785 · {formatNumber(stats.totalContracts)} contracts · EU scoreboard context · open source · RUBLI v0.8.5` + the disclaimer sentence as a second 13px line (max-width 68ch, left-aligned like every other colophon — no `text-center`); the `.page-footer` marker and the `<footer>` element go, so the shell colophon renders once below. `footers` 2 → **1**. Keep the `Shield` glyph before `AUC`.
- Prose: the eleven section intros `text-xs` (12px, `:436`, `:486`, `:509`, `:858`, `:1459`, `:1493`, `:1516`, `:1985`) → `text-[15px] leading-[1.6]` (the D2c measure caps them at 68ch ✓); FINDING bodies `text-xs` ×4 (`:964`, `:1067`, `:1247`, `:1435`) + the FINDING 04 explainer (`:1279`) → 14px (card prose at 476px; `text-sm`); timeline/wire/CTA intros `text-sm` → 15px; FINDING h3s 13px → 15px; the wire's `text-xs` title → 13px (it becomes the row link); `PesosAtRiskChart` footnote `text-[8px]` → 12px mono; MacroArc caption 12 → 12.5px mono (the site's figure-caption size). Mono agates/kickers stay 12–13px; nothing under 11px anywhere.
- Captions: the seven `PlateFrame`s (`:491`, `:515`, `:538`, `:1464`, `:1498`, `:1522`, `:1834`) get `captionFull` (Day 4 rule — a caption spans its figure; today 415px under a 1,052px plate, `captions pct 38`).
- Accept (probe): `frame` `{w: 1010, left == right ± 2}` at 1440 (`data-frame` found); `footers` 1 (the shell's); `proseP` every entry ≥ 14 (list the 12px survivors — mono agates are exempt); `captions` `pct` ≥ 95 ×7; `docH` reported; the header crop at 1440 compared with before (h1 size, accents, lede width unchanged).

### 7. Phones — `Executive.tsx`, `TopCategoriesChart.tsx`
- Cascade Ledger (`:1645-1791`): the `136px minmax(0,1fr) 92px` grid → below `sm` a stacked rung (count 26px on its own line, bar full width, label + pct on one 12px mono line — `whitespace-normal`, no `truncate`); the operation captions (`:1703-1726`) and the GT anchor band (`:1746-1790`) stack the same way (no more one-word-per-line `43 / named / cases`); `truncate` on the rung labels (`:1690`) and operations (`:1707`) → gone at every width (the labels are ≤ 40 chars; at 1010 they fit on one line — report).
- Wire rows: title `truncate` (`:1904`) → wrap (two lines at 390; titles are ≤ 100 chars); the sector column `hidden md:flex` stays; at 390 the chip + title stack above the amount/date (`flex-wrap`). `TopCategoriesChart` caption `truncate` (`:227`) → wrap; the `sm:grid-cols-[28px_1fr_176px_110px_18px]` header/row stays.
- The lens control wraps (`flex-wrap`) instead of overflowing at 334; FINDING 04's dumbbells and the histogram measure their card (Change 2) — no 240/320px fixed drawings.
- Accept (probe 390 EN + ES): `truncated` 13/16 → **0**; `chipsClipped` 4 → 0; `uppercaseWrapped` — the lens control may wrap to two rows (report), no other kicker wraps that did not before; `docOverflow` 0; `innerScrollers` 0; clip census `day04/clipcensus4.mjs` on `/dashboard` at 1440/1280/1024/390 EN + ES → 0 at ≥ 1024 (390: report, the atlas plate excluded); the 390 crops of the Cascade, the wire, FINDING 04 and the pesos plate read (LOOK).

### 8. Perf — `Executive.tsx`, new `components/executive/CaptureLeaders.tsx`, `components/executive/ChapterTiles.tsx`
- `ChapterRail` + `ColumnFrame` + the four tiles (`:547-849`) move to `components/executive/ChapterTiles.tsx` as module-level components (`ChapterTiles({ lang, stats, headlineSpend, headlineSpendUSD })`) — no component declared inside render; the tiles stop remounting on every page render.
- FINDING 04 (`:1255-1440`) with its `capDetail` state → `components/executive/CaptureLeaders.tsx` (`{ lang, leaders }`); the page no longer holds hover state (`:109` gone). `TopCategoriesChart`/`MacroArc` already own their state ✓.
- The Cascade IIFE (`:1531-1794`) and the codaChips memo stay; `useScatterClusters` stays lens-keyed. No new queries.
- Accept: a probe step — capture the DOM node of tile I (`main [data-tile="I"]`), hover capture rows 1–5 (`mouseover` each, 120ms apart), then assert the node is `===` the captured one and its `opacity` computed style is `1` throughout (before: the node is replaced and re-animates from 0); React DevTools not required. `npx eslint` on the two new files: 0 errors (react-refresh rule satisfied — components only). Bundle: report the `Executive-*.js` chunk size before → after from `npm run build`.

## Out of scope (→ PARALLAX backlog)
- `ObservatoryScatter` (shared with `/atlas`, Day 16): 7 orbs `g[role=button] tabindex=0` with `outline: none` and no ring; axis ticks `#7a7064` 4.12 ×21; `HIGH SCALE × HIGH RATE` 7.6px; `10`/`100k` ticks clipped past the svg edge (`svgClip`/`svgOut` 2); at 390 every glyph 2.4–4.5px (the plate scales 0.28) and 14 sub-24px targets.
- `PlateFrame` (shared): `Indexed` stamp at opacity .55 (2.11) ×7 and the `·` separators 1.96 ×7 — the Day 7 backlog item, plus `Indexed` printing `new Date()` instead of the data cut; `captionFull` should become the default once the remaining callers are probed (this page passes it explicitly).
- FINDING 02's typed claims (`ASF reviews ~5%`, `MX$1.25T above 5B`, `~25 years`) have no source line on the card — a citation sentence, not a UI change.
- Three case counts on one page (`1,427 documented corruption cases` in the lede, `43 named cases` in the Cascade, `Ten landmark cases` in the seismograph) — each is true and each explains itself; one glossary sentence in `/methodology` would end the question.
- `document.title` per route (site-wide, Day 6b backlog); the paper-grain `feTurbulence` overlay (sanctioned decoration, paint-time check on the graphics track).
- `formatCompactMXN` prints `240.0B MXN` on the pesos dots (the trailing-zero rule, Day 6b backlog).

## Risk
- **Change 2 rewrites the glyph layer of five figures without changing their marks.** Never write state in render (`useMeasuredWidth` is the only measurer; React #301); render the label layer only when `W > 0`; reserve each figure's height so the page does not shift when it measures (`minHeight` = the computed `H`); compare every after-crop with its before-crop and report anything that moved beyond the label swap (MacroArc's callout boxes ±12px).
- Change 1's DA floor removes seven drawn years from a loved plate — the caption says why, the kicker and lede counts follow (`Four administrations`). If the user wants 2002–2009 shown, the honest form is a hatched "not coded" band, not a line at 0 % — note it in the report.
- Change 3 turns cards into `<Link>`s: a `<Link>` may not contain another interactive element — FINDING 04's hover rows stay non-interactive (`onMouseEnter` only); the wire row's two links (title, chip) are siblings, not nested. Keep `state` off the links (nothing reads it on this page).
- Change 4 adds nuqs to a page that had no URL state — one key (`lens`), `clearOnDefault: true`, and the `Open full Atlas` link must still pass `?lens=` only when non-default.
- Change 6 removes a `<footer>` element: `index.css:1017` (`main:has(.page-footer) + footer`) stays for the other pages; this page simply stops carrying the marker.
- Change 8 moves ~300 lines into two files — pure extraction; the tiles' `steps` array, `tileBase`, the `motion` props and the `data-tile` attribute travel with them. Bilingual audit on both new files.

## Build notes for the executor
Same stack and rules as `DAY-09-institutions.md § Build notes`: worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, branch `parallax/day10-panel` (already created from `origin/main` `f6f13757`; this file is its first commit), servers running (backend `127.0.0.1:8001` = `_parallax_shots/day04/local_backend.py` no-scan — **no backend change today, do not restart it**; Vite `localhost:3009` on the worktree), ≤ 2 browsers, `MSYS_NO_PATHCONV=1` + `D:/` script paths, never rubli.xyz. Order: **STEP 0 → 6 → 4 → 3 → 5 → 8 → 1 → 2 → 7**. One commit per change: `feat(dashboard § PARALLAX D10 § Change N): …`, body cites `docs/parallax/DAY-10-panel.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Progress file `_parallax_shots/day10/progress.md` (one line after EVERY step, failures included); report `_parallax_shots/day10/report.md` (per change: done, every acceptance number before → after, deviations, noticed-not-fixed → backlog). Probes: extend `_parallax_shots/day10/audit10.mjs` (tag `after`) with the checks named in each Accept (band-edge years, MacroArc first tick / min cy, `sectionKickers` on `h2`, `ariaPressed`, tile node identity across hovers, `captions pct`); run EN and `LANG_ES=1` at `WIDTHS=1440,1024,390` (+ one 1920 for the frame); census `day04/clipcensus4.mjs` on `/dashboard` at 1440/1280/1024/390 EN + ES → 0 at ≥ 1024 (390: report). Crops: `_parallax_shots/day10/crop10.mjs` (tag `after`) — the same 14 blocks at 1440 and 390, EN + ES (never widen the viewport for mobile) — **LOOK at every crop** and put the before/after pairs of the six figures side by side in the report. Review: `vercel-react-best-practices` on the diff, `rubli-bilingual-audit` on every touched TSX (the two new files included); gates from `frontend/`: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` · `npm run build` · `npm run lint:tokens` · `npx eslint` on touched files · vitest on touched tests. Backend untouched → no pytest. Do NOT bump BUILD_ID, push, deploy or merge — Fable judges first.
