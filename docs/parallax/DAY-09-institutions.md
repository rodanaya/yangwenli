# PARALLAX Day 9 — Institution dossier + ranking (`/institutions`, `/institutions/:id`)

Routes: `/institutions` (Folio·VII «Institutions · where the high-risk money is» — masthead + scope control → FINDING callout → ACT I «Leaders of the pack»: `SpectralRegister` plate («La Placa», every federal buyer as one stroke on the 0–100 integrity axis) → «The red flags» five `ActaCard`s → «The honor roll» five `ChampionCard` rows → ACT II «The full ranking»: search + tier pills + sector pills + the 50-row table with `PillarBoleta` row-expand + pagination → ACT III «The shape of accountability» (collapsed; holds only the methodology footnote since the histogram was absorbed into the plate) → `PageFooter`; `?tab=reporte` swaps the body for `ReportCard` «El Corte Nacional»; `?scope=subnational|all` replaces the plate with a note) and `/institutions/:id` (`WayfindingSpine` → `InstitutionHero` (INST · I-00251, § kicker, h1, siglas, meta rule, `DualSeal`, lede) → `InstitutionStatStrip` → § La boleta (`PillarBoleta`) → § La lectura (`InstitutionReading`: waterfall + EU deviation + where-the-risk-sits) → § La concentración (`InstitutionConcentration`: HHI ladder + `ConcentrationRegister` 25 rows) → § El expediente (`InstitutionRecord`: risk over time, incumbency, «What they buy», largest contracts) → § Responsables de la UC (officers table) → `ProvenanceFooter`). Files: `pages/InstitutionLeague.tsx` (1,891), `pages/InstitutionDossier.tsx` (336), `pages/ReportCard.tsx` (692, the `?tab=reporte` body), `components/institution/{SpectralRegister 443, PillarBoleta 151, InstitutionHero 573, InstitutionCommandPanel 323, InstitutionDossierBody 548}`, `hooks/useLeagueField.ts`, `lib/institution-pillars.ts`, `lib/tiers.ts`, `components/dossier/command/primitives.tsx` (shared by the four dossiers), `components/WaterfallRiskChart.tsx` (shared with the vendor dossier), `backend/api/routers/institutions.py` (`top-categories`, `risk-waterfall`). Route → component verified in `App.tsx:90,616` (league) and `:42,490` (dossier). Branch `parallax/day09-institutions` off `origin/main` (`9fe97379`) in worktree `.claude/worktrees/parallax-day01`.

**Classification: GOLD — enhance in place.** The league's editorial spine stands — the plate (position = score, stroke height = money at risk, no dot-grid), the five verdict cards with the 60px numerals, the honor roll as a footnote list, the tier/sector pill filters, the table with the tier tile as the verdict and the boleta on expand — and so does the dossier's order (two seals → boleta → reading → concentration → record → officers). No `/designus`. What is wrong is the Days 4–8 list (inks, rings, rows-as-buttons, frame, footers, 10px floor) plus **two honesty defects the probe found on both dossiers and confirmed on prod**: «What they buy» is empty for every institution because `top-categories` filters on `c.institution_name`, a column `contracts` does not have (the `OperationalError` is swallowed → `rows = []` → 404 "No contracts found for institution 251"), and the concentration register's rows are 686px wide inside a 1,086px list — the Day 2c `li` measure (`main :where(li) { max-width: 68ch }`) caught them, exactly as the Day 5 backlog predicted for this page.

## Audit evidence (2026-09-23, local backend `127.0.0.1:8001` no-scan, Vite 3009 on the worktree at `9fe97379`; prod touched only for three single GETs to confirm the 404 and the waterfall)

Probe `_parallax_shots/day09/audit9.mjs` (from `day08/audit8.mjs`; routes `league` `/institutions`, `league-sub` `?scope=subnational`, `reporte` `?tab=reporte`, `dossier-a` `/institutions/251` IMSS = the lede's top exposure, `dossier-b` `/institutions/1425` CENSIDA = red flag #1; D9 checks: empty cells, `tr[role=button]`, `li` measure, table min-width, tier tiles < 11px, plate annotation bboxes vs the ch estimate, stat-strip label clipping, broken TOC anchors, raw-ES sector names on EN), runs `before-{en,es}.txt` + `before/{en,es}-report.json`; full pages `before/shots9.mjs` → `before/{league,dossier-a,dossier-b,reporte}-{en,es}-{1440,390}-full.png`; endpoint timings `endpoint-timing.txt`.

| Measure | league 1440 EN (ES identical unless noted) | league 390 | dossier IMSS / CENSIDA 1440 | dossier 390 |
|---|---|---|---|---|
| sub-10px text leaves | **2**: plate eyebrow `PLATE · I — THE SPECTRAL REGISTER` **9.5px** (`SpectralRegister.tsx:161`), `MEDIAN 46.1` **8.5px** svg (`:281`) | **6**: + callouts `Instituto Mexicano del… · 996.2B MXN` ×3 at **9px** (`:317`, one clipped 10px at the right edge), `No federal institution reaches this band` 9.5px (`:244`) | 0 / **10** — `WaterfallRiskChart` `z=0.82` ×10 at **8.5px** `#736a65` 2.81:1 (`WaterfallRiskChart.tsx:84`; IMSS shows 0 because its waterfall never loads locally, see below) | same |
| contrast < 4.5:1 — every failing leaf, computed | **224** (`minC` 1.40): medal numerals `#d4d4d8` **1.40** / `#facc15` **1.45** / `#d97706` (`InstitutionLeague.tsx:531`); `/ 100` `#736a65` at opacity .5 2.01 ×5; weak-pillar cells `A 18/30` `#f59e0b` **2.04** ×~40 (`pillarDeficitColor` → `var(--color-risk-high)` as 13px type, `PillarBoleta.tsx:593`); rank numerals `#5e7fa8` at opacity .65 **2.27** ×~40 (`:574`); tier pills/tiles in `TIER_STYLES.color` as 10–13px type: Satisfactorio `#5e7fa8` 3.66, Regular `#d97706` 2.82–3.03, Deficiente `#ea580c` 3.15–3.38 (`lib/tiers.ts` — the mark colours used as type); **svg** band labels `DEFICIENT 3.15 · ADEQUATE 2.82 · SATISFACTORY 3.66` + median tag 4.14 | 175 | **10 / 24**: `·` `#736a65` at opacity .5 ×2 (`InstitutionHero.tsx:467`); boleta values `16/25` `#f59e0b` 2.04 ×3 and `8/30` `#ef4444` 3.57 (`PillarBoleta.tsx:685`); siglas `IMSS` at opacity .6 **2.46** (`:168`); `WayfindingSpine` `— / —` `text-muted/40` 2.83; `49.0` 56px `#d97706` 3.03 (large, AA-large passes) + `Adequate` 12px 3.03; CENSIDA: `2,798` HHI `#f59e0b` 2.04, `5,529 / 6,446` `#ef4444` 3.57, `of contracts` `#b45309` 3.71, `z=` ×10 2.81 | same |
| `riskPaletteFails` / opacity-dimmed text | 60 / **52** (`/ 100` ×5, rank numerals ×~45, siglas) | 11 / 52 | 4 / 3 · 7 / 13 | same |
| frame / footers / prose | **1,176** (`max-w-screen-xl`, `:33` and the `TabBar`); **2 footers** (`PageFooter` + shell colophon); lede 15px ✓, scope disclaimer **12px mono, 263 chars** (`:149`) | 366 | **1,152** (`max-w-6xl`, `InstitutionDossier.tsx:214`) / 1 footer ✓; lede 18px serif ✓; `ProvenanceFooter` 13.5px (`:89`), officials note 13px | 366 |
| empty cells `—` | **34** in the money-at-risk column (`:1160`: `money_at_risk_mxn > 0 ? … : '—'`; the API returns **`0.0`, not null** — UNAM, DICONSA Oaxaca, El Colegio de Michoacán…: institutions with no high-/critical-risk contracts, printed as "no data") | 34 | 0 | 0 |
| rows-as-buttons | **50 `tr[role=button] tabIndex=0 onKeyDown`** (`:542-556`); `nestedInteractive` **100** (the expand `<button>` and the dossier `<a>` inside each row); `ChampionCard` (`:226`) and `ActaCard` (`:283`) are `<button onClick={navigate}>` — no Cmd-click, no middle-click | same | 0 | 0 |
| no focus style / targets < 24px | **131** (tabs `Ranking/Report`, scope radios, 6 tier pills, 13 sector pills, sort headers, 50 expand buttons at **16×16**, 50 dossier icons, pagination) / **52** | 131 / 52 | **9 / 8** (`Copy siglas` 18px, `ControlGroup` `SPEND RISK ALL MED+ HIGH+ CRIT` 23px, `See full methodology ↗` `<button>` that navigates) | same |
| focusable non-native | 50 (`tr`) | 50 | **1**: `RiskOverTimePanel` recharts `svg[role=application]` (`primitives.tsx:163`, shared) | 1 |
| truncation / clamp | 0 | **4** `ActaCard` names `line-clamp-2` (`:300`) — "Centro Nacional para la Prevención y el Control de…"; honor-roll names squeezed to a 60px column by the fixed `w-12` score + `w-24` tier (`:170-187`) | 0 | 0 |
| inner scrollers | 1 (sector pills 1,561 in 1,136) | **3**: tier pills 749, sector pills 1,561, **the table `min-w-[900px]` in 334** | 0 | 2 (contracts 380 in 300, officers 516 in 334) |
| `li` measure (D5 backlog QC: `li.width < ul.width − 40`) | — | — | **`ul` 1,086 → `li` 686 ×25** — `ConcentrationRegister` (`InstitutionDossierBody.tsx:342`): the DotBar, amount and risk columns sit 400px left of the list's edge (visible on `before/dossier-a-en-1440-full.png`) | — |
| headings / kickers | h1 → h2 ×4, `hSkip` 0; ACT III `h2` heads a collapsed section whose only content is the footnote `RUBLI Procurement Health Index v0.8.5 · …` (`:1780-1812`) | | h1 → h2 ×5 ✓, `§` kickers ✓ | |
| tier tile / badge sizes | tier tile **10px** ×50 (`:1120` `fontSize: isCritico ? '11px' : '10px'`), `worstPerformerBadge` **7px** (`:580`), `Percentile 100` wraps to two lines in a 96px cell (`:1147`) | | | |
| trend column | `TrendIcon` tests `'declining'` (`:169`); the API emits **`deteriorating`** (`compute_scorecards.py:116`) → the declining arrow never renders; 95 of 100 rows `stable` | | | |
| reliability | the headline gates at `RELIABLE_MIN = 30` contracts but the table ranks **#3 Instituto Tecnológico El Llano (12 contracts)**, #7–#8 DICONSA branches (13–52) with no marker | | | |
| plate (`SpectralRegister`) | annotations = svg `<text>` at 10px with names cut by `formatEntityName(…, 'sm')` (28 chars: "Instituto Mexicano del…", "Grupo Aeroportuario de la…"), collision by a **5.2px-per-char estimate** (`:107`) not a measured box; the svg is clickable (`onClick` → nearest stroke) with no keyboard path (`:186-194`); band labels/median in mark colours | callouts 9px, one clipped 10px | | |
| dossier endpoints (IMSS 251 / CENSIDA 1425, local) | | | detail 0.16s · `risk-profile` **16.6s** uncached (1h cache) · `vendor-pool` 0.02s · **`risk-waterfall` 251 never returns (> 280s; the live `AVG` over `contract_z_features` joined to 662K contracts on the cold disk — prod lacks the table and falls to `institution_z_means` in 0.3s)** · `risk-timeline` 1.6s · **`top-categories` 404 on both (and on prod)** · contracts 4.2s · officials 0.03s; console: `API Error: timeout of 30000ms exceeded` + the 404 on every dossier load | |
| copy / names | `Vih/Sida` — `InstitutionHero` uses its own `toTitleCase` (`:533`, rule 3) which lowercases siglas inside names; `Instituto Mexicano Del Seguro Social` ("Del") | | | |
| italics / `US$` in ES / raw ES sector names on EN | 0 / 0 (`usdEs` 1 on EN = the lede's `formatDualCurrency`, by design) / 0 ✓ | ✓ | 0 / 0 / — | ✓ |
| `?tab=reporte` | 17 failing leaves: `EU SCOREBOARD CONTEXT` `#22d3ee` **1.72** (`var(--color-oecd)`, `ReportCard.tsx:256`), `System Health · National Integrity Index` `#f59e0b` 2.04, `View full methodology →` 2.15, sector scores `#d97706` 3.19 ×4 / `#5e7fa8` 4.14 ×8; raw hex `#27272a #c41e3a #52525b #5e7fa8` (`:310,:329,:401`); `noFocus` 3; 1 footer ✓; at 390 ES 3 leaves at 6.1/3.8px (a CSS-scaled svg) | | | |

Toolkit passes run IN the audit:
- **web-design-guidelines** (rule set fetched 2026-09-23):
  - `InstitutionLeague.tsx:542-556` — `<tr role="button" tabIndex onKeyDown>` navigates (should be `<a>`/`<Link>`); `:592` expand button inside it (nested interactive); `:226`, `:283` — `<button>` used for navigation (`ChampionCard`, `ActaCard`); `:263`, `:350`, `:394` — `transition-all` → list properties; `:300` — `line-clamp-2` on an institution name; `:307-314` — search input lacks `name`/`autocomplete`, placeholder ends with `…` ✓; `:327-411` — 20 filter pills without `focus-visible:*`; `:434-517` — sort headers without `aria-sort` (the shared `SortHeaderTh` has it); `TabBar` (`:1832-1860`) — tabs without focus ring or `aria-current`; `:531-533` — raw hex medals; `:580` 7px badge; `:1120` 10px tier tile; `:1147` `Percentile 100` wraps; `:1160` `'—'` for a zero.
  - `SpectralRegister.tsx:186-194` — click target with no keyboard alternative (gesture-only); `:107` — layout by character estimate instead of measurement; `:161,:244,:281,:317` — 8.5–10px text; `:220` — tier mark colours as text.
  - `InstitutionDossier.tsx:94-100` — `<button onClick={navigate}>` for a link; `:214` — 1,152 frame; `:305` — `title` duplicating visible text; `:318-319` — `RISK_COLORS` (marks) as numeral ink; `:89` — 13.5px running text.
  - `InstitutionHero.tsx:92,:533,:555` — private `toTitleCase` instead of `formatEntityName` (CLAUDE rule 3); `:168,:467` — opacity-dimmed text; `:438-447` — copy button without focus ring, 18px target.
  - `InstitutionDossierBody.tsx:342` — `<li>` rows nobody bounded (the D2c measure); `:384-405` — `ControlGroup` buttons 23px, no ring; `:496` — category names 12px.
  - `primitives.tsx:163` — `EditorialAreaChart` without `decorative` → a `role=application` Tab stop with no ring (shared; the vendor/category/sector dossiers have the same stop).
  - `PillarBoleta.tsx:591-595` — `pillarDeficitColor` returns mark colours; used as 12–13px type at `:685` and in `WeakPillarCell` / `ActaCard`.
  - `WaterfallRiskChart.tsx:84` — 8.5px labels (shared with the vendor dossier).
  - `ReportCard.tsx:114,:310,:329,:401` — raw hex; `:256` — `--color-oecd` cyan as 12px type (1.72); `:516-520` — button that navigates.
  - Pass: `tabular-nums` on every numeral, `Intl` formatters, `prefers-reduced-motion` global, `aria-label` on icon buttons, `role=radiogroup` on the scope control, `aria-expanded` on the expand buttons and ACT III, `aria-live` on the empty state, `?tab= ?scope= ?sort= ?order= ?page= ?grade= ?sector= ?q=` all in the URL.
- **ui-ux-pro-max** `--domain ux` "clickable table row navigation nested button" → *Back button: preserve navigation history* (rows must be real links: history, Cmd-click) and *Table handling: horizontal scroll or card layout on mobile* (the 900px table in 334); "empty cell dash zero value" → *Empty states: show the fact, not blank* (a zero is a fact — "0 MXN at risk", not "—"); "ranking table sortable columns mobile" → *mobile-first, card layout*. `--domain chart` "strip plot annotation label collision" → no verified match for the strip plot itself (fallback: box/beeswarm family — *label directly, never colour alone*; keep the plate, fix the labels). `--domain typography` → no verified match (font-pairing catalogue only); the 11px floor is the Day 1 rule. `--stack react` "sortable table header aria-sort" → 0 results; the repo's own `SortHeaderTh` is the answer.
- **vercel-react-best-practices**: every list query keyed and gated ✓; the 50-row table is under the virtualisation line ✓; `useLeagueField` assembles 5 pages in parallel and caches 30 min ✓; `SpectralRegister` measures via `ResizeObserver` and sets state in the callback (fine) but keeps its own copy of the pattern — `useMeasuredWidth` exists since Day 6; the dossier fires 9 queries on mount, two of which (`risk-waterfall`, `top-categories`) fail on every load.
- **a11y** (Fable's pass): the rows above; `liveRegions` 1 (empty state) ✓; the plate has `role=img` + a full sentence ✓ but its callouts are svg `<text>` after the label; `officials` table has no `<caption>`/`aria-label`.
- **folio voice**: intact — Folio·VII, § kickers, «La Placa», ActaCards, "Honor roll — with reservations". No copy change except the zero cell, the thin-sample marker and the two dead panels that will finally print something.

## Keep

- The league's spine: the FINDING callout, the Spectral Register plate (strokes, bands, median rule, leader-lined callouts, the empty-Excellent annotation, the hover tooltip, the caption agate), the five red-flag `ActaCard`s with the 60px `EB Garamond` numerals and the weakest-pillar agate line, the honor roll as a flat five-row footnote, the tier + sector pill filters with their dots, the table's columns and the tier tile as the verdict, the `PillarBoleta` row-expand, the pagination, `?tab=reporte` «El Corte Nacional».
- The dossier's order and every section: INST index strip, § EL EXPEDIENTE kicker, the `DualSeal` (model risk + process integrity), the drop-cap lede, the stat strip, La boleta (56px score + five bullet rows + agate), La lectura (waterfall + EU scoreboard + where-the-risk-sits), La concentración (HHI ladder with the 4,000 line + the 25-row register with SORT/FLOOR), El expediente (risk over time, incumbency, what they buy, largest contracts), Responsables de la UC, the provenance footer.
- `EntityIdentityChip` for every vendor (rule 1 ✓), `formatCompactMXN` in cells, `formatDualCurrency` in the lede, MDP in ES, `DotBar` everywhere (no inline circles), the vivid tier palette on every **mark** (strokes, bands, left borders, pill dots, bullet fills, row tints), no green, no italic, no dot-grid.

## STEP 0 — dead code (its own commit, before Change 1)

`git grep` first (direct imports, lazy `import()`, string literals, tests, `_dev/ChartCatalog.tsx`, i18n registration in `src/i18n/index.ts`) — the audit's greps found:
- `pages/InstitutionProfile.tsx` (2,354 lines; unrouted since M5 2026-05-18, `App.tsx:38-40` keeps a commented import) — imported by nothing.
- `pages/InstitutionCompare.tsx` (1,214; "deferred to v1.1", `App.tsx:641` comment only) and `pages/InstitutionScorecards.tsx` (895; the retired `?tab=fichas`) — imported by nothing.
- Orphaned by those three: `components/RedThreadPanel.tsx` (104), `components/NarrativeCard.tsx` (65) and, once `NarrativeCard` goes, `lib/narratives.ts` (379); `i18n/locales/{en,es}/institutionScorecards.json` (never registered in `i18n/index.ts`).
- Stays: `components/ui/ScorecardWidgets.tsx` (`VendorHero`, `useVendorData` import it — its `GradeBadge10`/`InstitutionScorecardCard` exports become unused; leave them), `InstitutionBadge.tsx` (`EntityProfileDrawer`, `CaseDetail`, `InstitutionsTab`), `NetworkGraphModal` (`VendorDossier`), `WaterfallRiskChart`, `GenerateReportButton`, `ImpactoHumano`, `ChartDownloadButton`, `RiskFeedbackButton`, `AddToWatchlistButton`, `CategoryCaptureDumbbell`, `lib/institution-groups`.
- Clean the `App.tsx` comments that name the deleted files (`:38-40`, `:507-509`, `:641`); the stale `pillar` comment in `ScorecardWidgets.tsx:212` and the `tiers.ts:5` docstring mention.

Gate: tsc + build + lint:tokens. Commit `refactor(institutions § PARALLAX D9 § STEP 0): remove the three unrouted institution pages and their orphans` (≈ 5,100 lines). Report the exact line count.

## Change (8)

### 1. The two dead panels — `backend/api/routers/institutions.py`, new `backend/scripts/precompute_institution_categories.py`, `InstitutionDossierBody.tsx` (§ El expediente «What they buy»), `InstitutionDossier.tsx`
- `top-categories` (`institutions.py:930-1010`): the filter `c.institution_name = ?` names a column `contracts` does not have; the `OperationalError` is swallowed and every institution 404s. Rewrite on the canonical taxonomy (CLAUDE.md rule 4 — `category_stats.*` / `categories`, not `partida_codes`, which this DB does not have): `SELECT c.category_id, cat.name_es, cat.name_en, cat.sector_id, COUNT(*), SUM(amount_mxn), AVG(risk_score), direct-award % FROM contracts c JOIN categories cat ON cat.id = c.category_id WHERE c.institution_id = ? GROUP BY c.category_id ORDER BY SUM DESC LIMIT ?` (`idx_c_institution_id`). Because that is a 662K-row walk for IMSS (the same class as `risk-profile`'s 16s), read a precomputed **`institution_category_stats`** (`institution_id, category_id, contract_count, total_value_mxn, avg_risk_score, direct_award_pct, high_risk_count`) first and fall back to the live GROUP BY only when the table is absent; wrap the result in the router's `_top_cache` (1h) either way; **an empty result is `200 {data: []}`, never a 404**. Write `scripts/precompute_institution_categories.py` (one `GROUP BY institution_id, category_id` over `contracts` → the table + index; idempotent `CREATE TABLE IF NOT EXISTS` + `DELETE` + `INSERT`), run it on the local `RUBLI_NORMALIZED.db`, and export a plain-text SQL dump to `_parallax_shots/day09/precompute/institution_category_stats.sql` — Fable loads it on prod in-container before the deploy (Day 8 pattern). The `year` filter stays on the live path only.
- `risk-waterfall` (`:458-520`): read `institution_z_means` **first** (it exists here and on prod) and only compute the live `AVG` over `contract_z_features` when the precompute row is missing. Prod behaviour is unchanged (it has no `contract_z_features`); locally IMSS stops timing out.
- «What they buy» (`InstitutionDossierBody.tsx:489-506`): category names via `EntityIdentityChip type="category" id={category_id} name={…} size="sm" fullName` (rule 1 — the row links to `/categories/:id`), the DotBar by value stays, add the row's `DA %` and risk indicator (`RISK_TEXT_COLORS`) in the 12px mono sub-line like the register; 12px names → 13px; the empty note now reads `No categorised contracts for this institution.` / `Sin contratos categorizados…` and appears only for a genuinely empty list. `InstitutionDossier.tsx:189-197` drops the `.catch(() => null)` on a 404 (keep `retry: false`).
- Tests: one backend test per endpoint on the test fixture (`tests/test_institutions*.py` or the nearest existing file): `top-categories` returns 200 with `data: []` for an institution without categorised contracts and a sorted non-empty list for one with; the waterfall test asserts the precompute path is taken when `institution_z_means` has the row. Run `python -m pytest backend/tests -q --tb=short -p no:cacheprovider -k "institution or scorecard"` (the full suite takes 1h20m on this disk — run it once at the end with no probes in flight, report the counts).
- Accept: `curl /api/v1/institutions/251/top-categories?limit=8` and `/1425/…` → 200 in < 1s with ≥ 5 rows each; `/251/risk-waterfall` → 200 in < 1s with 16+ items locally; the dossier console has **0** API errors on both ids (`consoleErrors` 3 → 0, `loadMs` for IMSS 33.7s → ≤ 12s on the second load); «What they buy» prints ≥ 5 category chips on both dossiers EN + ES; backend tests green; the SQL dump exists and loads into a scratch copy of the DB.

### 2. League honesty: the zero cell, thin samples, the trend that never renders — `InstitutionLeague.tsx`, `i18n/locales/{en,es}/institutionleague.json`
- Money-at-risk cell (`:1160`): `money_at_risk_mxn === 0` → `0` in `text-text-muted` with a visible-on-hover `title` and an `aria-label` `No high- or critical-risk contracts` / `Sin contratos de riesgo alto o crítico`; only `null` prints `—`. Same rule in `ActaCard`'s agate and `SpectralRegister`'s tooltip (`formatCompactMXN(0)`).
- Thin sample: rows with `total_contracts < RELIABLE_MIN` (the headline's own 30) get a mono marker after the name — `n = 12` in `text-text-muted` with `title`/`aria-label` `Below the 30-contract reliability floor — headline excludes it` / `Debajo del piso de 30 contratos…`; the plate already floors at 30. Nothing is hidden; the reader sees why #3 is a 12-contract institute.
- `TrendIcon` (`:169`): accept `deteriorating` (the API's word) as well as `declining`; the icon gets `aria-label` from the same key. Report how many of the 50 rows now show ▼.
- `Percentile 100` (`:1147`) → `whitespace-nowrap` and the column `w-24` → `w-28` (or print `P100` — executor's call, report); `worstPerformerBadge` 7px → 11px; the tier tile 10/11px → **11px** at every width; `/ 100` at opacity .5 → `text-text-muted` full alpha.
- Accept (probe, EN + ES, 1440/390): `emptyCells` 34 → **0** on page 1 (only `null` may print `—`; report the count if any); `tierTiles` (< 11px) 50 → 0; the dump contains `n = 12` beside «Instituto Tecnológico El Llano» and the `title` text; `Percentile 100` is one line (`lines` 1 via the readout probe); `dimmed` 52 → the two `·` in the hero at most.

### 3. Rows are links, not buttons — `InstitutionLeague.tsx` (table, `ChampionCard`, `ActaCard`, `SortHeader`, `TabBar`, pills)
- Table rows (`:542-556`): the `<tr>` loses `role`, `tabIndex`, `onClick`, `onKeyDown`, `aria-label`; the institution name becomes `<EntityIdentityChip type="institution" id={…} name={…} variant="name" fullName size="sm" state={{ institutionName }}>` (Day 7b's rule-1 link; check the chip's `state`/`to` props — the wayfinding `data-wf-row` stays on the `<tr>`); the expand button stays a `<button aria-expanded aria-controls={rowPanelId}>` at `min-h-6 min-w-6` with a ring; the trailing dossier icon column (`:1168-1178`) is removed (the name is the link). Row hover tint stays.
- `ChampionCard` (`:226`) and `ActaCard` (`:283`): `<button onClick={navigate}>` → `<Link to={`/institutions/${id}`} state={{ institutionName }}>` with the same classes + `focus-visible:ring-2 focus-visible:ring-accent`; the `aria-label` moves to the link. `transition-all` ×3 → `transition-colors`.
- `SortHeader` (`:434-460`) → the shared `SortHeaderTh` from `@/components/ui` (aria-sort, ring); if its API does not fit the `th` layout here, add `aria-sort` on the `<th>` + a ring on the button and report.
- `TabBar` (`:1832`): `role="tablist"`/`tab`/`aria-selected` (they are URL tabs: `<Link to="?tab=…" aria-current="page">` is equally right — executor's call), ring; scope radios, 20 filter pills, pagination, the ACT III disclosure: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-1`; the search input keeps `focus:border-accent-data/50` and gains `name="q"` `autoComplete="off"` `spellCheck={false}`.
- Accept (probe, EN + ES, 1440/390): `trButtons` 50 → **0**; `focusableNonNative` 50 → 0; `nestedInteractive` 100 → 0; `noFocusStyleAtAll` 131 → **0**; `touchSmallReal` 52 → 0 (expand buttons ≥ 24); Tab walk from the h1 reaches `Ranking`, `Report`, the three scope radios, the five ActaCard links (`A+ring`), the five honor-roll links, the input, the pills, the sort buttons, then 50 row name links each followed by its expand button; Cmd-click on a row name opens a new tab (manual: `target` absent, `href` present in the dump); `?sort=&order=` still written; `aria-sort` present on the active header.

### 4. The plate's callouts and floor — `SpectralRegister.tsx`
- Callouts: the 8 (3 on phones) leader-lined labels move from svg `<text>` to an HTML label layer over the svg (absolute `<div>`s, `pointer-events: none`) seated with `placeLabels` / `measureLabel` from `lib/plateLabels.ts` (Day 4/7 pattern) — **full institution names** (`formatEntityName(…, 'full')`, two lines allowed via `fitLabel`) + the amount on its own mono line, **11px**, ink `var(--color-text-secondary)` on plate paper (`background: var(--color-background-elevated)`, 2px padding); collision by measured boxes, never the 5.2px estimate; the leader line stays in the svg and ends at the label's bottom-centre. Median tag 8.5px → 11px on paper at the axis; the empty-Excellent note 9.5 → 11px; the plate eyebrow 9.5 → 11px (`:161`); band labels 13px → keep size, ink → the tier **text** ink (Change 5) so `DEFICIENT / ADEQUATE / SATISFACTORY / EXCELLENT` read ≥ 4.5.
- Keyboard: the strokes are not 476 Tab stops; add one keyboard path — the 8 callouts render as `<a href="/institutions/:id">` (they already name the top exposures) inside the label layer with `pointer-events: auto` and a ring; the svg keeps `role=img` and the pointer hover/click. `useMeasuredWidth` replaces the private `ResizeObserver` (`:56-65`).
- Accept (probe, EN + ES, 1440/390): `smallCount` on the league 2 → **0** (1440) and 6 → 0 (390); `svgTextContrast` 4 → 0; `annOverlaps` 0 with **measured** boxes and every label box inside the figure (add the check: label bbox ⊂ figure bbox); `svgClip` 1 → 0 at 390; the dump contains `Instituto Mexicano del Seguro Social · 996.2B MXN` in full (no `…` in any callout); Tab walk includes 8 `A+ring` callout links (3 at 390); the plate crop at 1440 and 390 EN + ES compared with `before/league-en-1440-full.png` — same strokes, bands, median, leader lines.

### 5. Inks — `lib/tiers.ts`, `PillarBoleta.tsx`, `InstitutionLeague.tsx`, `InstitutionHero.tsx`, `InstitutionDossier.tsx`, `ReportCard.tsx`, `components/nav/WayfindingSpine.tsx`
- `TIER_STYLES` gains an `ink` per tier — the AA text twin of `color` on the page paper (measure with the probe's `contrastOf`; steel ~`#2f4a6b`/`#3f5f86`, Regular `#92400e`-class, Deficiente `#9a3412`-class, Crítico `#991b1b`-class — executor measures, every one ≥ 4.5 on `--color-background` and `--color-background-elevated`; report the six ratios). `color` stays for marks (strokes, bands, borders, dots, tints). Type sites switch to `ink`: tier pills (`:342-363`), tier tile (`:1103-1125`), `ChampionCard` tier label (`:186`), `ActaCard` pill (`:290-306`), the boleta score + tier pill on the dossier (`InstitutionDossier.tsx:250-265`), `ReportCard` tier chips (`:338`), plate band labels (Change 4). The 56px scores may keep `color` (large text ≥ 3:1) — report both ratios.
- `pillarDeficitColor` (`PillarBoleta.tsx:591`) returns marks; add `pillarDeficitInk(frac)` → `TIER_STYLES.Excelente.ink` / `RISK_TEXT_COLORS.high` / `RISK_TEXT_COLORS.critical` and use it for every text site (`WeakPillarCell`, `ActaCard` agate, the boleta value column `:685`); the bullet fills keep `pillarDeficitColor`.
- Rank numerals: medals `#facc15 #d4d4d8 #d97706` stay on the crown icon only (mark); the numeral is `text-text-primary` (top 3) / `text-text-secondary` (rest) at full alpha — `opacity: 0.65` (`:574`) gone; worst-performer numerals `RISK_TEXT_COLORS.critical`.
- Hero: siglas `opacity: 0.6` (`InstitutionHero.tsx:168`) → `var(--color-text-secondary)` full alpha; `·` separators (`:467`) `aria-hidden` full alpha; `WayfindingSpine` `— / —` `text-muted/40` → `text-text-muted` (shared, 1 token — every dossier gains it). Officers table risk numeral (`InstitutionDossier.tsx:319`) `RISK_COLORS` → `RISK_TEXT_COLORS` (the 8px dot keeps `RISK_COLORS`); CENSIDA's `of contracts` sub-line `#b45309` at 13px → `RISK_TEXT_COLORS.high` with `opacity` 1 (the `StatStrip` sub's `opacity: 0.85` on coloured cells, `primitives.tsx:75` — shared; set 1).
- `ReportCard.tsx`: `#27272a #c41e3a #52525b` → `var(--color-border)` / `RISK_TEXT_COLORS.critical` / `var(--color-text-muted)`; `#5e7fa8` (`:401`) → `TIER_STYLES.Satisfactorio.ink`; `var(--color-oecd)` cyan as 12px type (`:256`) → `var(--color-accent-hover)`; `OCHRE` 12–13px text → accent-hover; the three unringed buttons ringed; `View full methodology →` `<button>` → `<Link>`.
- Accept (probe, EN + ES, 1440): league `failingContrast` 224 → **≤ 2** (list every remaining leaf with colour + ratio; the only tolerated ones are shared PlateFrame-class leaves the page does not own); `riskPaletteFails` 60 → 0; `dimmed` 52 → ≤ 2; dossiers 10/24 → **0** outside the shared chip (list); `reporte` 17 → 0; `svgTextContrast` 0; `lint:tokens` still passes (no new raw hex in `src/pages`/`src/components`).

### 6. Reading frame, one footer, the empty Act — `InstitutionLeague.tsx`, `InstitutionDossier.tsx`, `InstitutionHero.tsx`
- League: `max-w-screen-xl` (`:33` and `TabBar:1841`) → `max-w-[1010px]` (keep `mx-auto px-4 sm:px-6`); the scope disclaimer (`:149`, 263 chars, 12px mono) → 13px; ACT III (`:1780-1812`) — a disclosure whose only content is the footnote line — is removed and its footnote becomes the page's single colophon `<p>` inside `main` (Day 5/6/8 pattern) replacing `<PageFooter />` (`:1814`): `RUBLI Procurement Health Index v0.8.5 · COMPRANET 2002–2025 · OECD / IMF methodology · Source: COMPRANET / SHCP` with the methodology link. `footers` 2 → 1, `headings` 5 → 4.
- Dossier: `max-w-6xl` (`InstitutionDossier.tsx:214`, the skeleton `:120`) → `max-w-[1010px]`; `ProvenanceFooter` prose 13.5 → **15px** in a 640 column, its `<button onClick={navigate}>` → `<Link to="/methodology">` with a ring; the officials note 13 → 15px serif (`:333`); the hero lede stays 18px in `66ch`.
- Accept (probe): `frame` league = 1,010 centred at ≥ 1280 (`left == right ± 2`, `frameEl` must be findable: add `data-frame` on the container), dossier 1,010; `footers` 1 on both; `proseP` ≥ 15px for every serif paragraph > 110 chars (list the exceptions: mono agates are exempt); `docH` change reported; no `h2` without content.

### 7. The dossier's register, waterfall, controls, names — `InstitutionDossierBody.tsx`, `InstitutionHero.tsx`, `components/WaterfallRiskChart.tsx`, `components/dossier/command/primitives.tsx`, `InstitutionDossier.tsx`
- `ConcentrationRegister` `<li>` (`:342`): inline `style={{ maxWidth: 'none' }}` (the Day 5 escape — a `max-w-none` utility loses to the unlayered rule; do not touch `index.css`); the incumbency `<li>`s (`:468`) and category rows the same if the probe flags them. `liMeasure.short` → false.
- `WaterfallRiskChart.tsx:84`: `z=` labels 8.5px → **11px**, `var(--color-text-muted)` at full alpha (shared with the vendor dossier — same floor rule); check the bar labels at 13px still fit at 390.
- `RiskOverTimePanel` (`primitives.tsx:163`): `EditorialAreaChart … decorative ariaLabel={…}` with a sentence built from the peak caption (`Risk over time: peak 27% in 2017, 2002–2025`) — the shared panel, so the vendor/category/sector dossiers lose the same `role=application` stop (report the probe on `/vendors/:id` once: `focusableNonNative` there before/after).
- `ControlGroup` buttons (`:384-405`): `min-h-6 px-2`, `aria-pressed` ✓ stays, ring; the group gets `role="group" aria-label={label}`. `Copy siglas` (`InstitutionHero.tsx:438`): `min-h-6`, ring. Officers `<table>` gets `aria-label`; the `title` on the officer cell (`:305`) dropped (the name is visible and wraps).
- Names: `InstitutionHero` `editorialName` (`:92`) and the lede's `name` (`:555`) → `formatEntityName('institution', institution.name, 'full')` (rule 3; the shared caser keeps `VIH/SIDA`, `IMSS`, `UNAM` and lowercases `Del`); delete the private `toTitleCase` (`:533`). `InstitutionDossier.tsx:46` imports `toTitleCase` for the officer names — keep (officers are people, not entities) but report.
- Accept (probe, EN + ES, 1440/390, both ids): `liMeasure` → every `ul` > 700 has `li ≥ ul − 40`; the register crop at 1440 shows the DotBar/amount/risk columns at the list's right edge (compare `before/dossier-a-en-1440-full.png`); `smallCount` CENSIDA 10 → **0**; `focusableNonNative` 1 → 0; `noFocusStyleAtAll` 9 → 0; `touchSmallReal` 8 → 0; h1 dump = `Centro Nacional para la Prevención y el Control del VIH/SIDA` and `Instituto Mexicano del Seguro Social`; the vendor dossier probe shows no new failure.

### 8. Phones — `InstitutionLeague.tsx`
- Below `lg` the table (`min-w-[900px]` in 334 → a 900px scroller with 10px tiles) renders as **card rows** (Day 7's "mobile rows as links"): one `<li>`/`<article>` per institution — rank · name link (Change 3) · tier tile · `score /100` · weak pillar · money at risk (or `0`) · the expand button → boleta below; the `<table>` stays at `lg+` (same data, same order, same URL state). No `overflow-x-auto` scroller on the ranking at 390; the `role=grid` table gets `role` removed (a plain `<table>` with `<th scope>` is the correct semantics).
- Tier and sector pill rows (`:326`, `:374`): `overflow-x-auto` → `flex-wrap` (two–three rows at 390; the dots make them scannable); at `lg+` they already fit one line (sector 1,561 in 1,136 → wraps to two lines at 1010 — accepted; report the row count at 1440).
- `ChampionCard` at < `sm`: name on its own line, `score · tier` on a second mono line (drop the fixed `w-12`/`w-24` below `sm`); `ActaCard` `line-clamp-2` → none (full names at every width; the 60px numeral stays).
- Dossier tables at 390 (contracts 380 in 300, officers 516 in 334): keep the scrollers, add the Day 2 `← scroll →` hint below `md` (or stack the contracts rows — executor's call, report).
- Accept (probe 390 EN + ES): `innerScrollers` on the league 3 → **0** (dossier ≤ 2 with the hint present in the dump); `truncated` 4 → 0; honor-roll name column ≥ 200px at 390 (add the measure); `tierTiles` 0; the 390 crops of the table cards, the ActaCards and the honor roll read (LOOK); clip census 0 at ≥ 1024 on `/institutions`, `/institutions/251`, `/institutions/1425`, `?tab=reporte` (390: report).

## Out of scope (→ PARALLAX backlog)
- `risk-profile` 16.6s uncached for IMSS (two full walks of the institution's contracts; 1h `_top_cache`) — a precompute like `institution_stats` would make it instant; today it is cached after the first load.
- `EntityIdentityChip` high/critical name inks (`#f59e0b` 2.15 / `#ef4444` 3.76) — shared, Days 5b/7/8 backlog; counts here are small (the register chips are vendors at `sm`).
- `ReportCard` 390 ES: 3 leaves at 6.1/3.8px inside a CSS-scaled svg — the reporte body is a secondary tab; one `useMeasuredWidth` pass when the Day 10 dashboard probe touches the same `BenchmarkRow`.
- `useLeagueField` fires 5 requests to assemble the 476-row field — a `per_page=500` on the scorecards endpoint is a one-line backend cap change (not today).
- `SpectralRegister` keeps a `domainMax = 86` constant — the axis top should follow the data (max score 78 today).
- `InstitutionDossier` fires 9 queries on mount; the largest contracts (4.2s) and the timeline could be deferred below the fold.
- Officers table for IMSS shows 1–12 rows depending on when the query resolves (`officerRows` 1 EN / 12 ES in the probe) — verify the ≥ 50-contract floor and the response cache.

## Risk
- **Change 1 touches the backend and adds a table.** The endpoint must degrade to the live GROUP BY when `institution_category_stats` is absent (tests run on the fixture DB without it); the precompute script must be idempotent; the prod load is Fable's step (dump → `docker exec -i … python -` → `wal_checkpoint(TRUNCATE)`) before the deploy, exactly as Day 8. No scoring column is written. Check `.claude/ACTIVE_WORK.md` — nothing else writes `contracts` or `categories`.
- Change 3 changes what a row *is*: the name link must carry `state={{ institutionName }}` so the dossier's wayfinding label survives; `data-wf-row` stays on the `<tr>` for `useOriginRowFlash`.
- Change 4 moves labels out of the svg: never write state in render (React #301); render the label layer only when `W > 0`; reserve the plate height.
- Change 5 adds `ink` to `TIER_STYLES` — the type `TierStyle` gains a field; every spread (`{ ...TIER_STYLES[key], label }`) keeps working; `ReportCard`/`PillarBoleta` import it.
- Change 8 duplicates the row content in two layouts — keep one `rowModel(item)` helper so the card and the `<tr>` print the same fields from the same code.

## Build notes for the executor
Same stack and rules as `DAY-08-sexenios.md § Build notes`: worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, create branch `parallax/day09-institutions` from `origin/main` (`9fe97379`), servers running (backend `127.0.0.1:8001` = `_parallax_shots/day04/local_backend.py` no-scan — **restart it after the backend change** (kill PID on 8001, relaunch from `backend/` with `DATABASE_PATH="D:/Python/yangwenli/backend/RUBLI_NORMALIZED.db" PYTHONPATH=. python ../_parallax_shots/day04/local_backend.py`), Vite `localhost:3009` on the worktree), ≤ 2 browsers, `MSYS_NO_PATHCONV=1` + `D:/` script paths, never rubli.xyz. Order: **STEP 0 → 6 → 3 → 2 → 5 → 7 → 1 → 4 → 8**. One commit per change: `feat(institutions § PARALLAX D9 § Change N): …`, body cites `docs/parallax/DAY-09-institutions.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Progress file `_parallax_shots/day09/progress.md` (one line after EVERY step, failures included); report `_parallax_shots/day09/report.md` (per change: done, every acceptance number before → after, deviations, noticed-not-fixed → backlog). Probes: extend `_parallax_shots/day09/audit9.mjs` (tag `after`) with the checks named in each Accept (measured callout boxes ⊂ figure, honor-roll name column width, `li` measure, empties by null vs zero); run EN and `LANG_ES=1` at `WIDTHS=1440,1024,390` (+ one 1920 for the frame); census `day04/clipcensus4.mjs` on `/institutions`, `/institutions/251`, `/institutions/1425`, `/institutions?tab=reporte` at 1440/1280/1024/390 EN + ES → 0 at ≥ 1024 (390: report). Crops (`day08/crop8.mjs` → `day09/crop9.mjs`, never widen the viewport for mobile): league masthead + scope, plate (1440 + 390), red flags ×5, honor roll, filters, table rows 1–10 (1440) / cards 1–6 (390), colophon; dossier hero + seals (IMSS, CENSIDA), stat strip, boleta, reading (CENSIDA, with the waterfall), concentration register rows 1–8, «What they buy», largest contracts, officers, provenance — at 1440 and 390, EN + ES. **LOOK at every crop.** Review: `vercel-react-best-practices` on the diff, `rubli-bilingual-audit` on every touched TSX; gates from `frontend/`: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` · `npm run build` · `npm run lint:tokens` · `npx eslint` on touched files · vitest on touched tests; backend: the `-k "institution or scorecard"` pytest after Change 1 and the full suite once at the end with no probes in flight. Do NOT bump BUILD_ID, push, deploy or merge — Fable judges first.

## Result

Built by Opus executor `parallax-day09` (STEP 0 + Changes 6 → 3 → 2 → 5 → 7 → 1 → 4 → 8 + a crop-driven final pass, 11 commits `4d9747c8`…`0c857e1b`), judged by Fable on the `after/` crops (plate 1440 + 390 EN + ES, red flags / honor roll / filters / expanded card at 390, IMSS concentration + record at 1440, CENSIDA reading ES) and on an independent `audit9.mjs` run against the executor's HEAD (`day09/judge-{en,es}.txt`, 1440/390, five routes). One judge round `16193e7f` (one zero wording — "No high- or critical-risk contracts" replaces "0 money at risk" / "MX$0 at risk" / "$0 en riesgo"; the four confidence bands localised in ES; the phone search row wraps so the placeholder is never clipped).

| Measure (league 1440 EN unless noted) | before | after |
|---|---|---|
| STEP 0 | `InstitutionProfile` 2,354 + `InstitutionCompare` 1,214 + `RedThreadPanel` 104 + `NarrativeCard` 65 + `lib/narratives` 379 (unrouted / orphaned) | **−4,116 lines** (`InstitutionScorecards` + its json were already gone on origin/main) |
| the two dead panels (both dossiers, prod included) | `top-categories` **404** ("No contracts found" — the query filtered on a column `contracts` does not have); `risk-waterfall` IMSS **> 280 s** locally (live 3M-row join first) | `top-categories` 200 in 0.10 s / 0.03 s from `institution_category_stats` (46,193 rows · 4,415 institutions; precompute-first, live GROUP BY on the `categories` taxonomy as fallback, empty = `200 {data: []}`); waterfall IMSS 0.017 s (`institution_z_means` first); dossier console errors 3 → **0**; IMSS load 33.7 s → 6.2 s; «What they buy» prints 6 category chips (`EntityIdentityChip type="category"`) on both dossiers EN + ES |
| register `li` measure (D5 backlog) | `ul` 1,086 → `li` **686** ×25 | `li` == `ul` (960/960) |
| empty `—` cells (page 1) | 34 (zeros printed as no-data) | **0** — 9 true zeros print `0` + the sentence; 0 nulls |
| thin samples / trend | #3 with 12 contracts unmarked; `declining` never matched the API's `deteriorating` | `n = 12` + reason on 11 rows of page 1; ▼ renders (1/50 — the API says 49 stable) |
| rows-as-buttons / nested / focusable non-native | 50 `tr[role=button]` / 100 / 50 | **0 / 0 / 0** — name = `EntityIdentityChip variant="name"`, cards = `<Link>` |
| no focus style / targets < 24px | 131 / 52 | **0 / 0** |
| failing contrast (every leaf) | **224** (`minC` 1.40) | **1** = the 40px accent h1 span (4.44, large); `riskAsType` 60 → 0; `dimmed` 52 → 0; `reporte` 17 → 0; dossiers 10/24 → 1 each (the 56px tier score, large) |
| `TIER_STYLES` | `color` used as type | `ink` twin per tier (≥ 4.5 on both papers) for pills, tiles, band labels, boleta; `color` stays on marks |
| sub-10px (league 1440 / 390 · CENSIDA) | 2 / 6 · 10 (`z=` 8.5px) | **0 / 0 · 0** |
| plate callouts | 8 svg `<text>` at 10/9px, names cut at 28 chars, ch-estimate collision, no keyboard path | 6 @ 1440 · 2 @ 390 HTML labels at 11px, **full names** (two lines), measured boxes, 0 overlaps / 0 over svg text / 0 outside / 0 clipped, each a ringed `<a>` (Tab walk 6 / 2) |
| frame / footers / headings | 1,176 (league) · 1,152 (dossier) / 2 / 5 (ACT III headed an empty disclosure) | **1,010** centred (103/103 @ 1440; 880 @ 1024) both / **1** / 4 (colophon carries the footnote) |
| phones (390) | 3 scrollers (pills ×2, the 900px table), 4 clamped names, honor-roll name column 60–70px | **0** scrollers (ranking as cards below `lg`, pills wrap), 0 clamped, name column 268px; dossier 2 scrollers with the hint |
| names | `Vih/Sida`, `Instituto Mexicano Del Seguro Social` (private caser) | `VIH/SIDA`, `del` — `formatEntityName` |
| shared | `RiskOverTimePanel` `svg[role=application]` on every dossier | `decorative` + sentence (vendor dossier `focusableNonNative` 42 → 41) |
| clip census (4 routes × 1440/1280/1024/390 × EN + ES) | — | 0 at ≥ 1024; 390: `reporte` 3 = the shared `BenchmarkRow` (backlog) |
| gates | — | tsc 0 · build OK · lint:tokens PASS · eslint 0 new · bilingual + React review clean · backend **893 passed** (52:50, full suite, no probes in flight) |

Deviations accepted: `state={{ institutionName }}` dropped from the row links (nothing reads it); officer names keep `toTitleCase` (people, not entities); `honorNameMinW` measured on the honor roll's name span; plate callouts capped by what fits (6/8 at 1440, 2/3 at 390) rather than forced; `pillarDeficitInk` lives in `lib/institution-pillars.ts` (react-refresh rule).

Noticed, not fixed (→ backlog): plate leader lines may pass behind another callout's paper (obstacle-aware leaders); `reporte` 390 3 leaves in the shared `BenchmarkRow`; `risk-profile` 16.6 s cold for IMSS; `useLeagueField` 5 requests; `domainMax = 86` constant; 9 dossier queries on mount.
