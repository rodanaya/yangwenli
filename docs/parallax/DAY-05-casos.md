# PARALLAX Day 5 — Casos (`/cases`, `/cases/:slug`)

Routes `/cases` (El Padrón) and `/cases/:slug` (El Expediente). Files: `frontend/src/pages/CaseLibrary.tsx` (657 LOC), `frontend/src/pages/CaseDossier.tsx` (912), `frontend/src/components/cases/{IndexBlocks,DossierBlocks,CasesShared,DispositionBand,CaseTimeline}.tsx`, one line in `frontend/src/components/dossier/primitives.tsx` (`ScaleBlock`). Branch `parallax/day05-casos` off `origin/main` (`adf80ac6`) in worktree `.claude/worktrees/parallax-day01`.

**Classification: GOLD — enhance in place.** El Padrón is the 2026-06-10 DESIGNUS synthesis (impunity masthead, disposition band, three tiers, agate ledger); El Expediente is the 2026-07-07 "El Cargo" synthesis (three acts, docket rail, timeline, cost-in-archive). The argument and the structure are right. What is wrong: a site-wide rule from Day 2c now caps the ledger rows at 68ch and cuts every case name to four letters (a live prod regression since Sep 18), three figures render their glyphs at 4–6px, four inline italics survived the Jul 3 sweep, the dossier sits in a 1,180px box with a 720px column, and 77 controls have no focus style. No `/designus`.

## Audit evidence (2026-09-22, local backend `127.0.0.1:8001` via `_parallax_shots/day04/local_backend.py`, Vite 3009, msedge; never prod)

Probe scripts in `_parallax_shots/day05/`: `audit5.mjs` (sizes, contrast, frame, a11y facts, screenshots; `LANG_ES=1`, `WIDTHS=`), `_rowprobe.mjs` (root-cause check for Change 1). Census: `_parallax_shots/day04/clipcensus4.mjs` (run with `MSYS_NO_PATHCONV=1` and a `D:/…` script path — Git Bash mangles a leading `/cases`). Before-shots and `en-report.json` / `es-report.json` in `_parallax_shots/day05/before/`.

| Measure | index 1440 | dossier 1440 (Línea 12 / Oceanografía) | index 390 | dossier 390 |
|---|---|---|---|---|
| sub-10px text leaves (rendered px, SVG scaled by viewBox) | **41** (all 8.5px: 3 stat captions + 38 `▪GT`) | **28 / 25** (rail labels 8.5, seals 9–10, kickers 9–9.5, timeline labels 8.5, cost-field labels **5.4–5.7**, severity labels 8.5, role tags 8.5) | 41 | **33 / 30** (timeline years **5.6**, timeline labels **3.9**, cost labels 5.3) |
| clip census EN / ES (all 4 widths) | **104 / 102 at 390**, 88+ at 1440 — `▪GT` and names inside `SPAN.min-w-0 truncate`, `Calderón → AMLO` own-clip +32px | 0 / 0 (Línea 12) · **2 / 2** (Oceanografía: `THRESHOLD · 50.0B MXN` escapes the SVG by 36–57px) | same | ES 390 Línea 12: hero sector label `Infraestructura` +16px |
| **root cause of the name cut** (`_rowprobe.mjs`) | `li` = **686px**, header = 1,120px, name column = **45px**; `index.css:1017` `main :where(p, li, dd, dt, figcaption, blockquote) { max-width: 68ch }` catches the `<li>` rows of the AgateLedger (Garamond 14.5px → 686px) | — | same | — |
| live italics (inline `fontStyle`, which the global `.italic` kill-rule cannot reach) | 0 | **4**: CaseCharge finding sentence, CostInArchive caption, CostInArchive multiplier sentence, §V ghost rows | 0 | 4 |
| contrast < 4.5:1 (non-accent) | separators `opacity-50` 2.0:1 (not `aria-hidden`) | SeverityScale labels **1.09:1** (`S2·20`) / 1.4:1 (`S4·5`) — text inside a 0.18-opacity segment; `·` separators 1.7–2.0 | | |
| figure geometry | — | CaseTimeline viewBox 720 (scale 1.0 at 1440, **0.46** at 390); CostInArchive viewBox 640 squeezed to ≈400px beside ScaleBlock (scale 0.63; 0.6 at 390 inside an `overflow-x-auto` scroller with `minWidth: 400`) | | |
| captions | band figcaption spans (100%) | CaseTimeline figcaption **51%** of its figure (`maxWidth: 64ch` inline) | | |
| frame | container 1,176, left-anchored blocks — fine for a register page | container **1,176**; rail 212 + story column **720** inside a 936px cell → ≈250px dead paper right of every section (`en-linea12-1440-full.png`) | 366 | 366 |
| headings | h1 + h2 (lead) + 4×h3 — the two section labels are `<div>`s | **h1 only** — 8 `§` kickers are `<p>` | | |
| controls with no focus style (`focus-visible:` or `focus:` class) | **77** (38 ledger rows, 3 `<summary>`, 4 sort pills, clear/search buttons); search input `focus:outline-none` with a JS border swap on `onFocus` | 8 (rail links, back link, "See full methodology" `<button onClick={navigate}>`) | 77 | 8 |
| targets < 24px | 6 band segments 18px tall, the two smallest 26px wide (7–8px at 390); hint "click a segment to filter" is hidden below `sm` | back link 20px tall, methodology button 18px | | |
| URL state | fraud/admin/status/search in URL (nuqs) ✓ · **sort is `useState`** — El Hilo publishes the sorted order but a refresh loses it | `?` n/a | | |
| search input | `type="text"`, no `name`, no `autoComplete` | — | | |
| number breaks | — | — | masthead `$208.4B MXN` wraps to two lines (`en-index-390-top.png`) | |
| USD in Spanish | 0 | **1** — `ScaleBlock` prints `≈ US$1B` on `/es` (Day 1 backlog item) | | |
| live regions / `div[role=button]` / nested interactive / doc overflow | 0 / 0 / 0 / 0 | 0 / 0 / 0 / 0 | | |

Toolkit passes run IN the audit:
- **web-design-guidelines** (live rule set fetched 2026-09-22): `outline-none` without focus replacement (`CaseLibrary.tsx:381`); interactive rows/pills/summaries without visible focus (`IndexBlocks.tsx:376`, `CaseLibrary.tsx:105`, `:543`); input without `name`/`autocomplete` (`CaseLibrary.tsx:376`); `useState` sort not in the URL (`CaseLibrary.tsx:200`); `<button>` used for navigation (`CaseDossier.tsx:332`); hover state via JS `style` mutation instead of `hover:` (`IndexBlocks.tsx:379–381`); headings not hierarchical (dossier `<p>` kickers, index `<div>` section labels); `truncate` cutting content that is the row's identity (`IndexBlocks.tsx:395`, `:411`, `:427`); decorative `·` separators not `aria-hidden` (`CaseLibrary.tsx:314`, `CasesShared.tsx:112`, `CaseDossier.tsx:212`); numerals for counts ✓ (`43 cases`); loading `…` ✓; `Intl.NumberFormat` ✓ (`formatCompactMXN`); Links are real `<Link>`s ✓.
- **ui-ux-pro-max** `--domain ux` "filter chips active state keyboard focus": *Compact Control Semantics — Critical: a native button with `aria-pressed` matching the visible label, visible focus on every control, never reveal the only action on hover* → sort pills and band get `aria-pressed`; the band's legend becomes the accessible path (the hint is hover copy). `--domain ux` "dense data table row truncation": *truncate only with an expand path; never overflow or cut off* → ledger names wrap in full (they are the row's identity), teasers on the secondary cards keep `line-clamp-2` (exempt by the Sep 18 judge decision: previews that open the full text on click). `--domain chart` "stacked proportion bar small segments": *label each category and count; filled colour alone is insufficient; fallback = percentage list* → the legend row carries the counts and becomes the control. `--domain chart` "dot strip / threshold": *place the number and target text beside the mark; label threshold zones directly* → HTML labels at 11px next to the marks, threshold labelled inside the plate. `--domain chart` "timeline": *direct labels, dashed vs solid carries meaning, table fallback in `aria-label`* ✓ keep the encoding, fix the glyph scale.
- **vercel-react-best-practices**: `client-localstorage-schema` n/a; `rerender` — `data` sort memo ✓; nothing heavier than 43 rows, no virtualisation needed; `rendering-content-visibility` not needed at 43 rows.
- **a11y** (Fable's own pass; the wshobson agents are not exposed as agent types): table rows above.
- **folio voice**: intact. Copy stays. The only wording change is the band hint (Change 4).

## Keep

- El Padrón's argument: "Forty-three case files. One conviction." with the ochre second clause, the three-stat rail, the sources line and the "One conviction in 43 cases. The record is the argument." coda; every number interpolated from `/cases/stats`.
- DispositionBand geometry and order (worst → resolved, lone-conviction ring + "← the lone conviction" gloss), segment colours from `casesVocab`.
- Native `<details>` filter menus with live counts, the URL contract `?fraud=&admin=&status=&q=`, the four sorts (Loss default), El Hilo publication of the displayed order, `useOriginRowFlash`.
- Lead → secondary (2 columns, newspaper gutter) → agate ledger tiers; unfiltered-only front page; the ledger's disposition rail, neutral tabular amounts (no rainbow), single-line rows, chevrons; `line-clamp-2` teasers on the secondary cards.
- Index container 1,180 — a register page, not a reading page.
- El Expediente's three acts (The Charge → The Account → The Implicated), computed `leadFinding()` sentence with accent emphasis, § I–VI sequential numbering, the sticky docket rail (folio, gap, sector spine, COMPRANET rungs, GT tag, § index), sector-tinted accents, drop cap, MarginNotes, legal-status callout, actor list with chip resolution and "no COMPRANET record" tags, vendor rows + ghost rows + COMPRANET footnote, source list, KeepReadingFooter, ProvenanceFooter, WayfindingSpine with `[`/`]`.
- CaseTimeline encoding (contract band in sector ink → discovery tick in critical → dashed open segment to today with the "N years without a conviction" bracket); CostInArchive as an annotated dot field on a √ scale with the threshold rule; SeverityScale as this-case-against-the-archive.
- CLAUDE.md rules 1–8 (chips for every vendor/case/sector outside its own dossier ✓), no dot-grid, no italic, no green for low, Spanish § kickers, MDP / billones in Spanish.

## Change (8)

### 1. Un-cap the ledger rows; names and terms in full — `IndexBlocks.tsx`
- Root cause, not symptom: the Day 2c measure (`index.css:1017`) reaches any `<li>` nobody bounded. The AgateLedger `<li>` gets `className="max-w-none"` (a utility beats the zero-specificity `:where()`); do **not** touch `index.css` — the rule is right for prose. Also the `<ul>` keeps `list-none p-0 m-0`.
- Name cell (`:395`): drop `truncate`; the name wraps if it ever needs to (at 1,120px it will not). Sexenio cell (`:411`): drop `truncate`, column `104px` → `132px` on `md` so `Calderón → AMLO` / `Fox → Calderón` fit; Status cell (`:427`): drop `truncate`. Grid becomes `md:grid-cols-[3px_64px_minmax(0,1fr)_132px_64px_72px_118px_96px_24px]`.
- Row hover: delete the two `onMouseEnter`/`onMouseLeave` style writes; `className` gets `hover:bg-accent/5`.
- Accept (probe, EN+ES, 1440/1280/1024/390): `li[data-wf-row]` width == its section header width (±1px); `truncated` = 0 and `ellipsisNames` = 0 on `/cases`; census `/cases` at all four widths = **0** apart from the four `line-clamp-2` teasers (listed, exempt); every name in the ledger readable in full on the 1440 crop.

### 2. Hero numbers never break; one formatter — `CaseLibrary.tsx`, `IndexBlocks.tsx`
- Delete `formatMXNHero` (its ES branch duplicates `formatCompactMXN`'s MDP/billones output; its EN branch prints a `$` the masthead clips). Masthead "Documented harm" value → `formatCompactMXN(totalLoss)`; all three `MastheadStat` values get `whitespace-nowrap`; the stat rail gets `flex-wrap` so at 390 a stat drops to the next line whole instead of breaking inside.
- Hero surfaces get their USD scale per the CLAUDE.md currency table, as a sub-line rather than inline (the rail cannot hold `208.4B MXN · ≈US$10B` at 390): `MastheadStat` gains an optional `sub?: string` rendered under the caption in the same 10px mono; pass `formatCompactUSD(totalLoss)` prefixed `≈` **only when `lang === 'en'`**. LeadCase amount plate: caption line appends `· ≈US$…` in EN only (`formatCompactUSD`). Spanish stays MXN-only by design.
- Accept: at 390 EN and ES no `MastheadStat` value is taller than 1.3 × its font-size; `$` no longer appears in the masthead; EN masthead shows a `≈US$` sub-line, ES shows none (`usdInEs` = 0 on `/cases` ES); values unchanged in ES (`208,370 MDP`).

### 3. Controls: focus, form, URL sort, menus inside the viewport — `CaseLibrary.tsx`, `IndexBlocks.tsx`
- Every control gets `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-1`: ledger row `<Link>`, lead/secondary `<Link>`s, `<summary>` (add `list-none` already present), menu option buttons, clear buttons, sort pills, "Read the file" link.
- Search input: `type="search"`, `name="case-search"`, `autoComplete="off"`, `spellCheck={false}`; delete the `onFocus`/`onBlur` border writes and the `focus:outline-none`; use `focus-visible:border-accent focus-visible:ring-1 focus-visible:ring-accent` (border colour via class, not JS). Keep the `aria-label`.
- Sort → URL: `const [sortBy, setSortBy] = useQueryState('sort', parseAsStringEnum<SortKey>(['loss','severity','year','gt']).withDefault('loss'))`; sort pills get `aria-pressed={active}` and the group a `role="group" aria-label` (EN "Sort" / ES "Ordenar"). Refresh keeps the order; El Hilo's published list matches.
- Filter menus at 390: the third `<details>` panel (`min-w-[240px]`, `left-0`) can leave the viewport. Panel → `min-w-[220px] max-w-[calc(100vw-2rem)]` and the `<details>` that is last in the row opens `right-0 sm:right-auto sm:left-0` (pass a `align?: 'start' | 'end'` prop; Status gets `end` — or simply give every panel `max-w-[calc(100vw-2rem)]` and probe; the smaller diff that passes wins).
- Decorative `·` separators in the masthead eyebrow and elsewhere on both pages → `aria-hidden="true"`.
- Accept: probe `noFocusStyleAtAll` = 0 on `/cases` (and 0 on the dossier after Change 8); `inputs[0]` = `{type: 'search', name: 'case-search', autocomplete: 'off'}`; `/cases?sort=year` renders Year first and the pill reads `aria-pressed="true"`; a Tab walk (log it) reaches search → 3 menus → clear → sort → lead → secondary → rows in visual order; at 390 every open menu panel's right edge ≤ viewport width; `failingContrast` on `/cases` contains no `·` entries.

### 4. Disposition band: legend is the control, segments are the picture — `DispositionBand.tsx`
- Band height 18 → **24px** (WCAG 2.5.8 floor), gap 1 → 2px; segments become `aria-hidden="true"` `tabIndex={-1}` buttons (pointer affordance stays, `title` stays).
- The figcaption legend items become the accessible controls: each `<button type="button" aria-pressed={active}>` with the dot + label + count, `min-h-6`, focus ring; clicking one toggles the same filter; the active one draws `border-b-2 border-accent`; inactive ones under an active filter dim via `text-text-muted` (not opacity). The `← the lone conviction` gloss stays a `<span>`.
- Hint copy: EN "select an outcome to filter" / ES "elige una resolución para filtrar", shown at every width (drop `hidden sm:inline`), 12px mono muted.
- Accept: `touchSmallReal` on `/cases` = 0; band `role`/`aria-label` unchanged; keyboard: Tab lands on the first legend item, Enter toggles `?status=`, `aria-pressed` follows; the six segment buttons are not in the tab order; 390 crop shows the legend wrapping by whole item.

### 5. 10px floor and no italic, both pages — `CaseLibrary.tsx`, `CaseDossier.tsx`, `IndexBlocks.tsx`, `DossierBlocks.tsx`, `CasesShared.tsx`
- Every `fontSize` below 10 → **10** (mono labels) — sites: `MastheadStat` caption 8.5; `▪GT` 8.5 (`IndexBlocks.tsx:402`); `DispositionSeal` sm 9 → 10 (md stays 10); `RailLabel` 8.5; rail `VERIFIED ✓` 8.5; `CaseCharge` kicker 9.5 and eyebrow 9; `FeatureSection` movement 9.5; actor role tag 8.5 (`CaseDossier.tsx:441`); "no COMPRANET record" 8.5 (`CaseDossier.tsx:411`, `DossierBlocks.tsx:441`); evidence chip 8.5 (`:460`); ghost note 8.5 (`:498`). `letterSpacing` can drop one notch (0.22em → 0.18em) where a label would otherwise wrap; check the rail at 212px.
- Italic → normal: `CaseCharge` finding sentence (`CaseDossier.tsx:176` `fontStyle: 'italic'` → `'normal'`; it stays Garamond 500 with the accent emphasis, which is what carries the voice); `CostInArchive` multiplier sentence and caption; §V ghost-row names. Remove the now-pointless `not-italic` classes.
- Accept: probe `smallCount` = 0 on `/cases` at 1440 and 390 EN+ES; on the two dossiers `smallCount` = 0 **excluding SVG** (SVG handled in Change 6, then 0 overall); `italic` = 0 on all three routes; a computed-style check on `p` elements (not only leaves) finds no `font-style: italic` in `main`.

### 6. Three figures at 1:1 with legible labels — `CaseTimeline.tsx`, `DossierBlocks.tsx` (CostInArchive), `CasesShared.tsx` (SeverityScale), `CaseDossier.tsx` (§ III layout)
Mechanic: **HTML owns glyphs, SVG owns geometry** (Day 4). Measured width via `ResizeObserver` (the `MeshPlano.tsx` pattern), viewBox = measured width so 1 SVG unit = 1px; labels in HTML.
- **CaseTimeline**: `W` = measured width (min 280), `H` 116, pads unchanged; year ticks stay SVG `<text>` at 12px mono (1:1 now); the three caps labels (CONTRACTS / DISCOVERY yyyy / TODAY) and the bracket text become HTML `<span className="absolute pointer-events-none …">` positioned from `x(year)`, mono **10.5px** for caps, Garamond 14px for the bracket, `whitespace-nowrap`, `transform: translateX(-50%)` for centred ones and the TODAY label `left`-anchored; if `x(CURRENT_YEAR) + 6 + labelWidth > W` it flips to `right`-anchored. Figcaption: drop the inline `maxWidth`, 12.5px mono, spans the figure.
- **CostInArchive**: move out of the flex row — § III becomes: row 1 `ScaleBlock` + (`rangeText`), row 2 `<CostInArchive>` full column width (760 at ≥ lg), row 3 amount note, SeverityScale, callout as today. Wrap it in `<figure>` with the caption as `<figcaption>`. Measured-width viewBox (min 280), no `overflow-x-auto`, no `minWidth: 400`. The three labels (THIS CASE · amount, THRESHOLD · amount, LARGEST · amount) become HTML through **`placeLabels` from `@/components/network/plateLabels.ts`** (reuse, do not fork): candidates anchored at `x(v)`, above the axis for THIS CASE and LARGEST, below for THRESHOLD, mono **11px**, `whitespace-nowrap`, bounds = the plate box, so a label at the right edge is re-anchored inside instead of clipped (the Oceanografía 50B threshold). Dots stay SVG; keep the `<title>`s.
- **SeverityScale**: labels leave the band. Below the 16px band render one flex row with the same percentage widths; each cell a mono **10px** `S{lvl} · {n}` in `text-text-secondary`, this case's cell bold in `severityColor(lvl)`; a cell narrower than its label prints only `{n}` (the sentence below already names the level). Band segments keep opacity 0.18 / 0.95 (they are colour, not text).
- Accept (probe, EN+ES, 1440/1024/390): `svgTexts` min rendered ≥ 12px (years only); `svgClip` = 0 on Oceanografía at every width; census 0 on both dossiers; HTML label boxes do not intersect each other and lie inside their plate (the Day 4 `labelAudit` check adapted: callouts = `figure span.absolute.pointer-events-none`); `failingContrast` has no `S*·*` entries; CaseTimeline figcaption ≥ 85% of its figure or one line; CostInArchive plate width == story column width at 1440 (760) and 334 at 390 with no inner horizontal scroller; the 390 crop of § II and § III is readable without zoom.

### 7. Dossier reading frame — `CaseDossier.tsx`, `DossierBlocks.tsx`, `primitives.tsx`
Pattern: `docs/parallax/DAY-02d-methodology-reading-frame.md` (rail 210 + gap 40 + figure 760 = 1,010).
- Page container and `DossierSkeleton`: `max-w-[1180px]` → `max-w-[1010px]`.
- Grid: `lg:grid-cols-[212px_minmax(0,1fr)] lg:gap-8` → `lg:grid-cols-[212px_minmax(0,760px)] lg:gap-10`; story column wrapper `max-w-[720px]` → `max-w-[760px]`. Running prose keeps the global 68ch (≈ 620px at 16px) — figures (timeline, cost field, band) span 760, text stays on the 68ch measure, the Day 2d shape.
- Hero: h1 keeps `24ch`; `CaseCharge` keeps `42ch`/`34ch`. The hero index strip (`folio · VERIFIED` left, sector right) gets `flex-wrap` so ES `Infraestructura` no longer escapes at 390.
- `KeepReadingFooter`, `ProvenanceFooter`, `WayfindingSpine` inherit 1,010 — nothing else to change unless the box-vs-text probe shows > 70px slack on a bordered box (the three keep-reading cards and the docket rail are exempt: fixed-width tiles).
- `ScaleBlock` (`primitives.tsx:206`): the `≈ US$…` line renders only when `lang !== 'es'` (Day 1 backlog item; the primitive already receives `lang`). Verify on `/vendors/:id` and `/institutions/:id` in ES that no `US$` remains and EN is unchanged.
- Accept (1920/1440/1280/1024/390): container 1,010 and centred (left margin == right margin ± 2px) at ≥ 1280; story column 760 at 1440/1920; `usdInEs` = 0 on both dossiers in ES and on one vendor dossier in ES; census ES 390 Línea 12 = 0; no document-level horizontal overflow; `docH` at 1440 does not grow by more than 5% (the narrower column must not balloon the page — if it does, report it).

### 8. Semantics: headings, real links, anchors — `CaseDossier.tsx`, `CasesShared.tsx`, `DossierBlocks.tsx`, `CaseLibrary.tsx`, `IndexBlocks.tsx`
- Dossier: `FeatureSection` title `<p>` → `<h2>` with the exact same classes/inline style (the measure rule exempts `.uppercase`; add `uppercase` to the className since it is currently an inline `textTransform`); `KeepReadingFooter` heading and `ProvenanceFooter` "§ Methodology and provenance" → `<h2>`; `CaseCharge` "ACT I · THE CHARGE" stays `<p>` (it is a movement label, like "The account").
- Index: "More on the front page · N cases" and "The full archive · N cases" `<div>` labels → `<h2>` with the same classes; the lead kicker stays `<p>`; the lead `<h2>` and secondary `<h3>`s stay.
- `section[id]` on the dossier gets `scroll-mt-6` (rail jump links land under the sticky spine).
- `ProvenanceFooter` `<button onClick={() => navigate('/methodology')}>` → `<Link to="/methodology">` with the same classes + focus ring; the back link and rail links get the focus ring; the methodology link ≥ 24px tall (`py-1`).
- `SeverityDots` in the ledger and the hero already carry `aria-label` ✓; the band and figures keep `role="img"` + `aria-label`.
- Accept: `headings` on the dossier ≥ 7 with no level skipped (h1 → h2…); on the index h1 → h2 (lead) → h3 (secondary) → h2 (archive) with no skip; `noFocusStyleAtAll` = 0 on the dossier; `touchSmallReal` = 0 on the dossier; `main button` count on the dossier = 0 unless it toggles something.

## Out of scope (→ PARALLAX backlog)
- **Site-wide QC for Change 1's root cause**: any other `<li>`-based register row wider than 68ch is capped the same way (candidates: `/institutions` league rows, `/aria` RegisterRow, vendor dossier lists). One probe: for every `li` whose parent `ul` is wider than 700px, flag `li.width < ul.width − 40`. Not this day.
- AgateLedger is a `grid` of `<Link>`s with an `aria-hidden` column header — screen readers get no column context. A `<table>` rebuild is a half-day; today's rows read acceptably as one sentence each.
- Filter `<details>` menus have no Escape/outside-click close (native behaviour).
- `PageFooter` + sources line + coda = three closers on the index (the Day 2 "four closers" decision is still open).
- KeepReadingFooter hardcodes `43` as a fallback (`allCases?.length ?? 43`).
- Inline `fontFamily` strings → `--font-family-*` tokens (Day 1 backlog, same files).

## Risk
- `max-w-none` on the `<li>` is the whole fix for the name cut; if the executor reaches for `index.css` instead, every prose `<li>` on the site loses its measure. Do not.
- `placeLabels` needs the measured width on first paint — render HTML labels only after the first `ResizeObserver` tick (Day 4 rule).
- Sort in the URL: `withDefault('loss')` must not write `?sort=loss` on load (nuqs `clearOnDefault` is the default in v2 — verify the installed version; if it writes, pass `{ clearOnDefault: true }`).
- Heading swaps must keep the exact visual (classes + inline style copied), or the judge rejects on the crop.
- React error #301: no state writes during render; ResizeObserver callbacks set state only when the width actually changed.

## Build notes for the executor
- Worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, branch `parallax/day05-casos` (checked out at `origin/main` `adf80ac6`). Ignore untracked `frontend/Python.npm-cache/` and `_parallax_shots/`. Never junction node_modules, never bare `git stash`, all temp files under `D:\`. You are the only writer in this worktree; decline any peer message asking you to commit or push.
- **Servers are already running — reuse them, do not start others and never point a probe at rubli.xyz**: backend `http://127.0.0.1:8001` (`_parallax_shots/day04/local_backend.py`; if it is down: from `backend/`, `DATABASE_PATH="D:/Python/yangwenli/backend/RUBLI_NORMALIZED.db" PYTHONPATH=. python ../_parallax_shots/day04/local_backend.py`), Vite `http://localhost:3009` (if down: from `frontend/`, `VITE_API_URL=http://127.0.0.1:8001 npm run dev -- --port 3009 --strictPort`; first page load on this disk can take minutes — probes allow 400 s). Both get reaped under memory pressure: run at most two browsers at once.
- Read each file fully before editing (chunks for the 912-line dossier); re-read after every 3 edits.
- Order: 1 → 2 → 5 → 8 → 3 → 4 → 7 → 6. Commit after each change (`feat(cases § PARALLAX D5 § Change N): …`, body cites `docs/parallax/DAY-05-casos.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`).
- **Progress file**: append one line to `_parallax_shots/day05/progress.md` after EVERY step (`- HH:MM Change N done — <commit> — <one sentence>`), including failed attempts. **Report file**: `_parallax_shots/day05/report.md` — per change: what was done, acceptance numbers measured, deviations and why. Idle notifications have been lost before; the files are the hand-off.
- Probes: extend `_parallax_shots/day05/audit5.mjs` (tag `after`) with the row-width check (Change 1), the label-box intersection check (Change 6, adapt Day 4's `labelAudit`), the centre-line/column-width check (Change 7), the heading-skip check and the Tab-walk log; run EN and `LANG_ES=1`; run the census with `MSYS_NO_PATHCONV=1 node "D:/…/day04/clipcensus4.mjs" http://localhost:3009 "/cases,/cases/linea-12-metro-collapse,/cases/oceanografia-pemex-fraud" 1440,1280,1024,390` EN and ES → **0 clipped on all 24 lines** (teasers excepted, listed) is a hard gate. Write `_parallax_shots/day05/crop5.mjs` modelled on `day04/crop4.mjs` (hide sticky chrome first) and crop: index masthead+band, filter row, lead, first 12 ledger rows; dossier hero+charge, rail, § II, § III, § IV–V; at 1440 and 390, EN and ES, into `after/`. LOOK at the crops before reporting.
- Review inside the agent: `vercel-react-best-practices` on the diff, `rubli-bilingual-audit` on every touched TSX, then gates from `frontend/`: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` · `npm run build` · `npm run lint:tokens`.
- Do NOT bump BUILD_ID, push or deploy — Fable judges first.
