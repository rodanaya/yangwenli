# PARALLAX Day 7 — Sectores (`/sectors`, `/sectors/:id`)

Routes: `/sectors` (Folio·II «El Arqueo» — WHO view: `ArqueoMesa` Marimekko → §A confound lede → `ConfoundPlate` (default) / `ExposureLedger` (toggle) → `SelfCaptureBand` → `FeDeArqueo`; WHAT view `?view=categories`: `ArqueoMesaCategorias` → `CategoryCaptureDumbbell` → catalog list / `CategoryTreeView` → `FeDeArqueo`) and `/sectors/:id` (`SectorDossier.tsx` + `components/sector/{SectorHero,SectorCommandPanel,SectorReferenceSections}.tsx`). Files: `pages/Sectors.tsx` (1,204), `pages/SectorDossier.tsx` (498), `components/sectors/{ArqueoMesa,ArqueoMesaCategorias,ConfoundPlate,ExposureLedger,SelfCaptureBand,SectorHoverDossier,FeDeArqueo,CategoryCaptureDumbbell,confoundScales}`, `components/sector/*`, one optional prop on the shared `charts/editorial/EditorialSparkline.tsx`. Branch `parallax/day07-sectores` off `origin/main` (`d9893d6a`) in worktree `.claude/worktrees/parallax-day01`.

**Classification: GOLD — enhance in place.** The Jul-4 Marimekko («La Mesa del Arqueo», Σ widths === bandW) and the Jun-24 decision (Plate default, Register a toggle away) stand: the argument (money scale vs house scale), the § order, the copy, the encodings and the two computed annotations per plate stay. What is wrong is mechanical, and it is the same list as Days 4–6: both Marimekkos draw their annotations, flags, ticks and legends at **8–8.5px** and the categories plate truncates its own column names (`Building Constr…`); the capture dumbbell is a 900-unit viewBox scaled by CSS, so its labels render at **4.5px** on a phone (41 leaves — the smallest type measured in the program so far); the plate rows and register rows are clickable `div`s that navigate, the Marimekko columns are `rect role=button` with `outline: none` and no ring, and 12 decorative sparklines are Tab stops; the sector palette is used as **type** on the dossier (energía yellow `#eab308` at 1.82:1 on every § kicker) and in the category tree; both pages sit in 1,152–1,176px boxes with 12.5–13px Garamond clauses. No `/designus`.

## Audit evidence (2026-09-22, local backend `127.0.0.1:8001` via `_parallax_shots/day04/local_backend.py`, Vite 3009, msedge; never prod)

Probe `_parallax_shots/day07/audit7.mjs` (routes `who`, `who-hovermesa`, `who-intensity`, `who-register`, `who-register-hover`, `what`, `what-tree`, `dossier-salud` `/sectors/1`, `dossier-energia` `/sectors/4`; `LANG_ES=1`, `WIDTHS=`, `ROUTES=`). Census `_parallax_shots/day04/clipcensus4.mjs` (`MSYS_NO_PATHCONV=1`, `D:/` script path). Before-shots + `{en,es}-report.json` in `_parallax_shots/day07/before/`; console output `before-{en,es}.txt`; census `before-census-{en,es}.txt`.

| Measure | 1440 EN (ES identical unless noted) | 390 |
|---|---|---|
| sub-10px text leaves | **`who` 20** (1 × 8 + 19 × 8.5): WHO mesa y-ticks `0 25 50 75 100`, `own spend`, `80%`, both annotations (2 lines each) — all SVG at 1:1 (`ArqueoMesa.tsx:304,420,425,479,490,532,543`); readout right label `% of own spend flagged` 8px (`:279`); ConfoundPlate `largest volume —` / `highest saturation ↘` 8.5 (`:419`), `½ own spend` 8 (`:278`), `80%` 8 (`:290`), ruler ticks 8.5, `TRILLIONS MXN` 8; SelfCaptureBand `▲ N ranks` / `of own spend` 8.5 ×6 (`:86,149`) · **`who-register-hover` 22**: + the dossier card's `VaR 1 / 12`, `DA · EU ≤10%`, `Trajectory`, GT seal at 8.5 (`SectorHoverDossier.tsx:91,136,151,225,238`) · **`what` 26**: categories mesa ticks 8.5 (`ArqueoMesaCategorias.tsx:340`), 7 column labels 8.5 **truncated to 16 chars** (`:435,439`), 9 circled ① at 8 (`:465`), `MEDIUM THRESHOLD · 25% (model)` 8 (`:488`), both annotations 8.5 (`:518,541`), legend line 8.5 (`:571`), readout label 8 (`:281`) · dossiers **0** | `who` **19**: MobileMesa spend-share `31.1%…` 8px ×12 (`ArqueoMesa.tsx:641`), `○ % of own spend · flag = ½` 8.5, climbers ×6 · **`what` 58**: the dumbbell's 900-unit viewBox at 334px = scale **0.37** → 41 leaves at **4.5px** and 16 at 4.8px (`CategoryCaptureDumbbell.tsx:385`) |
| contrast < 4.5:1 — **every failing leaf, computed colour** | `who` (19): `·` separators `#736a65` at `opacity .5` → **1.96/2.01** ×4 (`Sectors.tsx:474,476`, PlateFrame) · PlateFrame `Indexed` **2.11** ×2 (shared, `opacity .55`) · ochre `#a06820` `Folio·II·a/b` 11px **4.14** ×2, `Folio·II` 12px 4.44, `highest saturation ↘` 8.5px **4.14** (`ConfoundPlate.tsx:427`), rank-delta chips `10→02` 13px 4.44 ×3 (`SelfCaptureBand.tsx:79`), h1/h2 accent spans 28–40px (large, pass 3:1) · `RISK_COLORS.medium` `#a16207` as 14px 800 readouts `75%`, `66%` **4.36** ×2 (`intensityColor`, `ConfoundPlate.tsx:459`) · **SVG** ochre flag text `rgba(160,104,32,.7)` `½` 12px **2.56**, `own spend` 8.5 **2.56**, `80%` at `.35` **1.55** (`ArqueoMesa.tsx:419–426`) · `what` (91): 72 catalog `·` separators `text-text-muted/70` 1.98 (`Sectors.tsx:854`); header row `# Category Spend Risk DA%` `text-text-muted/50` 13px **3.88** ×5 (`:801`); `the 12 largest categories` `/50` 3.93 (`:681`); `RISK_COLORS.high` `#f59e0b` as 12–13px bold type `Insurance & Bonds 43.8%` **2.04** ×4 (`:187,908` + tree `:131`) · **SVG** categories ticks `currentColor` at `fillOpacity .4` **2.49** ×6 (`:343`), `MEDIUM THRESHOLD` `#f59e0b` **1.90** (`:490`) · `what-tree` (22): sector names in `SECTOR_COLORS` as 13px bold type — Energy `#eab308` **1.82**, Agriculture `#22c55e` 2.16, Environment `#10b981` 2.41, Labor `#f97316` 2.66, Treasury `#16a34a` 3.13, Infrastructure 3.38, Education 3.49, Technology 4.02 (`Sectors.tsx:131`) · `dossier-salud` (55) / `dossier-energia` (**81**): sector palette as type — `§ EL EXPEDIENTE`, `Exposure & scale`, nine `§` section eyebrows `#eab308` **1.82** ×11 (`SectorHero.tsx:93,247`, `SectorDossier.tsx:74`); `·` ×37 at opacity .5 (2.01); `Sector aggregate · 656 institutions` 16px at `opacity .6` **2.46** (`SectorHero.tsx:123`); `high-risk contracts` 13px at `.6` **2.36** (`:184`); legend `7% 5% 16% 72%` at `.7` 2.81 (`:509`); `#ef4444` as type — GT count `759` 12px **3.57** (`SectorDossier.tsx:339`), model-ladder `+0.56…+0.09` 13px 3.57 ×7 (`SectorReferenceSections.tsx:485`); `62% of spend` `#b45309` at `opacity .85` **3.71** (`SectorCommandPanel.tsx:121`); `Open the Watchlist →` `#a06820` 12px 4.44 (`SectorReferenceSections.tsx:705`) | same sets |
| clip census EN / ES, 1440·1280·1024·390 | `/sectors` **0/0** · `?reg=register` 0/0 · `?view=categories` 0/0 at ≥ 1024, **101/101 at 390** (the dumbbell) · `/sectors/1` **5/9** · `/sectors/4` **11/15**: DenseReadout labels `High+crit. exposure` +80px, `Institutions` +12, `Direct award` +12, `Total spend` +2 (ES `Exposición a+c` +32, `Instituciones` +22 …) — `nowrap + ellipsis` at 103px cells (`SectorCommandPanel.tsx:88`); largest-contract titles `truncate` +146…+762px ×7 (`SectorReferenceSections.tsx:811`); ES adds the institution table chips `Comisión Federal…` inside the `overflow` wrapper | `/sectors` ES **1**: MobileMesa readout `pase el cursor por una columna…` +358px in a fixed-height strip (`ArqueoMesa.tsx:581`) · dossiers 7–10 |
| SVG text outside its `<svg>` | 1 on `who` (`100` tick 6px above), 1 on `what` (`50` 7px above) — `overflow: visible` hides it (`ArqueoMesa.tsx:284`) | 0 |
| Marimekko geometry | svg **1,054** in a 1,112 figure; 12 columns, widths 298 · 238 · 192 · 65 · 41 · 36 · 36 · 34 · 33 · 15 · 14 · 10 → **8 narrow (< 48) → 8 circled** + a 13px legend line (fine); categories: 15 columns, 8 narrow, 9 circled, labels truncated | rotated rows |
| `div[role=button]` / `rect|circle[role=button]` / focusable non-native | `who` **12 rects** (`ArqueoMesa.tsx:396`, `outline: none` `:398`, no ring) + **12 `role=listitem tabIndex=0` rows that navigate** (`ConfoundPlate.tsx:323–347`) = 36 focusable non-native (24 + 12 mobile rows in the DOM) · `who-register` **24 div buttons**, **12 nested** (the `sm:hidden` expand `<button>` inside `div[role=button]`, `ExposureLedger.tsx:193,272`) + 12 `svg[role=application]` Tab stops (recharts sparklines) · `what` **39**: 15 mesa rects (`ArqueoMesaCategorias.tsx:384`) + **24 `circle[role=button]`** (`CategoryCaptureDumbbell.tsx:592,608`) | same |
| controls with no focus style | `who` **24** (WHO/WHAT tabs `Sectors.tsx:576,590`, 12 rects, Plate/Register `:1155`, VaR/Intensity `ConfoundPlate.tsx:141`, `view sector ↗` `SelfCaptureBand.tsx:126`, methodology links) · `who-register` **47** · `what` **48** · dossiers **5** (GT button `SectorDossier.tsx:331`, 2 area charts, Watchlist link, methodology `<button>` `:104`) | |
| Tab walk `who` | h1 → WHO → WHAT → 12 rects (no ring) → Plate → Register → VaR → Intensity → 12 listitems (ring) → Bienestar chip → `view sector` (no ring) … ; under `?lens=intensity` the DOM order is still VaR order (`rows.map`, `ConfoundPlate.tsx:309`) so Tab order ≠ visual order · `who-register`: 12 `div[button]` interleaved with 12 unnamed `svg[application]` stops | |
| targets < 24px | `who` 6 (climbers chips `size="xs"` 20px, `view sector` 20px) · `what` **144** (72 vendor + 72 institution chips `size="xs"`, `Sectors.tsx:860,877`) · dossiers 23 (GT button 18px, Watchlist 18px, methodology 18px, table chips 20px) | `who-register` 26 |
| truncation / `…` by code | `who` `Servicio de…` (climbers institution chip xs, `SelfCaptureBand.tsx:113`) · `what` **132 EN / 140 ES**: 7 mesa labels `truncate(name,16)`, `Dowell Schlumberger de…` `truncateLabel(28)` (`CategoryCaptureDumbbell.tsx:631`), every catalog institution `name.slice(0,28)+'…'` (`Sectors.tsx:874`) + xs chips · dossier contract titles ×7–11 | `what` 129/141 |
| headings / kickers | `who` h1 → h2 (mesa) → h2 (confound), then **no heading** for the Register (`<p>` `Sectors.tsx:1140` + `<div aria-label>` `:1128`), the climbers (`SelfCaptureBand.tsx:54`), the Fe de arqueo (`FeDeArqueo.tsx:136`) · `what` h1 → h2 (catalog lede) then `§ THE TABLE` is a `<span>` (`ArqueoMesaCategorias.tsx:260`), `§ The capture` / `§ The Catalog` are `<p>` (`Sectors.tsx:677,733`) · dossiers h1 → 9 h2 ✓, `§ Methodology and provenance` `<p>` (`SectorDossier.tsx:97`) | |
| frame | `/sectors` **1,176** (`max-w-7xl`, 20/20) — deck 16px at **445px** (`maxWidth 58ch`, `Sectors.tsx:553`), §A 15px at 490 ✓, mono finding 12px, plate key 12.5px (`ConfoundPlate.tsx:226`), climbers callout 13px at 424, Fe clauses **12.5px** at 581–661 (`FeDeArqueo.tsx:50`), WHAT lede **14px Inter** at 601 (`Sectors.tsx:655`); text lefts 52 / 64 / 81 / 90 · `/sectors/:id` **1,152** (`max-w-6xl` ×2, `SectorDossier.tsx:116,324`), provenance 13.5px (`:97`) | 366 |
| tables (dossier) | 2 × 6 columns, 1,086 in 1,088 ✓ no scroller | 2 inner scrollers (520 in 334), no hint |
| `aria-label` on role-less `div`/`span` | `who` 2 (hero anchor `Sectors.tsx:508`, Register wrapper `:1128`) · `what` 74 (72 single-bid dot `<span aria-label>` `:919`) | |
| hover z-order | the ½ flag `<line>` and the waterline are painted after the hit rects — a pointer exactly on y=½ hits the line, not the column (Playwright `intercepts pointer events`) (`ArqueoMesa.tsx:416`) | |
| italics / footers / overflow / console / `US$` in ES / `B MXN` in ES | 0 / 1 / 0 / 0 / 0 / 0 ✓ | ✓ |

Toolkit passes run IN the audit:
- **web-design-guidelines** (rule set fetched 2026-09-22): `<div>`/`<rect>`/`<circle>` with click handlers instead of `<a>`/`<Link>` (`ConfoundPlate.tsx:323,553`, `ExposureLedger.tsx:193`, `ArqueoMesa.tsx:396,597`, `ArqueoMesaCategorias.tsx:384,650`, `CategoryCaptureDumbbell.tsx:592,608`); `outline: none` without replacement (`ArqueoMesa.tsx:398`); interactive elements without visible focus (all toggles/tabs/links listed above); `<button>` used for navigation (`SectorDossier.tsx:104`); headings not hierarchical (six kickers with no heading); `aria-label` on non-landmark `div`/`span` (`Sectors.tsx:508,1128,919`); `title`-only intensity dot (`ExposureLedger.tsx:238`); text containers that truncate names (`Sectors.tsx:874`, `CategoryCaptureDumbbell.tsx:631`, `ArqueoMesaCategorias.tsx:439`); labels clipped with `nowrap + ellipsis` (`SectorCommandPanel.tsx:88,158`); `useState` sort/expand not in the URL (`ExposureLedger.tsx:352`, `CategoryTreeView` collapsed set) — accepted, view-local; layout reads in a handler (`ExposureLedger.tsx:363` `offsetTop` — on hover only, fine). ✓ `Intl`/`formatCompactMXN`, `aria-pressed` on every toggle, `tabular-nums`, `?view= ?cview= ?sort= ?lens= ?reg=` in the URL, `prefers-reduced-motion` global.
- **ui-ux-pro-max** `--domain chart` "marimekko mosaic part-to-whole column width labels" → *Part-to-Whole: always label slices directly; do not rely on colour alone; keyboard: focus reveals values* → every column keeps a direct label (full name or circled index + legend), keyboard focus must be visible and reveal the readout. `--domain chart` "dumbbell comparison two values per row" → no verified match (fallback: FT Visual Vocabulary dumbbell — the encoding stays; only the glyph mechanics change). `--domain ux` "compact control semantics native button pressed visible focus" → *Critical: native role, accessible name, state matching the label, visible focus — never a clickable div* → plate rows, register rows, mesa columns. `--domain ux` "dense data table label truncation ellipsis" → *Compact Label Overflow — High: bound only unpredictable values, expose the full text; never clip a label with title-only recovery* → DenseReadout labels wrap, names never truncate. `--domain ux` "svg chart text scaling responsive labels" → *Readable Font Size — High: no tiny text on mobile* → the dumbbell at 1:1. `--domain typography` "editorial serif body text readability size" → *EB Garamond "very legible"* at body sizes; nothing supports 12.5px serif clauses — Day 2d's 15px applies to the Fe de arqueo and the WHAT lede.
- **vercel-react-best-practices**: queries are cached and gated per view ✓; the dumbbell fans out 12 requests at concurrency 3 ✓ (unchanged); ResizeObserver writes guarded in `useMeasuredWidth` ✓; `ConfoundPlate` recomputes `orderIndex` per lens (12 rows, negligible); no virtualisation needed (72 rows).
- **a11y** (Fable's pass; the wshobson agents are not exposed as agent types): rows above.
- **folio voice**: intact. Precedents stay as cited in the files (FT Marimekko, Cleveland dumbbell, Reuters two-annotation discipline, NYT Upshot dot strip). No copy changes except the mobile scroll hint, the "Intensity" header word and the sparkline direction word in the row names.

## Keep

- The Jul-4 `ArqueoMesa`: Σ widths === bandW allocation, fine/dense hatch = flagged / critical pesos, the ½ and 80% ruled flags, the waterline, exactly two computed annotations (widest, tallest), circled legend for narrow columns, hover readout strip, the mobile rotated table. The headline ("The model flags X of Y…") and the 13.5px plate caption.
- `ArqueoMesaCategorias`: top-14 + remainder, mean-risk height, the dashed 25% rule at `RISK_THRESHOLDS.medium`, tallest + big-and-hot annotations, the † direct-award footnote.
- §A confound lede: the two-questions h2 with the computed leader/saturation names, the "How many pesos? / How saturated?" deck, the mono finding line.
- **The Plate as the default view of the Register** (user decision 2026-06-24), the Plate/Register toggle, the shared `?lens=` (VaR / Intensity) with the FLIP reorder, `OwnSpendTrack`, the ½ + 80% flags, filled dot (log VaR) vs hollow ring (own-spend share), the two named outliers on the plate, the hover readout of the VaR value.
- `ExposureLedger`: one clean line per sector, `DABullet` vs the EU line, VaR big number, intensity dot + score, trajectory sparkline + direction glyph, the floating `SectorDossierCard` that edge-flips, the mobile tap-to-expand inline dossier, the ∑ double rule, the model note, the legend microline, the two † marginalia.
- `SelfCaptureBand`: computed rank-delta picks, `OwnSpendTrack` reuse, chip + separate "view sector" link (no nested anchors), the 22px readout.
- `FeDeArqueo` clauses and the methodology link. `CategoryCaptureDumbbell`'s data-driven domain, the 38px "14%" anchor stat, the "concentration ceiling", the honesty sentence, the rich tooltip. Catalog list / tree, the sort chips, `?sort=` `?cview=`.
- Dossier: hero verdict seal (HR%), lede with drop cap + exposure companion, DenseReadout anchors, the 2×2 diagnostic grid, every reference section and its order, `EntityIdentityChip fullName` in tables, `WayfindingSpine`.
- CLAUDE.md rules 1–8 (chips for vendors/institutions/categories/cases ✓, `getRiskLevelFromScore` ✓, no green, "risk indicator" ✓, MDP in ES ✓, § kickers ✓), no dot-grid, no italic, the vivid sector/risk palette on every **mark** (strips, hatch, rules, dots, rings, bars, bands).

## STEP 0 — dead code (its own commit, before Change 1)

Delete `frontend/src/components/sectors/EditorialSectorStoryCharts.tsx` (124 lines, imported by nothing) and the three components only it imports: `RiskSpendBeeswarm.tsx` (813), `CompetitionSlopeChart.tsx` (564), `CategorySectorSwimlane.tsx` (585) — 2,086 lines. `git grep` each name first (direct imports, string literals, lazy imports, tests, `_dev/ChartCatalog.tsx`); the only other mention is a comment in `ArqueoMesa.tsx:17` ("like CategorySectorSwimlane") — reword it. `SectorTreemap.tsx` stays (`pages/explore/*`). Fix the stale header comment of `Sectors.tsx:1–9` (it still describes "Lede strip + CumulativeRibbon + ExposureLedger table … no beeswarm"). `pages/InstitutionProfile.tsx` (unrouted since M5, imports `CategoryCaptureDumbbell`) is Day 9's STEP 0 — do not touch it, but it must still type-check after Change 5 (the dumbbell's props are unchanged). Gate: tsc + build. Commit `refactor(sectors § PARALLAX D7 § STEP 0): remove the unrouted editorial story charts (beeswarm, slope, swimlane)`.

## Change (8)

### 1. Reading frame + prose register — `Sectors.tsx`, `SectorDossier.tsx`, `FeDeArqueo.tsx`, `ConfoundPlate.tsx`, `SelfCaptureBand.tsx`, `ExposureLedger.tsx`, `SectorCommandPanel.tsx`
Pattern `docs/parallax/DAY-02d-methodology-reading-frame.md` as applied on Day 6: centred **1,010** container, **640** text column, plates and registers at the full 1,010 (this page is plate-led; the Marimekko's 12 columns want every pixel — at 946 the labelled set is unchanged: Treasury 65 → 58px still clears the label threshold).
- `Sectors.tsx:457,563` `max-w-7xl` → `max-w-[1010px]` (keep `mx-auto px-4 sm:px-6 lg:px-8`). `SectorDossier.tsx:116,324` `max-w-6xl` → `max-w-[1010px]`.
- Text column 640 on `lg`: deck (`:553` `maxWidth: '58ch'` → drop; wrap in `lg:max-w-[640px]`, keep 16px), §A section (`<section>` at `:1014` — add `lg:max-w-[640px]` to the h2's parent block, not to the section: the h2 at 28px reads well at 640), the mono finding line 12 → **13px**, the WHAT lede (`:655` `text-sm text-text-secondary` Inter → `style` EB Garamond **15px** `lineHeight 1.55` `lg:max-w-[640px]`), the climbers callout (`SelfCaptureBand.tsx:171` 13 → **15px**, `lg:max-w-[640px]`), the Fe de arqueo clauses (`FeDeArqueo.tsx:50` 12.5 → **15px**, the `<ol>` gets `lg:max-w-[640px]`), the dossier provenance paragraph (`SectorDossier.tsx:97` 13.5 → 15, `maxWidth: '72ch'` → the 640 column).
- Asides stay Garamond but never below **13.5px**: ConfoundPlate key sentence (`:226` 12.5 → 13.5), ExposureLedger marginalia (`MARGIN_NOTE_STYLE` 13 → 13.5). Mono microlines stay 12–13px (site register).
- DenseReadout labels never clip: `SectorCommandPanel.tsx:88` drop `whiteSpace: nowrap / overflow: hidden / textOverflow: ellipsis` → `lineHeight: 1.25` (labels wrap in their cell) and the grid `minmax(132px, 1fr)` → `minmax(150px, 1fr)` (`:76`); `FullBarRow` label (`:158`) same (wrap, no ellipsis).
- Accept (probe `frame`, `proseP`, `readout`, census; 1920/1440/1280/1024/390, EN + ES): both pages' container is 1,010 and centred in `main` (left == right ± 2) at ≥ 1280; on `/sectors` every Garamond `proseP` longer than 110 chars has `fs ≥ 15` except the two 13.5 asides (list them) and the mono lines have `fs ≥ 12`; `readout` on `/sectors/1` and `/sectors/4` has `clipped: false` for all ten DenseReadout labels at 1440, 1024 and 390, EN and ES; census own-clip on those labels = 0; the two dossier tables still measure `w == wrapW` at 1440 and 1024; `docH` on `who` at 1440 grows ≤ 12 % (report).

### 2. Both Marimekkos: 11px floor, HTML-owned annotations, no truncated names, visible focus — `ArqueoMesa.tsx`, `ArqueoMesaCategorias.tsx`
The plates are already width-measured (1 svg unit = 1 px), so glyph size is a number, not a scale. Mechanic per Day 4/6: SVG keeps the geometry; the two annotations and the legend become HTML.
- SVG text ≥ **11px**: WHO y-ticks (`:304`) 8.5 → 11 `fill var(--color-text-muted)`; `½` 12 → 13 (Garamond 700) and `own spend` (`:420`) 8.5 → 11, `80%` (`:425`) 8.5 → 11 — text ink `var(--color-accent-hover)` (the lines keep `OCHRE_STRONG` / `OCHRE_FAINT`); categories ticks (`:340`) 8.5 → 11 `fill var(--color-text-muted)` **without** `fillOpacity .4` (`:343`); `MEDIUM THRESHOLD · 25% (model)` (`:488`) 8 → 11, `fill RISK_TEXT_COLORS.high` (the dashed rule keeps `RISK_COLORS.high`); circled ticks (`:465`) 8 → 13 (WHO already 13); readout right labels (`ArqueoMesa.tsx:279`, `ArqueoMesaCategorias.tsx:281`) 8 → 11; categories legend (`:571`) 8.5 → 13 and its `height: LEGEND_H` → `minHeight` (it wraps); † footnote stays 13.
- Annotations → HTML: replace `AnnotationWidest`/`AnnotationTallest` (`:449–555`) and the categories `tallest`/`bigAndHot` `<text>`s (`:503–555`) with absolutely positioned `<div aria-hidden>` mono **11px** two-line labels inside a `position: relative` wrapper over the svg, `pointer-events: none`, `left` clamped so the box stays within `[0, width]` (`clampX`: measure the box with `measureLabel` from `components/network/plateLabels.ts` after `useFontsReady`, or a `ref` + `offsetWidth` on first paint — executor's call, report which); the SVG keeps only the leader `<line>`s. If the two boxes of a plate collide (WHO: widest = Health, tallest = Treasury are 5 columns apart today — they will not; categories: `tallest` vs `bigAndHot` may share a column), seat them with `placeLabels` (obstacles = the tick column) and drop the second rather than overprint. Reserve `TOP_PAD = 28` inside the svg height so the `100` / `50` tick and the leader lines no longer escape the box; the svg loses `overflow: visible` (`:284`).
- Column names never truncate: `ArqueoMesaCategorias.tsx:439` `truncate(name, 16)` → show the full name at **11px** only when its measured width + 6 ≤ column width (`measureLabel` / canvas `measureText` in the plate's mono face), else the circled index (`MIN_LABEL_W` goes; the decision is per name). WHO keeps 13px uppercase names with the same measured rule instead of `NARROW_LABEL_THRESHOLD = 48` (Treasury at 946 must still be labelled — report the labelled set at 1440 and 1024). MobileMesa (categories `:706` `truncate(name, 22)`) → full name, wrapping.
- Keyboard focus is visible: drop `outline: 'none'` (`ArqueoMesa.tsx:398`); when `hoverId`/`hoveredKey` is set (mouse **or focus** — `onFocus` already sets it) draw a `<rect>` outline `stroke var(--color-accent)` `strokeWidth 2` `fill none` `pointer-events none` around that column band, rendered after the columns. Flag lines, waterline and annotation leaders get `pointerEvents="none"` so the ½ line never steals the hover (`:416,424,430`).
- MobileMesa readout (`:581`): `height: READOUT_H` → `minHeight`, no `nowrap` (fixes the ES 390 clip); spend-share label (`:641`) 8 → 11. Mobile rows (`:597` `div role=button`, categories `:650`) → the whole row is a `<Link to=/sectors/:id aria-label={ariaLabel} className="block …">` (categories: remainder row stays a plain `div`); same layout, focus ring `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent`.
- Accept (probe, EN + ES, 1440/1024/390): `svgTextSizes` on `who` and `what` has no key < 11; `svgClip` **0** and `svgOutsidePlate` 0; `svgTextContrast` **0**; `ellipsisNames` on `what` contains **no** category name (7 → 0) and `truncated` 0; `mesa`: `cols == labelled + circled` and for every labelled column `text.getBBox().width + 6 ≤ strip width` (add this check); `who@390` ES census 0; a crop of the WHO plate after Tab → Tab → Tab (third column focused) shows the accent outline; hovering the plate at y = ½ still fills the readout (`readout[0].txt` names a sector in `who-hovermesa`); `divButtons` on `who@390` 0 (mobile rows are links).

### 3. ConfoundPlate + climbers + hover dossier: 11px floor, real links, inks for type — `ConfoundPlate.tsx`, `SelfCaptureBand.tsx`, `SectorHoverDossier.tsx`, `ExposureLedger.tsx` (helper)
- Sizes → **11px**: `½ own spend` / `80%` header flags (`:278,290`), the two named-outlier callouts (`:419,427`), ruler ticks (`:503`) and `TRILLIONS MXN` (`:512`, drop `opacity .7`), mobile `○ % of own spend · flag = ½` (`:542`); `SelfCaptureBand.tsx:86,149` `▲ N ranks` / `of own spend`; `SectorHoverDossier.tsx:91,136,151,225,238`. The `†` sup (`ExposureLedger.tsx:224`) 8 → 10 (a superscript glyph, aria-hidden).
- Inks: `highest saturation ↘` `--color-accent` → `--color-accent-hover` (`:427`); rank-delta hot chips `var(--color-accent)` → `var(--color-accent-hover)` (`SelfCaptureBand.tsx:79`); the % readouts use the risk palette as TYPE (`ringColor` from `intensityColor`: plate `:459`, mobile `:585`, climbers `:141`, dossier card `:105`) → add `export function intensityTextColor(score)` beside `intensityColor` in `ExposureLedger.tsx` returning `RISK_TEXT_COLORS[level]` (low → `var(--color-text-muted)`) and use it for every numeral; rings/dots keep `intensityColor`. Note: `RISK_TEXT_COLORS.medium` `#a16207` measures **4.36:1** on the paper — report the number after the change (site-wide constant → backlog, not a Day 7 fix).
- Rows are not buttons: desktop rows keep `role="listitem"` + the FLIP `transform`, lose `tabIndex`, `aria-label`, `onClick`, `onKeyDown`, `cursor-pointer` (`:323–347`); the sector name (`:353`) becomes `<Link to={`/sectors/${r.sectorId}`} aria-label={aria}>` with the exact same inline style (Garamond 500 15px, sector rule, underline on hover/focus-within) plus `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/60 rounded-sm`; the row's hover/dim state comes from `onMouseEnter` on the row and `onFocus`/`onBlur` on the link. Render the rows from `ordered.map` (not `rows.map`, `:309`) so DOM order == lens order and the Tab order follows the sort (keyed elements keep their nodes, the transform still transitions). Mobile rows (`:553`, name `:574`) same: `div role=listitem`, name → `<Link>`. **Rule-1 note**: the registry's sector names are `<Link>`s, not `EntityIdentityChip`s, on purpose — the plate's Garamond name column is the loved centrepiece and a sans chip would recast it; same reading as Day 4's rail buttons. Recorded for the panel.
- Climbers: institution chip `size="xs"` → `size="sm" fullName` (`SelfCaptureBand.tsx:113`, fixes `Servicio de…`); `view sector ↗` link gets the focus ring + `py-1` (`:126`).
- Accept (probe, EN + ES, 1440/390, `who`, `who-intensity`, `who-register-hover`): `smallCount` **0** on all three (the hover card `hoverDossier.sub10` 0); `focusableNonNative` on `who` lists only the 12 mesa rects (0 listitems); `nestedInteractive` 0; Tab walk on `who`: after `Intensity ↓`, twelve `A+ring` sector names in **VaR order**, then per climber `A+ring` chip → `A+ring view sector`; Tab walk on `who-intensity` (add it): the twelve names come in **intensity order** (Treasury first); `ellipsisNames` on `who` 0; `failingContrast` on `who` at 1440 = only the PlateFrame `Folio·II·a/b` / `Indexed` leaves (shared, backlog), the ≥ 24px accent spans and the `RISK_TEXT_COLORS.medium` readouts at 4.36 — nothing at `#a06820` ≤ 13px, nothing at `#a16207`… (report).

### 4. The Audited Register: real links, no nested controls, decorative sparklines, ringed sort — `ExposureLedger.tsx`, `charts/editorial/EditorialSparkline.tsx` (shared, one optional prop)
- `LedgerRowItem` root (`:193`) `div role=button tabIndex onClick onKeyDown` → plain `div` (hover dossier stays on the wrapper's `onMouseEnter`/`onFocusCapture`); the name span (`:218`) → `<Link to aria-label={ariaLabel + ' · ' + direction word}>` with the same style + ring; the mobile expand `<button>` (`:272`) stays a sibling, `min-h-6 min-w-6` + ring, `aria-controls` the inline dossier `id`; the `title`-only intensity cell (`:238`) → `aria-label` on the span (`role="img"`).
- `EditorialSparkline`: add `decorative?: boolean` (default false) → when true, `accessibilityLayer={false}` on `LineChart`/`AreaChart` and `aria-hidden="true"` on the wrapper. Pass `decorative` from the register rows (`:260`) and `SectorHoverDossier.tsx:156` (the direction word now lives in the row's accessible name). Other callers (`EntityProfileDrawer`, `Administrations`, `_dev/ChartCatalog`) unchanged — screenshot `/administrations`'s sparkline row before/after, same pixels.
- Sort headers (`:370`): `min-h-6 px-1 rounded-sm` + ring; label `Intens.` → `Intensity` / `Intensidad` (`:404`) and both the header and the row cell `w-[64px]` → `w-[92px]` (`:246`).
- Accept: `divButtons` **0** and `nestedInteractive` **0** on `who-register` (1440 + 390); `focusableNonNative` on `who-register` = the 12 mesa rects only (no `svg[application]`); Tab walk `who-register`: after `Intensity`, twelve `A+ring` names with **no** `svg` stop between them; Enter on the first name navigates to `/sectors/1`; at 390 the twelve toggles are ≥ 24 × 24, `aria-expanded` flips and the inline dossier (with `id`) appears; `/administrations` sparkline crop unchanged; `hoverDossier` still appears on hover at 1440 (`w 330`).

### 5. CategoryCaptureDumbbell at 1:1 with HTML glyphs — `CategoryCaptureDumbbell.tsx` (props unchanged: `{ categories }`)
Mechanic Day 5/6: measured width, 1 unit = 1 px, HTML owns every glyph.
- `useMeasuredWidth` (`@/hooks/useMeasuredWidth`) on the container; render the rows only when `W > 0`. Each row is an HTML grid: `[minmax(140px,190px)_minmax(0,1fr)_96px]` at ≥ `sm`, stacked (`[1fr]`, label above the track) below; the middle cell holds a **1:1** `<svg width={trackW} height={ROW_H}>` (`viewBox 0 0 trackW ROW_H`) with the connector + the two circles (`aria-hidden`, no `role`, no `tabIndex`, no handlers); the category label is the existing `EntityIdentityChip` (no `foreignObject`); the #1 vendor name is an `EntityIdentityChip type="vendor" size="sm" hideIcon fullName` positioned absolutely in the track cell at `left: x1 + 11` (right-anchored when `x1 > 0.6 · trackW`), so it is a real link and never truncates (`truncateLabel` and `:631` go); the numerics `14% / 4%` are HTML mono 13px with `SECTOR_TEXT_COLORS` on the #1 figure; the axis header (`0% … 20%`, gridlines), the ceiling label and the `#1 / #2` legend become HTML rows (11–13px) above/below the row stack, the gridlines an absolutely positioned `div` layer. Drop the SVG pull-quote on the leader row (the 38px anchor stat + sentence already carry it — report). Zebra via a CSS class.
- #2 vendor stays reachable: an `sr-only` span in the row (`#2 Caf Mexico · 4.3%`) and the tooltip, which now also opens on `onFocusCapture` of the row (keyboard) and closes on blur.
- Accept (probe, EN + ES, 1440/1024/390): `what@390` `smallCount` **0** (58 → 0) and `what@1440` `svgTextSizes` has no key < 11; `svgScale` on `what` lists no viewBox-scaled svg (scale 1.00 or none); `divButtons` on `what` = the 15 mesa rects only (24 circles gone); `ellipsisNames` on `what` contains no vendor name; Tab walk on `what` (extend to 60 stops) reaches 12 category chips and 12 vendor chips with rings, and Tab onto a row opens its tooltip; crops at 1440 and 390 show the dumbbells at 1:1 with readable vendor names (LOOK — names must not overprint the numerics; if `x1 + 11 + name width` crosses the numerics column, flip the label left of the dot); `InstitutionProfile.tsx` still type-checks; census `?view=categories` at 390 **0**.

### 6. The catalog list + tree — `Sectors.tsx`
- Header row `text-text-muted/50` → `text-text-muted` (`:801`); `the 12 largest categories` `/50` → `text-text-muted` (`:681`); the top-vendor/institution wrapper `text-text-muted/70` → `text-text-muted` (`:854`); `·` separators `aria-hidden="true"` (`:866`, `:474,476` masthead too).
- Names: `cat.top_institution.name.slice(0,28)+'…'` (`:874`) → the plain name with `fullName`; vendor + institution chips `size="xs"` → `size="sm"` (`:860,877`; the tree keeps its category chips `sm` ✓).
- Type inks: tree sector names `style={{ color }}` (`:131`) → `getSectorTextColor(sectorCode)` (the 3px rule keeps `color`); category risk numerals `RISK_COLORS[riskLevel]` (`:187,908`) → `RISK_TEXT_COLORS[riskLevel]` (the bar `:901` keeps `RISK_COLORS`); `bg-zinc-400` (`:151,823`) → `bg-text-muted` (a mark, but off the raw palette).
- Controls: WHO/WHAT tabs (`:576,590`), Plate/Register (`:1155`), List/Tree (`:731`), sort chips (`:702` + `min-h-6`), tree headers (`:121`), the error `Retry` (`:956`) — `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-1`; single-bid dot `<span aria-label>` (`:919`) → `role="img"`.
- Accept (probe, EN + ES, 1440/390, `what`, `what-tree`): `failingContrast` = only the PlateFrame ochre/`Indexed` leaves and ≥ 24px accent spans (list); `sectorPaletteFails` **0**, `riskPaletteFails` **0**; `ellipsisNames` **0** on both routes in both languages; `touchSmallReal` on `what` = 0 (report chip heights); `ariaLabelNoRole` 0 on `what`; `noFocusStyleAtAll` on `what`/`what-tree` = the 15 mesa rects only.

### 7. Dossier inks + semantics — `SectorDossier.tsx`, `SectorHero.tsx`, `SectorCommandPanel.tsx`, `SectorReferenceSections.tsx`
- Sector palette as type → `getSectorTextColor(sector.code)`: `DossierSectionHeader` eyebrow (`SectorDossier.tsx:74` — pass `accentText`), hero § kicker (`SectorHero.tsx:93`), `Exposure & scale` eyebrow (`:247`); `sectorAccent` stays on the 6px band, rules, bars and the drop cap (decorative, already `aria-hidden`).
- `#ef4444` as type → `RISK_TEXT_COLORS.critical`: GT count (`SectorDossier.tsx:339`), model-ladder numerals (`SectorReferenceSections.tsx:485` — split `color` into `barColor` = `RISK_COLORS.critical` / `textColor` = `RISK_TEXT_COLORS.critical`).
- Opacity on text: `SectorHero.tsx:123,184` `opacity: 0.6` → drop; `:509` `opacity: 0.7` → drop; `SectorCommandPanel.tsx:121` `opacity: c.color ? 0.85 : 1` → drop; `·` separators `aria-hidden="true"` (`SectorHero.tsx:145,147,418,563`, `SectorReferenceSections.tsx` case roll + composition + queue ×n) keeping their `opacity .5`.
- `Open the Watchlist →` `--color-accent` 12px → `--color-accent-hover` (`SectorReferenceSections.tsx:705`) + ring + `py-1`; `See full methodology ↗` `<button onClick={navigate}>` (`SectorDossier.tsx:99–107`) → `<Link to="/methodology">` same classes + `py-1` + ring; the GT-linkage `<button>` (`:331`) → `<a href={`#${gtTarget}`}>` (sections already `scroll-mt-20`) with `py-1` + ring, rendered only when `gtTarget` exists; `ProvenanceFooter` kicker `<p>` (`:97` … the `§ Methodology and provenance` line) → `<h2>` same classes.
- No clipped text: `SectorLargestContracts` title `truncate` (`SectorReferenceSections.tsx:811`) → `break-words` (wraps; it is one 13.5px line of description, two lines at worst); `SectorSexenioStrip` admin name `truncate` (`:360`) → `break-words`; the two tables keep `overflow-x-auto` (Day 6 precedent) and gain a mono 12px `← scroll →` / `← desliza →` hint `lg:hidden aria-hidden` under each; `DensePanel` labels are one line already.
- Accept (probe + census, EN + ES, 1440/1024/390, `/sectors/1`, `/sectors/4`): `sectorPaletteFails` **0**, `riskPaletteFails` **0**, `opacityDimmedCount` **0** (aria-hidden separators are excluded by the probe), `failingContrast` **0** on both dossiers (report every leaf if not); `navButtons` 0; `hSeq` = h1 → h2 × 10, `headingSkips` 0; `truncated` **0** at 1440 and 1024, and at 390 only the two table wrappers appear in `innerScrollers` with the hint visible in the crop; census `/sectors/1` + `/sectors/4` EN + ES **0** at 1440/1280/1024 (the 390 lines may list the scroller chips — report them; they are inner scrollers, not clips); `touchSmallReal` on both dossiers 0 (chips excluded; report chip heights); the Salud hero crop shows the § kicker in the darker red twin, the Energía hero in `#854d0e`.

### 8. Semantics + controls on `/sectors` — `Sectors.tsx`, `ArqueoMesaCategorias.tsx`, `SelfCaptureBand.tsx`, `FeDeArqueo.tsx`, `ConfoundPlate.tsx`
- Kickers with no heading of their own → `<h2>` with identical classes/inline style: `§ The Count Register · two views, one order` (`Sectors.tsx:1140`), `§ The climbers of the count` (`SelfCaptureBand.tsx:54`), `AUDITOR'S NOTE · METHOD AND LIMITS` (`FeDeArqueo.tsx:136`), `§ The capture …` (`Sectors.tsx:677`), `§ The Catalog · N categories` (`:733`), `§ THE TABLE · BY CATEGORY` (`ArqueoMesaCategorias.tsx:260`, span → h2). Kickers that already precede an h2 (`§ THE COUNTING TABLE`, `FINDING · THE CONFOUND`, `Hallazgo · El Catálogo`) stay `<p>`.
- `<div aria-label="The Count Register">` (`:1128`) → `<section aria-labelledby="registro-heading">` (the new h2 gets the id); the hero anchor `<div aria-label>` (`:508`) → `role="group"`; ConfoundPlate `role="group"` sort control ✓ stays.
- Every control on the page has a ring (the list in Change 6 + `VaR ↓`/`Intensity ↓` `ConfoundPlate.tsx:141`, the Fe methodology link, the register's methodology link, `view sector ↗`).
- Accept (probe, EN + ES, 1440, `who`, `what`, `who-register`): `kickers` lists only the three `P:` kickers that precede an h2; `headings` on `who` ≥ 6 (h1 + 5 h2), on `what` ≥ 5; `headingSkips` 0; `ariaLabelNoRole` **0** on `who` and `what`; `noFocusStyleAtAll` on `who` = 12 (mesa rects), on `what` = 15, on `who-register` = 12 — everything else ringed; `outlineNoneNoRing` 0 everywhere.

## Out of scope (→ PARALLAX backlog)
- `PlateFrame` (shared, Day 1): `Indexed 2026·09·22` stamp at `opacity .55` (2.11:1) and the `Folio·N` label in ochre 11px (4.14) — apply the Day 6b accent-hover decision once, inside the primitive, for every plate on the site.
- `RISK_TEXT_COLORS.medium` `#a16207` measures 4.36:1 on the paper as 13–14px bold type (the constants comment says "already AA-safe") — one darker step in `constants.ts`, site-wide.
- `pages/InstitutionProfile.tsx` (2,312 lines, unrouted since M5 2026-05-18) → Day 9 STEP 0.
- Sector names on the plate/register as `<Link>` rather than `EntityIdentityChip` (rule 1) — panel question for Day 7b.
- Marimekko tail columns 10–15px wide are 10–15px hit targets; a wider hit area would overlap neighbours — a keyboard-only list fallback (the legend as links) is the honest fix.
- Ledger/table mobile card layouts (Day 2/5/6 precedent kept the scroller + hint).
- `ExposureLedger` sort/expand and `CategoryTreeView` collapsed set are `useState` (not in the URL).
- Inline `"EB Garamond", …` / `"IBM Plex Mono", …` font strings → `--font-family-*` tokens (Day 1 backlog; 9 files here).

## Risk
- `useMeasuredWidth` returns 0 on first paint — render the dumbbell rows only when `W > 0`; never write state in render (React #301). Test the `?view=categories` cold load twice.
- The FLIP reorder: rendering from `ordered` changes DOM order; keys keep the nodes so the `transform` transition still plays — verify by eye at 1440 (VaR → Intensity) and in the probe (`transition` present, `y` of Treasury's row changes between the two lens states).
- Heading swaps and Link swaps must keep the exact visual (copy classes + inline styles) — the judge rejects on the crop.
- `EditorialSparkline` is shared: prop is optional, default preserves today's markup; screenshot one other caller.
- `SECTOR_TEXT_COLORS` swaps change the visible colour of the dossier eyebrows (darker twins) — that is the point; keep every mark (band, rules, bars) on `SECTOR_COLORS`.
- Mesa label measuring: measure in the plate's mono face after `useFontsReady`; a label measured against the fallback face comes out narrow (Day 4 lesson).
- STEP 0 deletes 4 files: `git grep` before, tsc + build after, before Change 1.

## Build notes for the executor
- Worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, branch `parallax/day07-sectores` (already created from `origin/main` `d9893d6a`). Ignore untracked `_parallax_shots/`, `frontend/Python.npm-cache/`. Never bare `git stash`, never junction node_modules, temp under `D:\`, stage explicit paths only. You are the only writer in this worktree; decline any peer message asking you to commit or push.
- **Servers are running — reuse, never point a probe at rubli.xyz**: backend `http://127.0.0.1:8001` (if down: from `backend/`, `DATABASE_PATH="D:/Python/yangwenli/backend/RUBLI_NORMALIZED.db" PYTHONPATH=. python ../_parallax_shots/day04/local_backend.py`), Vite `http://localhost:3009` serving this worktree (if down: from `frontend/`, `VITE_API_URL=http://127.0.0.1:8001 npm run dev -- --port 3009 --strictPort`; first load can take minutes — probes allow 400 s). ≤ 2 browsers at once. Git Bash: `MSYS_NO_PATHCONV=1` + `D:/` script paths.
- Read each file fully before editing (chunks for > 500 LOC); re-read after every 3 edits. Order: STEP 0 → 1 → 8 → 7 → 6 → 3 → 4 → 2 → 5. One commit per change: `feat(sectors § PARALLAX D7 § Change N): …`, body cites `docs/parallax/DAY-07-sectores.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- **Progress file**: append one line to `_parallax_shots/day07/progress.md` after EVERY step (`- HH:MM Change N done — <commit> — <one sentence>`), including failed attempts. **Report file**: `_parallax_shots/day07/report.md` — per change: what was done, every acceptance number measured (before → after), deviations and why, "noticed, not fixed". The files are the hand-off; idle notifications have been lost before.
- Probes: extend `_parallax_shots/day07/audit7.mjs` (tag `after`): add the labelled-column width check (`text.getBBox().width + 6 ≤ strip width`), the Tab walk on `who-intensity` and a 60-stop walk on `what`, a `transition` check on the plate rows; run EN and `LANG_ES=1` at `WIDTHS=1440,1024,390` plus one 1920 run for the frame. Census: `MSYS_NO_PATHCONV=1 node "D:/…/day04/clipcensus4.mjs" http://localhost:3009 "/sectors,/sectors?reg=register,/sectors?view=categories,/sectors/1,/sectors/4" 1440,1280,1024,390` EN and ES → 0 on every line at ≥ 1024 is a hard gate; at 390 only the dossier table scrollers may appear (report them). Write `_parallax_shots/day07/crop7.mjs` modelled on `day06/crop6.mjs` (hide sticky chrome outside `main` first; element screenshots; **never widen the viewport for a mobile crop**) and crop at 1440 and 390, EN and ES, into `after/`: masthead, WHO mesa, confound lede, plate (VaR), plate (`?lens=intensity`), plate with the third column focused, register (`?reg=register`) + hover card, climbers, Fe; WHAT mesa, dumbbell, catalog rows 1–15, tree; `/sectors/1` hero + stat strip, diagnostic grid, vendors table, cases, provenance; `/sectors/4` hero, signals. **LOOK at every crop before reporting** — read the sentences and the label placements, not just the layout.
- Review inside the agent: `vercel-react-best-practices` on the diff, `rubli-bilingual-audit` on every touched TSX, then gates from `frontend/`: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` · `npm run build` · `npm run lint:tokens` · `npx eslint` on the touched files.
- Do NOT bump BUILD_ID, push, deploy or merge — Fable judges first.

## Result

Built by Opus executor `parallax-day07` (STEP 0 + Changes 1→8→7→6→3→4→2→5 + four review/crop follow-ups), judged by Fable on the `after/` crops at 1440 + 390, EN + ES, and on re-runs of `audit7.mjs` + the census against the executor's HEAD (`judge`, `judge3`); one judge round of seven fixes (`6ed810c2`, `3ed55984`), re-verified on `judge2/` crops.

| Measure | before | after (`3ed55984`) |
|---|---|---|
| sub-10px text leaves (1440 / 390) | `who` 20 / 19 · register hover 22 · `what` 26 / **58** (dumbbell at 4.5px) | **0 / 0 on every route**, EN + ES |
| smallest SVG glyph | 4.5px | 11px; `svgClip` 0, `svgOutsidePlate` 0, `svgTextContrast` 0 |
| Marimekko names | 7 category names truncated (`Building Constr…`), labels overprinting | 0 truncated: measured names or circled index + 13px legend; Treasury circled (55px column), categories 1 labelled + 14 circled at 946 (backlog) |
| clickable non-native (`div`/`rect`/`circle` buttons, focusable listitems) | `who` 24, register 36, `what` 39 | 12 / 12 / 15 (the mesa columns, now with a visible focus outline); 0 at 390 — rows and names are `<Link>`s, Tab order follows the lens |
| nested interactive / no focus style / targets < 24px | 12 / 47 / 144 (`what`) | 0 / 0 / 0 |
| sector palette as type | energía dossier 10, tree 8 | 0 (`getSectorTextColor`) |
| failing contrast, dossiers | 55 / 81 | 1 / 1 (shared `WayfindingSpine` `— / —`) |
| failing contrast, `/sectors` at 1440 | 19 (incl. ochre 8.5px callout, `#f59e0b` 12px type, `/50` headers) | 12 = PlateFrame ×6 (shared) + `#a16207` 4.36 ×2 (constants) + accent spans ≥ 28px ×4; `what` 8 = the same + 2 chip high-risk names (shared chip) |
| headings / skips | `who` 3, `what` 2, dossiers 9 | 6 / 6 / 10, skips 0; `aria-label` on role-less elements 76 → 0 |
| clip census, 5 routes × 4 widths × EN/ES | dossiers 5–15 per width, `?view=categories` 101 at 390 | **0 / 40** |
| frame | 1,176 / 1,152 | 1,010 centred on both pages (103/103 at 1440, 343/343 at 1920); prose 15px; DenseReadout labels 0 clipped |
| values breaking across lines (judge) | mobile plate amount, dumbbell numerics at 390, stat-strip anchors EN + ES | 0 (`nowrap`, anchors span two tracks) |
| `docH` `who@1440` | 3,129 | 3,363 (+7.5 %) |

Commits: `742cf432` STEP 0 · `7ee44348` §1 · `e00ae614` §8 · `c56a87d8` §7 · `7990c9b5` §6 · `319fdfe9` §3 · `7d2bcceb` §4 · `5ec5a3c7` §2 · `5055b92e` §5 · `e82aaa4f` `34c103a6` `bc3b1b4f` `864b3194` `2677d36f` review/crop follow-ups · `6ed810c2` `3ed55984` judge fixes (hashes as built on `d9893d6a`; rebased onto `23d6ae23` for the ship). Gates: tsc 0 · build OK (39 s) · lint:tokens PASS · eslint clean on touched files (3 pre-existing warnings) · bilingual audit clean · `EditorialSparkline` other caller byte-identical (md5 05a59513).

Deviations accepted: the ConfoundPlate FLIP is replayed with WAAPI (a node React moves loses its CSS transition; reduced-motion respected); Treasury falls to the circled legend on the WHO plate and 14 of 15 category columns are circled — the measured rule replaces a 48px threshold that let a 66px name sit in a 65px column; the categories run now starts at the tick gutter; mobile mesa rows are 24px links; the `MEDIUM THRESHOLD` label uses accent-hover (`RISK_TEXT_COLORS.high` measured 4.45 on the plate tint); `intensityTextColor` lives in `confoundScales.ts` (react-refresh); the catalog list scrolls at 680px on phones with the numeric columns tightened.

Noticed, not fixed (→ PARALLAX backlog): PlateFrame stamp/folio inks; `RISK_TEXT_COLORS.medium` 4.36; chip high-risk names; WayfindingSpine `— / —`; `InstitutionProfile.tsx`; two-line Marimekko labels; Link-vs-chip on the plate; DenseReadout grid holes; tail-column hit targets; desktop hover readout ellipsis at 1024 ES; catalog mobile card layout; view-local `useState`.
