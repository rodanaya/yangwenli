# PARALLAX Day 8 — Sexenios (`/administrations`)

Route: `/administrations` (Folio·XI «Five administrations, one pattern» — ACT I `SeamStrip` handover chart → ACT II the file per administration: tabs + `AdminSummaryCard` (fingerprint, `ProcurementGradeCard`, mini sparkline) → § I yearly record (`EditorialSparkline` ×3 + § VOLUMEN bars) → § II `AdminSectorDeviation` → § III `ExpedienteSpine` case file → § IV `AdminVendorBreakdown` + top-100 `AdminVendorsDeepList` → § V `AdminBuyersSection` → ACT III `AdminSurvivorsSlope` «Los Sobrevivientes» → «The itinerants» `OfficialTenureBands` → `ComparePeriodView` (collapsed) → `PageFooter`). `?admin=` selects the file. Files: `pages/Administrations.tsx` (1,106), `components/administrations/{SeamStrip 317, AdminSummaryCard 441, ProcurementGradeCard 120, ExpedienteSpine 241, AdminVendorsDeepList 356, AdminBuyersSection 344, AdminSurvivorsSlope 482, OfficialTenureBands 340, AdminSectorDeviation 142, ComparePeriodView 389, data.ts}`, `charts/AdminVendorBreakdown.tsx` (106). Route → component verified in `App.tsx:63,250`. `AdminCycleSmallMultiples.tsx` (373) is imported by nothing. Branch `parallax/day08-sexenios` off `origin/main` (`4c6acd24`) in worktree `.claude/worktrees/parallax-day01`.

**Classification: GOLD — enhance in place.** The three-act argument (the pattern → the file → the ones who remain) and «Los Sobrevivientes» (Jul 3, loved) stand: the seam chart with its handover cards, the per-administration file with its § I–V order, the slope's ochre/grey connector semantics, the itinerants ledger. What is wrong is the Days 4–7 list again, plus one honesty defect: **the page keeps its own calendar.** `components/administrations/data.ts` starts AMLO in 2018 and Sheinbaum in 2024 (`start`/`dataStart`), while the canonical `lib/administrations.ts` and the backend `api/administrations.py` (which feeds `/analysis/admin-breakdown`) put AMLO at 2019–2024 and Sheinbaum from 2025 — the same convention the sector dossier's sexenio strip prints. So the file's label says "2018–24", its yearly record runs "2018 → 2023", and the fingerprint numbers come from an era the page does not name. No `/designus`.

## Audit evidence (2026-09-23, local backend `127.0.0.1:8001` no-scan, Vite 3009 on the worktree at `4c6acd24`; never prod)

Probe `_parallax_shots/day08/audit8.mjs` (copy of `day07b/audit7b.mjs`; routes `admin` `/administrations`, `admin-fox`, `admin-sheinbaum`; `LANG_ES=1`, `WIDTHS=`), before-runs `before-{en,es}.txt` + `before/en-report.json`; shots `before/shots8.mjs` → `before/{en,es}-{1440,390}-full.png` + bands `before/bands/b00…b08.png` (1440) and `m{en,es}00…08.png` (390).

| Measure | 1440 EN (ES identical unless noted) | 390 |
|---|---|---|
| sub-10px text leaves | **37** (`admin`), 32 (`fox`), 29 (`sheinbaum`): `SeamStrip` svg ticks `0% 10% 20%` + years at **8px** (`:259,:267`) and `Natl. avg 10.9%` **7.5px** (`:274`) in a 960-unit viewBox scaled by CSS (1.13 at 1440); `ProcurementGradeCard` `PROCUREMENT GRADE` **8px** (`:66`), `Direct Award / High-Risk Rate / Single Bid` **7px** + `truncate` → "DIREC… HIGH-… SINGL…" (`:99`); § VOLUMEN counts `190,177…` **8.5px** (`Administrations.tsx:788`); `ExpedienteSpine` `CRIT`/`HIGH` chips **8px** (`:186`); `AdminSurvivorsSlope` amounts `11.8B MXN` **9.5px** (`:370`), non-survivor names 10px (`:382`), `†` 7px (`:363`), legend `<p>` **9px** (`:419`); `·` tspans 7px | **29** + the tab sub-labels `PAN · 2002–06` **8.5px** (`AdminSummaryCard.tsx:233` `text-[8.5px] sm:text-[13px]`); the seam svg at 0.67 scale → ticks at **5.3px** ×9 and 5px ×1 |
| contrast < 4.5:1 — every failing leaf, computed | **124** (`minC` 1.92): `#ef4444` (`RISK_COLORS.critical`) as 13px type ×**84** — seam-card deltas `9.0%→9.5% ▲+0.5pp`, § I `76.2%→82.2%`, `▲ +13.5`, the top-100 ledger's `100% high risk` numerals (3.76:1); chip names `#f59e0b` ×9 2.15 (shared `EntityIdentityChip` `RISK_COLOR_CLASS`, backlog); `#fb923c` 2.26 / `#f87171` 2.77 as type — `ExpedienteSpine` year labels `2020 2021 2022 2023` 12px and the `CRIT`/`HIGH` chip text; party blue `#3b82f6` `2019` 3.68 (`ExpedienteSpine`); grade letter `C` in `#eab308` **1.92** (`ProcurementGradeCard`); `#736a65` at opacity → `·` ×4 2.01, seam-card labels `PAN→PAN†` 3.17 ×4; ochre `#a06820` 12px `Folio·XI`, `Act I`, `Act III` 4.44 and 15px accent numerals `3`, `2`, `18 of 24` 4.44; **svg** text 20: slope non-survivor names `#736a65` at opacity 2.04 (`AdminSurvivorsSlope.tsx:382`) | same sets |
| `riskPaletteFails` / `sectorPaletteFails` / opacity-dimmed text | 98 / 3 (`fox` 11: party colours as type) / **24** (16 `·` + slope names) | same |
| CSS-scaled svg (`svgScale`) | 3: seam 960 → 1083 (1.13, 10 texts), summary mini-sparkline 220 → 273 (1.24, 3 texts), slope 1040 → 1083 (1.04, 40 texts) | seam 960 → 640 (**0.67**), mini-sparkline 1.31 |
| svg text clipped / outside | `11.5%` clipped 3px (summary mini-sparkline label, `AdminSummaryCard`) | `2018`, `2024`, `2025` outside (seam) |
| inner scrollers | 1: the seam strip's handover-card row `sw 1108 > 1083` — the 4th card `MOR→MOR†` is cut at the right edge (band `b00`) | 1: seam wrapper `min-w 640` in a 297px column → a 640px horizontal scroller with 5px ticks |
| `div[role=button]` / focusable non-native / no focus style | **24** (`OfficialTenureBands.tsx:209` rows) / **27** (24 rows + 3 recharts `svg[role=application]` — the § I `EditorialSparkline`s) / **55** (admin tabs ×5 + admin buttons ×5, `Share`, `Context` 16×16, `Value/Risk/Contracts` sort, `Open X's file` ×5, `institutions/value/direct award`, `View in Cases`, `4 documented scandals`) | 26 / 54 |
| targets < 24px | **113**: the top-100 ledger's vendor chips `size="xs"` 869×**20** ×100, `Context` 16×16, … | 9 |
| truncation / `…` by code | `truncated` 3 (grade labels) · `ellipsisNames` **17**: slope names `formatVendorName(name, 19/20)` in `<text>` (`AdminSurvivorsSlope.tsx:361,:385`) — "Coahuila Industrial…", "Halliburton de…", "Grupo Farmacos…" | `truncated` **41**: tabs `PAN · 2002–06` ×5 + `Peña Nieto`, `Sheinbaum` (`AdminSummaryCard.tsx:227,233` `truncate`), grade labels, institution names in § V, `Abelardo Ignacio Garri…` (`OfficialTenureBands.tsx:225` `truncate`) |
| headings | h1 → h2 (admin name, `AdminSummaryCard:268`) → h3 § I–V → h4 (top sectors) → h2 (itinerants); `ACT I/II/III` kickers are `<p>`; `hSkip` 0 | |
| frame / footers / prose | **1,176** (`max-w-screen-xl`, `Administrations.tsx:439`); **2 footers** (`PageFooter` `:1049` + shell colophon); card bio 13.5px serif, deck 16px at ~470px | 366 |
| calendar | labels `PAN · 2002–06 · 2006–12 · PRI · 2012–18 · MORENA · 2018–24 · 2024–25`; AMLO file "2018–2024", § I "2018 → 2023", § VOLUMEN bars 2018…2023 (`data.ts:12-16` `start 2018 / dataStart 2018`, Sheinbaum `2024`); backend eras `amlo 2019–2024`, `sheinbaum 2025–2030` (`api/administrations.py:26-34` → `analysis.py:4362 _ADMIN_ERAS`); `lib/administrations.ts:29-34` the same; sector dossier strip `2019–24 / 2025–` | |
| copy | "1 contracts" (`AdminVendorsDeepList.tsx:292`, § IV top vendors); `Natl. avg 10.9%` printed on the line it names; `PAN` band label printed over the series at 2002; itinerants header `CHANGE OF GOVERNMENT — PEÑA→AMLO` overprints `AMLO→SHEINBAUM` at 390 (`OfficialTenureBands.tsx:155-165` absolute labels); `INST.` header; 2025 partial marked only on the seam card (`†`) — not on Sheinbaum's § I / § VOLUMEN | |
| italics / console / `US$` in ES | 0 / 0 / 0 ✓ (`usdEs` 2 in EN by design) | ✓ |

Toolkit passes run IN the audit:
- **web-design-guidelines** (rule set fetched 2026-09-23): `<div role=button>` rows instead of `<button>` (`OfficialTenureBands.tsx:209`); interactive elements without visible focus (the 55 above); text containers truncating names (`AdminSurvivorsSlope.tsx:361,:385`, `AdminSummaryCard.tsx:227,233`, `OfficialTenureBands.tsx:225`, `ProcurementGradeCard.tsx:99`); `title`-only content ×6; headings not reflecting the three acts (kickers as `<p>`); `useState` sort not in the URL (`AdminVendorsDeepList`, `OfficialTenureBands` — accepted, view-local); `?admin=` written by a hand-rolled `useSearchParams` setter (`Administrations.tsx:176-195`); `tabular-nums` ✓; `role=tablist`/`tab`/`aria-selected` ✓ (`AdminSummaryCard.tsx:196-216`) but no arrow-key handler and no `aria-controls`/`tabpanel`; `Intl` ✓; `prefers-reduced-motion` global ✓.
- **ui-ux-pro-max** `--domain chart` "slopegraph two column ranked labels" → no verified match (fallback: Tufte slopegraph — direct labels on both columns, never truncated; the encoding stays). `--domain ux` "table row name truncation" → *Interactive chips need a native role, accessible name, state and visible focus; never a clickable div or a hover-only action*; *tables overflow on mobile → horizontal scroll or card layout* (the seam strip's 640px scroller is the wrong one — the chart must draw at 1:1 in the column). "expandable row disclosure" → *interactive elements need accessible names*.
- **vercel-react-best-practices**: queries gated per admin ✓; the top-100 list is a 560px `overflow-y-auto` list of 100 rows (no virtualisation needed); `ResizeObserver` absent — the three svgs scale by CSS (the Day 4–7 defect class).
- **a11y** (Fable's pass): rows above; `liveRegions` 0 (the admin switch re-renders the whole file with no announcement — the tab semantics cover it once `aria-controls` exists).
- **folio voice**: intact — Folio·XI, three acts, § kickers, "the file answers", Fe-style footnotes. No copy change except the calendar labels, the plural, and the 2025 partial note.

## Keep

- The three-act spine and every kicker: ACT I «The pattern» seam chart with party bands, handover cards (`PAN→PRI 9.0%→9.5% ▲+0.5pp`) and the "2 of 4 handovers with no risk drop" sentence with its † Structure-A footnote; ACT II «The file» tabs (five presidents, party, years, HR%) and the per-file § I–V order; ACT III «The ones who remain» — the slope's columns, ochre = survives a change of party / grey = same party, the crossing connectors, the † partial-roster marker, the phone card list; «The itinerants» ledger with the change-of-government rules and the 2018+ window.
- `AdminSummaryCard`: portrait, party chip, the "highest high-risk rate in the modern record — 1.1× the national average" verdict lede, the fingerprint (contracts · total value · MXN at risk · DA · SB · HR with deltas vs 2002–2025), the grade tile's *idea* (letter grades against EU lines), the mini "high-risk % by year" sparkline, the scandal dots.
- § I's three auto-scaled panels with the honesty line "each panel auto-scales — compare the shape within a metric, not across them"; § VOLUMEN bars with the peak in ochre; § II's deviation bars against the term average with `↑ 6pp`; § III's two-column timeline with REFORM/CRIT/HIGH chips and "View in Cases"; § IV `DotBar` top-6 + top-sectors + the top-100 ledger (chips, sparklines, share); § V buyers with siglas + FED marker + concentration line.
- `EntityIdentityChip` for every vendor/institution (rule 1 ✓), `formatCompactMXN` in cells, MDP in ES, `DotBar` (no inline dot strips), the inflation note, the COMPRANET/SHCP provenance line.
- CLAUDE.md rules 1–8, no dot-grid, no italic, the vivid party/risk palette on every **mark** (bands, bars, dots, connectors).

## STEP 0 — dead code (its own commit, before Change 1)

Delete `components/administrations/AdminCycleSmallMultiples.tsx` (373 lines, imported by nothing — `git grep` first for direct imports, lazy imports, string literals, tests, `_dev/ChartCatalog.tsx`). Fix the stale header comment of `Administrations.tsx:12-18` (it lists "§ VI El Ciclo del Sexenio", which no longer renders). Gate: tsc + build. Commit `refactor(administrations § PARALLAX D8 § STEP 0): remove the unrouted cycle small-multiples`.

## Change (8)

### 1. One calendar — `components/administrations/data.ts`, `pages/Administrations.tsx`, `AdminSummaryCard.tsx`, `SeamStrip.tsx` (labels only)
- `data.ts` derives its term years from `lib/administrations.ts` (`yearStart`/`yearEnd`; `dataStart` = `max(yearStart, 2002)`): Fox 2002–2006, Calderón 2007–2012, Peña Nieto 2013–2018, AMLO 2019–2024, Sheinbaum 2025–. Every printed range follows: tabs `PAN · 2002–06 · 2007–12 · PRI · 2013–18 · MORENA · 2019–24 · 2025–`, the file header `2019–2024`, § I `2019 → 2024`, § VOLUMEN bars 2019…2024 (six bars), § kickers `— AMLO · 2019–2024`, the slope column heads `2019–2024` / `2025–`, the ES twins. The seam handovers keep their true dates (`:406-407` 2018.92, 2024.75) and the party bands on the seam chart follow the same year boundaries.
- Trace which endpoint feeds each number (`/analysis/admin-breakdown` eras for the fingerprint and § IV/§ V; the yearly series for § I/§ VOLUMEN sliced client-side) and make the client slices use the same years, so **the fingerprint's CONTRACTS equals the sum of the § VOLUMEN bars** for every administration. If any endpoint slices by its own years, report it — do not fork a second mapping.
- Sheinbaum: a `2025*` on her § I range and § VOLUMEN bar + the shared `PARTIAL_YEAR_NOTE` (from `lib/constants.ts`, Day 7b) under § VOLUMEN; the seam card keeps its `†`.
- Accept (probe, EN + ES, 1440): for each of the 5 files, `fingerprintContracts === Σ volumeBarCounts` (add the check); dumps contain no `2018–24`, `2024–25`, `2018 →`; `who`-style dump of `/sectors/1` sexenio strip and this page agree on `2019–24`/`2025–`; `2025*` + the partial note present on the Sheinbaum file only.

### 2. Reading frame, one footer, headings — `Administrations.tsx`, `AdminSummaryCard.tsx`
- `max-w-screen-xl` (`:439`) → `max-w-[1010px]` (keep `mx-auto px-4 sm:px-6`); the deck and the "3 of 5 administrations…" sentence in a 640 column at 16/15px; the card bio (`AdminSummaryCard`) 13.5 → **15px** serif in a 640 column; the seam's "2 of 4 handovers" sentence stays 19px, its † footnote 13px mono ✓; the inflation/provenance notes 13.5px.
- One footer: `<PageFooter />` (`:1049`) → the same colophon text as a `<p>` inside `main` (Day 5/6 pattern), `footers` 2 → 1.
- `ACT I · THE PATTERN`, `ACT II · THE FILE`, `ACT III · THE ONES WHO REMAIN` `<p>` kickers → `<h2>` with identical classes; the admin name stays `h2` (a sibling under Act II), § I–V stay `h3`, `2018+ WINDOW · ACROSS ADMINISTRATIONS` `<p>` → `h2`?? — no: «The itinerants» already has its `h2` (`OfficialTenureBands:107`); leave its kicker a `<p>`. `hSkip` stays 0; `headings` 9 → 12.
- Accept: `frame` 1,010 centred at ≥ 1280 (left == right ± 2), 928 at 1024; `footers` 1; `hSeq` starts `h1, h2 (Act I), h2 (Act II), h2 (admin), h3…`; `proseP` ≥ 15px for every serif paragraph > 110 chars; `docH` growth ≤ 12% (report).

### 3. The seam chart at 1:1 — `SeamStrip.tsx`
- `useMeasuredWidth` (`@/hooks/useMeasuredWidth`) on the plate; svg `width={W} height={H}` with `viewBox 0 0 W H` (1 unit = 1px; the geometry constants become functions of `W`); render when `W > 0` with the height reserved. Drop the 640px `min-w` wrapper: at 390 the chart draws in the 358px column — thin the year axis to the handover years + 2002/2025, keep the five bands.
- Text ≥ **11px**: ticks and years (`:259,:267`) → 11px `fill var(--color-text-muted)`; `Natl. avg 10.9%` (`:274`) → an 11px HTML label on plate paper (`background: var(--color-background-elevated)`, 2px padding) seated **above** the dashed line at its left end, never on it; the party band labels `PAN PRI MOR` (12px) sit on the band's baseline strip below the series (a `y` below the plot's minimum), never over the line — at 2002 `PAN` currently overprints the series (band `b00`).
- Handover cards: four cards fit the row at 1440 (the row is 1,108 in 1,083 — reduce card padding or let the row wrap to two rows of two at < 1200) — `innerScrollers` 0 at every width; at 390 they stack (already).
- `role="img"` + `aria-label` ✓ keep; the hidden `md:hidden` caption stays.
- Accept (probe + a collision check copied from `day07b/dv7b.mjs`, EN + ES, 1440/1024/390): `svgScale` lists no seam entry (or scale 1.00); `svgTextSizes` on the seam ≥ 11; `svgOutsidePlate` 0 at 390; `innerScrollers` **0** at 1440 and 390; `Natl. avg` label × series/line hits 0; band labels × series 0; crops at 1440 and 390 EN + ES read (LOOK: the line, the bands, the labels, four cards).

### 4. The file card: tabs, grade tile, fingerprint inks — `AdminSummaryCard.tsx`, `ProcurementGradeCard.tsx`
- Tabs: `text-[8.5px] sm:text-[13px]` (`:233`) → **11px** floor at every width; names and sub-labels never truncate (`:227,233` `truncate` → wrap; the tab row may take two lines at 390 or scroll horizontally with the Day 6 hint — executor's call, report); arrow-key navigation (`←`/`→`, Home/End) between tabs + `aria-controls` on each tab and `role="tabpanel"` + `aria-labelledby` on the file; every tab and every `Open X's file` button gets `focus-visible:ring-2 focus-visible:ring-accent`; the mini "high-risk % by year" sparkline at 1:1 (`useMeasuredWidth`, 220-unit viewBox gone) with its `11.5%` / `2018` / `2023` labels as HTML on paper, no clip (`svgClip` 0), labels never overprint each other.
- Grade tile: `PROCUREMENT GRADE` 8px → 11px; `Direct Award / High-Risk Rate / Single Bid` 7px + `truncate` → **11px**, two lines allowed, never truncated; the letter grades' ink: `#eab308` (`C`, 1.92) and the other letters → `RISK_TEXT_COLORS` by grade (A/B → `text-muted`… executor maps the five letters onto `RISK_TEXT_COLORS` low/medium/high/critical and reports the ratios); the tile's fill tints stay.
- Fingerprint deltas and the `HIGH RISK %` value in `#ef4444` 13px (3.76) → `RISK_TEXT_COLORS.critical`; `▲`/`↗` glyphs keep their ink but as part of the same text run.
- Accept (probe, EN + ES, 1440/390): `truncated` on the tabs 0 (41 → the § V institution names only, which Change 5 fixes); `smallCount` in the card 0; Tab walk: `←`/`→` moves between the five tabs and Enter/Space selects; `aria-controls` resolves to the `tabpanel`; `svgClip` 0; the card crop at 390 EN + ES read.

### 5. The file's § I–V: inks, sizes, chips, plural, rings — `Administrations.tsx`, `ExpedienteSpine.tsx`, `AdminVendorsDeepList.tsx`, `AdminBuyersSection.tsx`, `charts/AdminVendorBreakdown.tsx`
- § I: the three `EditorialSparkline`s pass `decorative` (Day 7 prop; `svg[role=application]` 3 → 0; each panel's headline + range text already carry the reading); panel values in `#ef4444` (`76.2%→82.2%`, `▲ +13.5`) → `RISK_TEXT_COLORS.critical`; § VOLUMEN counts 8.5 → 11px `whitespace-nowrap`.
- § III `ExpedienteSpine`: year labels in `#fb923c`/`#f87171`/`#3b82f6` (2.26–3.68) → `RISK_TEXT_COLORS` by impact (party blue for reforms → `--color-text-secondary`); `CRIT`/`HIGH`/`REFORM` chips 8px → 11px, chip text ink `RISK_TEXT_COLORS`, the tint fill stays; `View in Cases` links ringed.
- § IV: top-6 "1 contracts" → a plural helper (`1 contract` / `1 contrato`, `n contracts` / `n contratos`) applied everywhere the page prints counts; the top-100 ledger's vendor chips `size="xs"` → `size="sm" fullName` (targets 20 → 24px, `touchSmallReal` 113 → ≤ 1), its `100% high risk` numerals (`#ef4444` ×~80) → `RISK_TEXT_COLORS.critical` (the sparkline stroke keeps the party hex); `Value / Risk / Contracts` sort buttons ringed (`aria-pressed` ✓ stays); the download icon button gets an `aria-label` if it lacks one.
- § V: institution names never truncate at 390 (the rows already wrap at 1440 — check the `truncate` on the phone branch); `FED` marker and siglas stay.
- `·` separators `aria-hidden` (16); `Context` button 16×16 → `min-h-6 min-w-6` + ring + label.
- Accept (probe, EN + ES, 1440/390, all three files): `failingContrast` = the 9 shared chip names + PlateFrame-class shared leaves only (list every remaining leaf with its ratio); `riskPaletteFails` 98 → 0 outside the shared chip; `focusableNonNative` = the 24 tenure rows only until Change 7, then 0; `touchSmallReal` 0 (report chip heights); dumps contain no `1 contracts` / `1 contratos`; `dimmed` 24 → 0.

### 6. «Los Sobrevivientes» at 1:1 with full names — `AdminSurvivorsSlope.tsx` (the loved plate: geometry, colours, connectors, † and the phone list unchanged)
- `useMeasuredWidth`; svg `width={W}` with the 1040-unit geometry recomputed from `W` (columns `W / 5`), 1 unit = 1px; render when `W > 0`, height reserved.
- Names: `formatVendorName(seat.name, 19|20)` in `<text>` (`:361,:385`) → HTML labels (absolutely positioned `<div>` layer over the svg, `pointer-events: none`) seated with `fitLabel` from `lib/plateLabels.ts` at **11px**, ≤ 2 lines in the column width minus the amount; the amount `11.8B MXN` 9.5 → **11px** mono on its own line; non-survivor names 10px muted at opacity → 11px `var(--color-text-muted)` full alpha (2.04 → ≥ 4.5); the `†` sup 7 → 9px (aria-hidden, the legend explains it); the legend `<p>` 9px → 12px mono; the column heads (foreignObject `truncate` `:311`) → wrap, never truncate. The connectors, the ochre/grey rule, the seat order and the six-row cap stay exactly.
- Accept: `ellipsisNames` **17 → 0** on `admin`, `admin-fox`, `admin-sheinbaum` (EN + ES); `svgTextContrast` 20 → 0; `svgScale` no slope entry; every label bbox inside its column and off the connectors (collision check); the ACT III crop at 1440 EN + ES compared with `before/bands/b05.png` — same lines, same colours, full names; the 390 card list unchanged (crop).

### 7. «The itinerants» rows are disclosures — `OfficialTenureBands.tsx`
- `div role="button" tabIndex onKeyDown` rows (`:209`, ×24) → the officer name is a `<button type="button" aria-expanded aria-controls={panelId}>` (Day 6 disclosure pattern) with the row's accessible name (`Joel Arreguin Casillas, 2018–2025, 4 institutions…`), ring, `min-h-6`; the expanded panel gets the `id`; the row `div` loses `role`/`tabIndex`/handlers. Officer names `truncate` (`:225`) → `break-words`; `INST.` → `Institutions` / `Instituciones` (wrap allowed); the two change-of-government header labels (`:155-165`, absolute at `left: x%`) never overprint: below `md` they become one mono line above the grid (`PEÑA→AMLO · 2018.92 | AMLO→SHEINBAUM · 2024.75`), at `md+` they stay on the rules but are end-/start-anchored so their boxes do not intersect (measure); sort chips ringed.
- Accept: `divButtons` 24 → **0**; `focusableNonNative` 0; Tab walk reaches 24 `BUTTON+ring` names, Enter toggles `aria-expanded` and the panel appears; header-label bbox intersection 0 at 1440/1024/390 EN + ES; `ellipsis` on officer names 0; the ledger crop at 390 read.

### 8. Inks and focus across the page — `Administrations.tsx`, `SeamStrip.tsx`, `AdminSummaryCard.tsx`, `OfficialTenureBands.tsx`
- Ochre 12px kickers `Folio·XI`, `Act I/II/III` and the 15px accent numerals `3`, `2`, `18 of 24` (4.44) → `var(--color-accent-hover)` (Day 6b standard for accent type ≤ 15px; the ≥ 28px h1 span stays accent); seam-card labels `#736a65` at opacity 3.17 → full alpha; `·` separators `aria-hidden`; `Share`, `Context`, `4 documented scandals · view case file`, the download icons and every remaining control get `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-1`; `title`-only content ×6 → `aria-label` (or visible text); `?admin=` through nuqs `useQueryStates({ admin: parseAsStringLiteral(slugs).withDefault('amlo') }, { history: 'replace', clearOnDefault: true })` replacing the `useSearchParams` writer (`:176-195`) — the wayfinding/back state must survive (check `useLocation().search` readers).
- Accept: `noFocusStyleAtAll` 55 → **0** on all three files; `outlineNoneNoRing` 0; `titleOnly` 0; `failingContrast` at 1440 EN = only the 9 shared chip names (list) — nothing at `#a06820` ≤ 15px, nothing at `#ef4444`/`#f87171`/`#fb923c`/`#3b82f6`/`#eab308` as type; `/administrations?admin=fox` still opens Fox's file and `?admin=amlo` is never written (default).

## Out of scope (→ PARALLAX backlog)
- `EntityIdentityChip` high-risk names `#f59e0b` 2.15 (shared; Day 5b/7 backlog) — 9 leaves here.
- "Ricardo Garcia ." and other garbled officer/vendor names — DB, not UI.
- `ComparePeriodView` (collapsed by default, untouched today) and `GapRecoveryPanel` tier tag (Day 10 probe).
- `AdminVendorsDeepList` / `OfficialTenureBands` sort state as `useState` (view-local, same call as Days 6–7).
- Sparkline per-row auto domain (`EditorialSparkline`, shared).
- The seam chart's `†` Structure-A footnote could become a hollow-dot encoding on the 2002–2010 stretch (dataviz day, not today).

## Risk
- **Change 1 moves numbers.** Every AMLO/Sheinbaum/Peña Nieto figure on the page can change when the slices align with the API's eras; the fingerprint-equals-Σ-bars check is the gate, and the executor must report the before → after totals per administration in the report. Nothing in the backend changes.
- Change 3 and 6 replace CSS-scaled svgs with measured ones: never write state in render (React #301); render only when `W > 0`; reserve the height; test the cold load twice.
- Change 6 touches the loved plate — the judge compares the ACT III crop with `before/bands/b05.png`; any change to the connectors, colours or order is a reject.
- Change 4's tab keyboard handler must not steal `Tab`; only arrows/Home/End.
- Change 8's nuqs swap: `?admin=` is read by `adminSlug`; keep the slug vocabulary identical.

## Build notes for the executor
Same stack and rules as `DAY-07b-sectores-panel.md § Build notes`: worktree `D:\Python\yangwenli\.claude\worktrees\parallax-day01`, branch `parallax/day08-sexenios` (created from `origin/main` `4c6acd24`), servers running (backend `127.0.0.1:8001` no-scan, Vite `localhost:3009`), ≤ 2 browsers, `MSYS_NO_PATHCONV=1` + `D:/` script paths, never rubli.xyz. Order: **STEP 0 → 2 → 1 → 8 → 5 → 4 → 3 → 6 → 7**. One commit per change: `feat(administrations § PARALLAX D8 § Change N): …`, body cites `docs/parallax/DAY-08-sexenios.md § Change N`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Progress file `_parallax_shots/day08/progress.md` (one line after EVERY step, failures included); report `_parallax_shots/day08/report.md` (per change: done, every acceptance number before → after, deviations, noticed-not-fixed → backlog). Probes: extend `_parallax_shots/day08/audit8.mjs` (tag `after`) with the checks named in each Accept (fingerprint = Σ bars, seam/slope collision checks copied from `day07b/dv7b.mjs`, tab arrow-key walk, header-label bbox intersection); run EN and `LANG_ES=1` at `WIDTHS=1440,1024,390` (+ one 1920 for the frame); census `day04/clipcensus4.mjs` on `/administrations`, `?admin=fox`, `?admin=sheinbaum` at 1440/1280/1024/390 EN + ES → 0 at ≥ 1024 (390: report). Crops (`day07b/crop7b.mjs` → `day08/crop8.mjs`, never widen the viewport for mobile): masthead, seam chart + cards, tabs + card (AMLO, Sheinbaum), § I, § VOLUMEN, § III, § IV top-6 + ledger rows 1–8, § V, ACT III slope (1440 + 390), itinerants head + rows 1–6, footer, at 1440 and 390, EN + ES. **LOOK at every crop.** Review: `vercel-react-best-practices` on the diff, `rubli-bilingual-audit` on every touched TSX; gates from `frontend/`: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` · `npm run build` · `npm run lint:tokens` · `npx eslint` on touched files · vitest on touched tests. No backend change is expected — if Change 1 needs one, stop and report. Do NOT bump BUILD_ID, push, deploy or merge — Fable judges first.

## Result

Built by Opus executor `parallax-day08` (STEP 0 + Changes 2 → 1 → 8 → 5 → 4 → 3 → 6 → 7 + a crop-driven final pass, 10 commits `2563d1a7`…`d39e8c1a`), judged by Fable on the `after/` crops (seam 1440 + 390, card AMLO/Sheinbaum, § I, § IV, slope, itinerants 390, ES 390 sheet) and on an independent `audit8.mjs` run against `d39e8c1a` (`day08/judge-{en,es}.txt`, 1440/1024/390, three files). One judge round `8cbaa161` (fingerprint values never break — 6 cells × 5 files × 3 widths × 2 languages = 0 multi-line, 0 overflow; `Inst.` in an `<abbr>` below `sm`, no hyphenation anywhere).

| Measure (AMLO file, 1440 EN unless noted) | before | after |
|---|---|---|
| sub-10px text leaves (1440 / 390) | 37 / 29 (seam ticks at 5.3px) | **0 / 0** on all three files, EN + ES |
| calendar | `data.ts` AMLO 2018–24, Sheinbaum 2024–25; backend/lib 2019–24, 2025– | one calendar from `lib/administrations.ts`; fingerprint == Σ § VOLUMEN bars **5/5** EN + ES; contracts Fox 147,004 → 206,314 · Calderón 472,827 → 481,450 · Peña Nieto 1,106,381 → 1,228,625 · AMLO 1,097,708 → 1,050,552 · Sheinbaum 235,652 → 92,631; headline "2 of 5" → "3 of 5 above the national high-risk average"; AMLO GT cases 290 → 256 |
| `admin_breakdown` + `admin_{vendors,institutions}_{amlo,sheinbaum}_*` precomputes | 2026-05-15 / 06-09 on the old eras (fox 2002–05 … amlo 2018–24) — **prod identical** | recomputed by the endpoints on `_ADMIN_ERAS` (13 keys, 14:44Z, 19s + 12 × ≤ 7s); carried to prod as a plain-text SQL dump loaded in-container before the deploy |
| failing contrast | 124 (`#ef4444` ×84 as type, tints, party blue, grade `C` 1.92, ochre 12/15px) | 87 = 86 shared chip names + the 44px h1 accent (large); `riskAsType` outside the chip 0, `dimmed` 24 → 0 |
| CSS-scaled svgs | 3 (seam 1.13 / 0.67 at 390, mini-sparkline 1.24, slope 1.04) | 0 — seam, sparkline and slope at 1:1 (`useMeasuredWidth`) |
| `div[role=button]` / focusable non-native / no focus style / targets < 24px | 24 / 27 / 55 / 113 | **0 / 0 / 0 / 0** |
| truncation / ellipsised names (1440 / 390) | 3 / 17 · 41 / 17 | **0 / 0** — slope names via `fitLabel` (rows 38 → ≤ 68px), tabs and grade labels wrap, officer names full |
| headings / footers / frame | 9 (acts as `<p>`) / 2 / 1,176 | 12 (ACT I/II/III h2) / 1 / 1,010 centred (928 at 1024) |
| inner scrollers / svg clip / svg outside | 1 (seam 640px) / 1 (`11.5%`) / 3 at 390 | 0 / 0 / 0 |
| "1 contracts" | 148 occurrences across the five files | 0 |
| tabs | `role=tab` without arrows/`aria-controls` | ←/→/Home/End + `aria-controls` → `tabpanel`; Tab exits the list |
| clip census (3 routes × 1440/1280/1024/390 × EN + ES) | — | 0 at ≥ 1024; 12 per route at 390 = the shared `BenchmarkRow` (backlog) |
| gates | — | tsc 0 · build OK · lint:tokens PASS · eslint 0 new · bilingual + React review clean; no backend change |

Deviations accepted: slope rows grow with the longest name (≤ 4 lines, plate ≈ 450px) — connectors, colours and seat order unchanged, rosters follow the calendar; grade detail tiles → label · letter · value rows (11px tiles broke words letter by letter); itinerants rule labels sit on the rules from `xl` only, one line below (`Dec 2018 / Oct 2024`); the seam average label slides along the rule and shortens to `Prom. 10.9%` on the ES phone plate; the "~80 `#ef4444` numerals" were the shared chip names.

Noticed, not fixed (→ backlog): shared chip inks; shared `BenchmarkRow` clips at 390; `TemporalRiskChart`/`TrendsTab` old calendar; § IV "100% avg. risk" data check; no refresh path for the admin precomputes; pre-existing eslint warnings.

Shipped: _pending_
