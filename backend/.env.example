# Yang Wen-li Backend Environment Variables

# CORS: Comma-separated list of allowed origins
# Add your production domain before deploying
CORS_ORIGINS=http://localhost:3009,http://127.0.0.1:3009

# Database query timeout in seconds (default: 30)
DB_QUERY_TIMEOUT=30

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO
