# Yang Wen-li Backend Dependencies
# Full development environment — API server + ETL/ML pipeline.
#
# For production Docker (API only): use requirements-api.txt
# For scripts/pipeline on top of this file: pip install -r requirements-scripts.txt

# Core
numpy>=1.24.0
pandas>=2.0.0              # Used in ETL/scoring scripts (not the API)

# API Server
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
slowapi>=0.1.9              # Rate limiting for FastAPI
httpx>=0.25.0               # HTTP client (required by TestClient)

# Observability & Caching
structlog>=24.0.0          # Structured JSON logging
cachetools>=5.3.0          # Thread-safe TTL caches

# Change-point detection (used lazily by analysis_service.py)
ruptures>=1.1.0            # Structural break detection for temporal analysis

# Entity Resolution (HYPERION — scripts + hyperion package)
unidecode>=1.3.0           # Unicode to ASCII transliteration (hyperion normalizer)

# Data Processing (ETL scripts)
openpyxl>=3.1.0            # Excel file support
chardet>=5.0.0             # Character encoding detection
python-dateutil>=2.8.0

# Web Scraping (CENTINELA / ASF scrapers)
beautifulsoup4>=4.12.0     # HTML parsing (ASF scraper)
lxml>=5.0.0                # Fast XML/HTML parser (ASF scraper)
pdfplumber>=0.10.0         # PDF table extraction (ASF MDB reports)

# Utilities
tqdm>=4.65.0               # Progress bars

# AI (used by ai_explain.py router + aria_generate_memos.py)
anthropic>=0.40.0

# Authentication
python-jose[cryptography]>=3.3.0  # JWT encode/decode
passlib[bcrypt]>=1.7.4            # Password hashing

# Testing
pytest>=7.4.0              # Test framework
pytest-asyncio>=0.21.0     # Async test support
pytest-cov>=4.1.0          # Coverage reporting (70% threshold target)
